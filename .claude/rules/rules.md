# Claude Rules Index

This file is a pointer only. It is not an independent policy source.

- Use `AGENTS.md` for root repository policy.

- Use `docs/ai-developer/rules.md` for rules every AI persona must follow.

- Use `docs/ai-developer/specific_rules/gated-workflow.md` for AI-authored
  gate workflow work.

- Use `docs/ai-developer/specific_rules/new-feature.md` if you are building a
  new feature.

- Use `docs/ai-developer/specific_rules/bug-fix.md` if you are fixing a bug.

- Use `docs/ai-developer/specific_rules/hotfix.md` if the owner explicitly
  authorizes hotfix mode.

- Use `docs/ai-developer/specific_rules/docs-change.md` if you are changing
  documentation only.

- Use `docs/ai-developer/specific_rules/agent-dispatch.md` if you are
  dispatching or coordinating agents.

- Use `docs/ai-developer/specific_rules/document-standards.md` if you need the
  ADR-042 document structure reference.

- Use `docs/ai-developer/personas/manager.md` if you are the manager persona.

- Use `docs/ai-developer/personas/implementer.md` if you are the implementer
  persona.

- Use `docs/ai-developer/personas/adr-author.md` if you are the ADR author
  persona.

- Use `docs/ai-developer/personas/audit-reviewer.md` if you are the audit
  reviewer persona.

- Use `docs/ai-developer/templates/agent-dispatch-checklist-template.md` if you
  are creating a manager dispatch checklist.

- Use `docs/ai-developer/templates/agent-dispatch-prompt-template.md` if you
  are dispatching non-audit work.

- Use `docs/ai-developer/templates/agent-dispatch-audit-with-context-prompt-template.md`
  if you are dispatching an audit with issue, checklist, PR, and claimed-work
  context.

- Use `docs/ai-developer/templates/agent-dispatch-audit-no-context-prompt-template.md`
  if you are dispatching an audit without current task context.

- Use `docs/ai-developer/skills/scistudio-e2e-test/SKILL.md` if you are running
  a live end-to-end SciStudio session from a scenario file under
  `docs/ai-developer/e2e/`. Pair it with
  `docs/ai-developer/e2e/template.md` and `docs/ai-developer/e2e/README.md`.
