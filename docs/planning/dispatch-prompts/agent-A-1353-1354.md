[DISPATCH-TEMPLATE-V1: implementer]

## Task Identity

- Repository: SciStudio
- Owner request: implement ADR-039 Addendum 1 §11 — remove stash from GUI/REST/engine (#1353) and replace its role on dirty branch switch + restore with auto-commit (#1354). Combined into one PR per file-collision matrix in the manager plan.
- Task kind: feature
- Persona: implementer
- Issue: #1353 + #1354 (this PR closes BOTH)
- Issue URLs: https://github.com/zjzcpj/SciStudio/issues/1353 and https://github.com/zjzcpj/SciStudio/issues/1356 — read both bodies in full; they enumerate the exact files, line ranges, and response-shape changes.
- Umbrella PR: #<pending — see checklist `## 1`> `[DO NOT MERGE]`
- Protected branch: main
- Umbrella branch: umbrella/adr-039-addendum-1-impl
- Agent branch: feat/issue-1353-1354/remove-stash-and-auto-commit
- Agent worktree: must be created with `git worktree add` under `.claude/worktrees/agent-A-1353-1354/` off the umbrella branch
- Gate record: `.workflow/records/1353-1354-remove-stash-and-auto-commit.json`
- Checklist: `docs/planning/adr-039-addendum-1-impl-checklist.md` (edit only your own rows in `## 6` Agent A and `## 7`)

## Required Rules

Read and follow:

- The GitHub issues `#1353` and `#1354` and all owner instructions in them.
- AGENTS.md
- docs/ai-developer/rules.md
- docs/ai-developer/specific_rules/agent-dispatch.md
- docs/ai-developer/specific_rules/gated-workflow.md
- docs/ai-developer/specific_rules/new-feature.md
- docs/ai-developer/personas/implementer.md
- docs/adr/ADR-039.md §11 (Addendum 1) — the addendum is authoritative for the design

## Scope

You own ONLY these files:

**Backend:**
- `src/scistudio/core/versioning/git_engine.py` — DELETE `stash_list`/`stash_save`/`stash_apply`/`stash_drop` methods (lines 717-761); MODIFY `restore()` to replace the auto-stash block (lines 403-418) with an `engine.commit(message=f"pre-restore @ {iso_ts} (commit={short_sha[:7]}, scope=...)", prefix="auto")` call. Catch `GitError(1, "nothing to commit, working tree clean", ...)` and treat as no-op. KEEP the `_files_unchanged_vs_commit` short-circuit (lines 387-401) as-is.
- `src/scistudio/api/routes/git.py` — DELETE `StashSaveRequest` + `StashApplyRequest` classes; DELETE the 4 stash endpoints (lines 446-498); MODIFY `/restore` (lines 286-306) to remove the `if was_dirty: stashes = engine.stash_list()` block and return `{status: "ok", auto_commit_sha: <sha or null>}`. MODIFY `/branch/switch` (lines 322-360) to pre-check `engine.status()["dirty"]`; if dirty, call `engine.commit(message=f"pre-switch @ {iso_ts} (from={old_branch}, to={new_branch})", prefix="auto")` before `engine.branch_switch`. Return `{status: "ok", current_branch, auto_commit_sha: <sha or null>}`. Update the docstring on line 102 to drop `stash_apply`.
- `src/scistudio/api/app.py` — verify no stash router mount needs touching; if there is one, remove it.

**Frontend — delete entire files:**
- `frontend/src/components/Git/StashListPanel.tsx`
- `frontend/src/components/Git/StashApplyDialog.tsx`

**Frontend — edit:**
- `frontend/src/components/Git/GitTab.tsx` — remove `StashListPanel` import, `stashOpen` state, Stashes button, `<StashListPanel/>` mount, and update docstring.
- `frontend/src/components/Git/GitHistoryList.tsx` — remove `StashApplyDialog` import, `stashPrompt` state, `status === "stashed"` branch in `handleRestore`, `<StashApplyDialog/>` mount. MODIFY `handleRestore` to consume new `{status, auto_commit_sha?}` response and show an auto-commit hint via the existing local state slot (rename `stashPrompt` → `autoCommitHint` or similar; reuse the same JSX surface — do NOT touch the row layout or `handleRowClick`).
- `frontend/src/components/Git/BranchPicker.tsx` — in the `switchBranch` flow (around lines 64-73), surface a transient toast/notice when the response carries a non-null `auto_commit_sha`. Use the existing `lastError`-adjacent slot in `gitSlice` (add a `lastNotice` field if needed). Do NOT touch `handleDelete`.
- `frontend/src/store/gitSlice.ts` — remove `stashes` state + `loadStashes` thunk; remove `stashes: null` reset in invalidation block; update `switchBranch` and `restore` return types per the new shapes; add `lastNotice` state if you went that route in BranchPicker.
- `frontend/src/lib/api.ts` — remove `gitStashList`/`gitStashSave`/`gitStashApply`/`gitStashDrop`; update `gitRestore` and `gitBranchSwitch` types.
- `frontend/src/types/api.ts` — remove `GitStashEntry` (around line 363) and `GitStashApplyResult` (around line 412). Update `GitRestoreResult` shape (or whatever the type is named — find it via grep).
- `frontend/src/components/Lineage/RunDetail.tsx` — replace the `"stashed"` branch + `stashHint` message (around lines 596-599) with the new auto-commit hint: `"Your unsaved changes were committed as <short_sha> — see History tab to revert if unintended"`. The user-reported bug repro turns on this exact message change.

**Tests (REQUIRED — implementation-category PRs MUST modify tests):**
- `tests/core/test_git_engine.py` — DELETE `test_stash_*` tests. REWRITE `test_restore_round_trip` to assert an `auto: pre-restore @ ...` commit appears on HEAD when restore runs against a dirty tree. REWRITE `test_branch_create_switch_delete_cycle` (or split into pieces) to assert an `auto: pre-switch @ ...` commit appears on HEAD when switching with a dirty tree.
- `tests/api/test_git_endpoints.py` — DELETE the 4 `test_stash_*_endpoint` tests + `test_stash_save_clean_409`. REWRITE `test_restore_endpoint_*` and `test_branch_switch_*` to assert the new `auto_commit_sha` response payload and the auto-commit commits.
- `frontend/src/store/__tests__/gitSlice.test.ts` — REWRITE `switchBranch` and `restore` thunk tests for new response shapes; DELETE stash thunk tests if present.
- `frontend/src/components/Git/__tests__/GitTab.test.tsx` — DELETE stash UI tests.
- `frontend/src/components/Lineage/__tests__/RunDetail.restore.test.tsx` — REWRITE auto-commit hint assertion (replace any stash-hint test with the new "committed as <sha>" assertion).

**Docs (CHANGELOG mandatory):**
- `CHANGELOG.md` — add an entry under `[Unreleased]` `### Changed` (template: see the #1352 entry for shape). Cite both #1353 and #1354.

You must NOT touch:

- `frontend/src/components/Git/GitHistoryList.tsx::handleRowClick` (lines 72-92) — Agent B owns this
- `frontend/src/components/Git/GitHistoryList.tsx` row layout / button section (lines 247-310) — Agent B
- `frontend/src/components/Git/GitGraph/interactions.ts` — Agent B
- `src/scistudio/api/routes/git.py::branch_delete` (lines 374-382) — Agent C
- `frontend/src/components/Git/BranchPicker.tsx::handleDelete` (lines 102-114) — Agent C
- `src/scistudio/core/lineage/store.py` — Agent C
- `src/scistudio/core/versioning/git_engine.py` ADDITIONS (new `commits_reachable_only_from` / `tag` helpers) — Agent C
- `docs/adr/ADR-039.md` — already merged via #1358; read-only for you

If you need an out-of-scope path, stop and report back. Do not edit it.

## Coordination

- You are not alone in this codebase. Two other agents (B + C) will run in parallel AFTER you merge into the umbrella. Your work feeds their starting point.
- MUST work only on `feat/issue-1353-1354/remove-stash-and-auto-commit`.
- MUST work only in `.claude/worktrees/agent-A-1353-1354/`.
- MUST NOT use `pip install -e .`.
- Do not revert or overwrite other agents' work — but they have not run yet, so this rule is just about staying in your write set.
- Do not broaden scope. If you find a stash reference outside your write set, STOP and amend the gate record.
- MUST target your PR to `umbrella/adr-039-addendum-1-impl`, NOT `main`.
- MUST NOT merge the PR — owner authorizes the final umbrella → main merge.
- Edit only your checklist rows (`## 6` Agent A row and `## 7.3` Implementation rows).
- Record every completed row with a PR, commit, test, report, or gate evidence.

## TODO And Deferral Rule

Deferred work must be tracked in the repo. Use `TODO(#NNN): <reason>` and cite an issue, ADR, spec, or follow-up ticket. Do not leave hidden V1, MVP, or later work.

Known deferred items: none for this PR.

## Two-Commit Structure (REQUIRED per owner decision 2026-05-21)

Your PR MUST contain exactly two atomic commits:

**Commit 1 — `chore(#1353): remove all git stash affordances (GUI/REST/engine)`**

Pure deletion. Stash code, types, state, methods, endpoints, tests. After this commit:
- `grep -ri stash src/scistudio frontend/src tests` returns matches only in non-git contexts (axis_iter, GitGraph laneAssign local var) + CHANGELOG / docs.
- Restore on dirty tree will now FAIL with git's "your local changes would be overwritten" because nothing handles dirty state. This is the expected intermediate state — commit 2 fixes it.
- Some tests will be in a known-failing state (covered by commit 2's rewrites). Mark them xfail with `# TODO(#1354)` cite if vitest/pytest infra needs green commit-by-commit; otherwise leave as-is, GitHub validates only the tip commit.

**Commit 2 — `feat(#1354): auto-commit dirty tree on branch switch + restore (replace stash semantics)`**

Pure addition. Auto-commit logic in `engine.restore` + `routes/git.py::branch_switch`, response shape changes, frontend hint updates, test rewrites. After this commit:
- All tests green.
- Restore on dirty tree auto-commits with `auto: pre-restore @ ...` message.
- Branch switch on dirty tree auto-commits with `auto: pre-switch @ ...` message.
- Hints in `RunDetail.tsx` + `BranchPicker.tsx` say "committed as <sha>", NOT stash.

If you produce the work as a single commit accidentally, use `git reset HEAD~` + re-stage in two passes. Do NOT amend.

## Work To Do

1. Create the agent worktree: `git worktree add -b feat/issue-1353-1354/remove-stash-and-auto-commit .claude/worktrees/agent-A-1353-1354 umbrella/adr-039-addendum-1-impl`.
2. Start the gate record: `python -m scistudio.qa.governance.gate_record start --task-kind feature --issue 1353 --slug remove-stash-and-auto-commit --branch feat/issue-1353-1354/remove-stash-and-auto-commit --owner-directive "..." --include <list>`. Then `amend --include` to add the second issue `#1354` via the issue body's reference.
3. Read #1353 and #1354 in full via `gh issue view`. Read ADR-039 §11 in full. Spot-check that the line numbers in your write set are still accurate (the user merged #1361 just before you started, but it touched unrelated files).
4. Implement commit 1: delete all stash code per the write set. Stage. Run `gate_record pre-commit --staged`. Commit with the trailer block.
5. Implement commit 2: auto-commit replacement per the write set. Stage. Run `gate_record pre-commit --staged`. Commit.
6. Run the required checks (see below). Record each in the gate record.
7. Push: `git push -u origin feat/issue-1353-1354/remove-stash-and-auto-commit`.
8. Open PR: use the new helper `python scripts/scistudio_pr_create.py --gate-record .workflow/records/1353-1354-remove-stash-and-auto-commit.json --base umbrella/adr-039-addendum-1-impl --title "feat(#1353-1354): ..." --body <body>` (this wrapper runs `gate_record ci` pre-flight). Body MUST include `Closes #1353` and `Closes #1354`.
9. Finalize gate record with PR URL + commit SHA. Commit + push the finalize entry.
10. Wait for CI green + Codex review (cap 5 min). Reconcile any Codex P1 in a follow-up commit.
11. Edit your checklist rows.
12. Report back with PR URL, both commit SHAs, test/check output paths, and any blocker.

## Required Tests And Checks

- `ruff check .` from agent worktree
- `ruff format --check .` (markdown formatting is experimental — skip for `.md` files)
- `pytest tests/core/test_git_engine.py tests/api/test_git_endpoints.py -v --timeout=60`
- `cd frontend && npm test -- --run src/store/__tests__/gitSlice.test.ts src/components/Git/__tests__/GitTab.test.tsx src/components/Lineage/__tests__/RunDetail.restore.test.tsx`
- `python -m scistudio.qa.audit.full_audit --repo-root . --format json --output docs/audit/full-audit-latest.json` — record output
- Sentrux MCP: `mcp__plugin_sentrux_sentrux__{scan, session_start, rescan, check_rules, health, session_end}` — record the session_end delta vs baseline
- Chrome smoke (MANDATORY per memory `feedback_mandatory_chrome_smoke_test`): start `scistudio gui` from your worktree; open a project with at least one prior run; edit a workflow without saving; open Lineage tab; click "Restore this run's workflow"; verify the hint says "Your unsaved changes were committed as <sha> — see History tab to revert if unintended" and NO amber stash message appears anywhere. Click History tab and confirm the `auto: pre-restore @ ...` commit is at the top. Save the screenshot + a console transcript to your gate record evidence.

## Output Required

Before reporting done, provide:

- Changed file paths (full list)
- Both commit SHAs
- All test/check output (paths in the gate record)
- Sentrux delta (quality_signal before / after, rules_checked, violations)
- Chrome smoke screenshot path
- Checklist rows updated
- PR number + URL
- Any blocker or scope issue

## Stop Conditions

Stop and report back if:

- You need an out-of-scope file (especially `GitHistoryList.tsx` row layout, `branch_delete`, or `BranchPicker.tsx::handleDelete`).
- The task conflicts with AGENTS.md, ADR-039 §11, or the gate record.
- CI or local checks fail for unclear reasons.
- Sentrux quality_signal regresses materially (>5% drop from baseline 4126).
- You cannot add/update the required tests.
- The 2-commit boundary cannot be cleanly maintained — propose an alternative before producing the PR.
