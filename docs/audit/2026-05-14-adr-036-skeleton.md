# ADR-036 Skeleton Audit — 2026-05-14

**PR audited**: [#855](https://github.com/zjzcpj/SciStudio/pull/855) — `feat(adr-036): Phase 1 skeleton (#848)`
**Branch under audit**: `feat/issue-848/skeleton` → tracking branch `track/adr-036/code-editor`
**Umbrella issue**: [#843](https://github.com/zjzcpj/SciStudio/issues/843)
**Auditor**: A36-skeleton
**ADR**: [ADR-036 — Embedded code editor for project files](../adr/ADR-036.md)

---

## Verdict

**pass-with-fixes** (block merge into `track/adr-036/code-editor` until 3 P1 fixes land).

The skeleton is well-structured, comment blocks are excellent, and ADR coverage is faithful — but **3 distinct Codex P1 findings are real and reproducible**, all of them shipping-blockers for the implementation phase. None are deferrable per the overnight-merge protocol; F36-skeleton must rebase + fix in a follow-up PR before the umbrella PR is unblocked.

---

## Summary of evidence collected

| Check | Result |
|---|---|
| `ruff format --check .` | 461 files already formatted |
| `ruff check .` | All checks passed |
| `pytest tests/api/test_*_skeleton.py --timeout=60` | **20 xfailed** (expected) |
| `mypy src/scistudio/ --ignore-missing-imports` | 2 pre-existing errors in `tools_inspection.py` (NOT in this PR's diff — confirmed via `git diff --name-only`) |
| `cd frontend && npm ci` (committed lockfile) | **FAILS** — Missing 7 packages from lock file |
| `cd frontend && npm install && npm run build` | builds in 15.7s |
| `cd frontend && npx vitest run` | 124 passed, 12 skipped (the new skeleton stubs) |
| Verify Workflow Compliance CI | green |

---

## Section 1 — Checklist accuracy

All 11 rows ticked under `### Skeleton (Owner: S36)` in `docs/planning/adr-035-036-checklist.md` correspond to real artifacts in the diff:

| Row | Artifact present | Notes |
|---|---|---|
| `types.ts` discriminated union | yes (lines 196–251) | `kind` is intentionally **optional** on `WorkflowTab` for skeleton-phase backcompat — documented in the comment block; Phase 2A flips it to required. Acceptable scaffolding choice. |
| `tabSlice.ts` action stubs | yes (lines 184–223) | Three throw-stubs with cross-references to docstrings in `types.ts`. |
| `projects.py` file GET/PUT stubs | yes (lines 100–251) | NotImplementedError with full implementation-plan + edge-cases + test-plan + references. |
| `lint.py` new module | yes | Self-contained, response models defined, soft-fail behaviour documented. |
| `blocks.py` template endpoint stub | yes (lines 166–219) | NotImplementedError + plan + test plan. |
| `_templates/__init__.py` + `block_base_template.py` | yes | Compiles (verified by `python -c 'import …'` against actual exports). |
| `CodeEditor.tsx` component shell | yes | Returns a `data-testid="code-editor-skeleton"` placeholder; xterm-mirror lazy-import plan written but not executed. |
| `Toolbar.tsx` kind-switch scaffold | yes (lines 138–173) | `FileToolbar` is a no-op `return null`; kind-switch is a comment block — existing buttons untouched, per dispatch scope. |
| `ProjectTree.tsx` double-click stub | yes (lines 226–256) | Existing behaviour preserved; new branches are comment-only TODOs. |
| Test stubs | yes (3 pytest xfail files + 2 vitest skip files) | xfail catches accidental drift; rationale captured in module docstrings. |
| `package.json` lists `@monaco-editor/react` | yes (line 19) | But **lockfile not updated** → see P1-3 below. |

No out-of-scope ticks. No drift to revert.

---

## Section 2 — Comment-block sufficiency

For each NotImplementedError / `throw new Error("…")` stub:

| Stub | Purpose | Signature contract | Implementation steps | Edge cases | Test plan | References | Verdict |
|---|---|---|---|---|---|---|---|
| `read_project_file` | y | y (FileReadResponse) | y (5 numbered steps) | y (6 cases) | y (6 named tests) | y (`filesystem.py:47-65`) | **Sufficient** |
| `write_project_file` | y | y (FileWriteResponse) | y (5 steps incl. `mark_self_write` ordering) | y (5 cases incl. concurrent PUT) | y (6 named tests) | y (`workflow_watcher.py:415`) | **Sufficient** |
| `lint_python` | y | y (LintResponse) | y (5 steps) | y (4 cases) | y (6 named tests) | y (ADR §3.3 + §6) | **Sufficient** |
| `get_block_template` | y | y (BlockTemplateResponse) | y (4 steps incl. `importlib.resources`) | y (2 cases) | y (3 named tests) | y (template path) | **Sufficient** |
| `openFileTab` (TS) | y | y in types.ts L273-287 | y (3 steps) | n/a | y (4 cases) | y (ADR §3.10) | **Sufficient** |
| `saveFileTab` (TS) | y | y in types.ts L288-304 | y (4 steps) | n/a | y (3 cases) | y (ADR §3.10) | **Sufficient** |
| `updateFileTabContent` (TS) | y | y in types.ts L305-319 | y (3 steps) | n/a | y (3 cases) | y (App.tsx:478-487 reference) | **Sufficient** |
| `CodeEditor` (TSX) | y (top-of-file) | y (CodeEditorProps) | y (3 steps incl. xterm-mirror reference) | y (3 cases) | y (5 named tests) | y (TerminalView.tsx:76-88) | **Sufficient** |
| `FileToolbar` shell | y | y (props) | y (3 steps with code snippet) | n/a | y (2 cases) | y (ADR §3.7 matrix) | **Sufficient** |
| `ProjectTree.handleDoubleClick` extension | y | n/a (in-place comments) | y (4-line code snippet) | n/a | y (3 cases) | y (ADR §3.5) | **Sufficient** |

**ADR §3.10 / §3.2 / §3.3 / §3.5 / §3.7 / §3.12 all faithfully scaffolded.** An implementation agent could begin coding without re-reading the ADR.

**Deviation explicitly verified**: the dispatch's literal `from scistudio.blocks.base import Block, BlockSpec, PortSpec` is wrong against the current codebase (`BlockSpec` lives in `scistudio.blocks.registry`; only `InputPort`/`OutputPort` exist). The skeleton agent corrected this **and documented it** in:

- `block_base_template.py` lines 24–30 (top-of-file NOTE for I36c).
- `test_blocks_template_skeleton.py` lines 17–21 (test docstring loosens the assertion to "contains `from scistudio.blocks.base import`" rather than the literal wrong import line).
- PR #855 body's "Dispatch deviation flagged" section.

The triple documentation is complete. Verdict: **correct deviation handling**.

---

## Section 3 — Codex reconciliation (9 comments → 3 unique findings)

All 9 comments are 3 unique findings × 3 Codex submissions on commit `f44bf65671`.

| # | Finding | Severity | Comment IDs | Verdict | Verified by |
|---|---|---|---|---|---|
| C1 | `/{project_id:path}/file` declared after `/{project_id:path}` greedy route → unreachable | P1 | 3239884231, 3239884943, 3239885175 | **accepted** | `python -c "import …; for r in router.routes: print(r.path)"` shows `/{project_id:path}` at index 2/3/4 and `/file` at index 5/6 — FastAPI matches first-declared first, so any `GET /api/projects/<id>/file` reaches `get_project` with `project_id="<id>/file"`. Live `TestClient` confirmed `get_project` was hit. |
| C2 | `/template` declared after `/{block_type}` catch-all → unreachable | P1 | 3239884237, 3239884952, 3239885187 | **accepted** | Same inspection: `/{block_type}` at index 1, `/template` at index 4. `GET /api/blocks/template` is matched by `get_block_schema` with `block_type="template"`, returns 404 from `registry.get_spec("template") is None`. |
| C3 | Lockfile not updated for `@monaco-editor/react` → CI `npm ci` fails | P1 | 3239884240, 3239884956, 3239885192 | **accepted** | `grep -c monaco-editor frontend/package-lock.json` returns 0 on the committed state. `cd frontend && rm -rf node_modules && npm ci` fails with EUSAGE listing 7 missing packages. `.github/workflows/ci.yml` lines 151 + 188 both use `npm ci`. |

**0 deferred, 0 rejected.** Per memory `audit_p1_override`: no Codex P1 is being marked "non-blocking" — the dispatcher would override anyway, so they are accepted now.

All 9 comments have been replied to via `gh api -X POST .../replies` (sample reply URL: https://api.github.com/repos/zjzcpj/SciStudio/pulls/comments/3239925419).

---

## Section 4 — Local CI

| Step | Status |
|---|---|
| `ruff format --check .` | pass |
| `ruff check .` | pass |
| `pytest tests/api/test_*_skeleton.py --timeout=60 --no-cov` | 20 xfailed (expected) |
| `mypy src/scistudio/ --ignore-missing-imports` | 2 pre-existing errors in `src/scistudio/ai/agent/mcp/tools_inspection.py` — file is **NOT** in PR diff (`git diff --name-only` confirms). Not a regression of this PR. |
| `cd frontend && npm ci` (committed lockfile) | **FAIL** — see C3 |
| `cd frontend && npm install && npm run build` | pass (15.7s) |
| `cd frontend && npx vitest run` | 124 passed, 12 skipped |

All Python checks green. Frontend build green after `npm install` regenerates the lockfile (which then is the fix for C3).

---

## Section 5 — Risks for the implementation phase

| Risk | Mitigation already in skeleton | Owed to F36-skeleton or I36a/b/c |
|---|---|---|
| Implementation agent picks the wrong import line in template | Triple-documented (template + test docstring + PR body) | I36c verifies actual `base/__init__.py` exports at impl time |
| Self-write suppression coordination ordering | Comment-block in `write_project_file` calls it out explicitly: `mark_self_write` MUST run **before** `os.replace` | I36a's test_write_file_self_write_suppression asserts call order |
| Auto-save vs lint timing race (800 ms save vs 600 ms lint) | Documented in CodeEditor.tsx top-of-file | I36b implementation |
| Bundle size (Monaco ~600 KB gzip) | Lazy-import requirement called out three times: ADR §3.1, CodeEditor.tsx top, Phase 2B checklist | I36b enforces; e2e bundle-size measurement is out of scope for skeleton |

---

## Section 6 — Required follow-up before unblocking the umbrella PR

**F36-skeleton must:**

1. Move `@router.get("/{project_id:path}/file", ...)` and `@router.put("/{project_id:path}/file", ...)` decorators + handlers in `src/scistudio/api/routes/projects.py` to **above** line 34 (the existing `GET /{project_id:path}` decorator). Add a regression test asserting the route registration order.
2. Move `@router.get("/template", ...)` decorator + handler in `src/scistudio/api/routes/blocks.py` to **above** line 101 (the existing `GET /{block_type}/schema` decorator). Add a regression test.
3. Run `cd frontend && npm install` to regenerate `package-lock.json` with the 7 new packages (`@monaco-editor/react`, `monaco-editor`, `@monaco-editor/loader`, `state-local`, `dompurify`, `marked`, `@types/trusted-types`). Commit the lockfile. Verify `cd frontend && rm -rf node_modules && npm ci` succeeds.

All 3 are mechanical — under 50 LOC of code change + a regenerated lockfile. Skeleton tests should still xfail after the fix; runtime contract is unchanged.

---

## Section 7 — Acceptance

This audit recommends **block merge of #855 into `track/adr-036/code-editor`** until F36-skeleton lands the 3 fixes above. Once merged, all S36 checklist rows can stay ticked (no row is invalidated by the route-ordering fixes — they're internal route registration order, not contract changes).

— A36-skeleton, 2026-05-14
