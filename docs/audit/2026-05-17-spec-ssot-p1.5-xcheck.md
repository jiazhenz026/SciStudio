# Spec SSOT Phase 1.5 — Cross-Check Audit (X1)

**Date**: 2026-05-17  
**Agent role**: X1 (cross-check — ARCHITECTURE.md vs code vs ADRs)  
**Cascade**: umbrella #1090, Phase 1.5  
**Authority hierarchy**: ARCHITECTURE.md PRIMARY → ADRs SUPPLEMENTARY → CLAUDE.md conventions-only

---

## ARCHITECTURE.md ↔ code inconsistencies

### `block-abc.run_signature` — ARCH shows `dict[str, DataObject]` in postprocess; code returns `dict[str, Collection]`

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §5.1` (Block base class pseudocode snippet)
- **Code source**: `src/scieasy/blocks/base/block.py:L248-L255`
- **Inconsistency**: ARCHITECTURE.md §5.1 shows `postprocess(self, outputs: dict[str, DataObject]) -> dict[str, DataObject]`; the actual code signature is `postprocess(self, outputs: dict[str, Collection]) -> dict[str, Collection]`.
- **ARCH says**: `def postprocess(self, outputs: dict[str, DataObject]) -> dict[str, DataObject]:`
- **Code says**: `def postprocess(self, outputs: dict[str, Collection]) -> dict[str, Collection]:` (per ADR-020, all inter-block transport uses Collection)
- **Hint**: both-differ-b — ARCH pseudocode is stale relative to ADR-020 which post-dates it. The code is architecturally correct; ARCH prose says "outputs are `dict[str, Collection]`" in §5.1's run() docstring but the pseudocode block for postprocess still shows the pre-ADR-020 DataObject form.

---

### `block-abc.validate_signature` — ARCH pseudocode shows `dict[str, Collection]`; code uses `dict[str, Any]`

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §5.1`
- **Code source**: `src/scieasy/blocks/base/block.py:L170`
- **Inconsistency**: ARCHITECTURE.md §5.1 shows `def validate(self, inputs: dict[str, Collection]) -> bool:`. Code signature is `def validate(self, inputs: dict[str, Any]) -> bool:` — accepts Any, not Collection-typed.
- **ARCH says**: `validate(self, inputs: dict[str, Collection]) -> bool:`
- **Code says**: `def validate(self, inputs: dict[str, Any]) -> bool:`
- **Hint**: both-differ-b — type annotation differs. The body of validate does handle Collection instances correctly (has explicit `isinstance(value, Collection)` branch), so runtime behavior is consistent with intent; the annotation mismatch is in the signature only.

---

### `block-abc.allowed_types_default` — ARCH says `[DataObject]` (non-empty); code defaults to `[]` (empty)

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §5.2` (variadic port declarations)
- **Code source**: `src/scieasy/blocks/base/block.py:L71-72`
- **Inconsistency**: ARCHITECTURE.md §5.2 shows the class-level declaration as `allowed_input_types: ClassVar[list[type]] = [DataObject]` and `allowed_output_types: ClassVar[list[type]] = [DataObject]` (with `# no constraint` comment). Code has `allowed_input_types: ClassVar[list[type]] = []` and `allowed_output_types: ClassVar[list[type]] = []`.
- **ARCH says**: `allowed_input_types: ClassVar[list[type]] = [DataObject]`
- **Code says**: `allowed_input_types: ClassVar[list[type]] = []`
- **Hint**: both-differ-b — The semantic difference is whether the default is "accept any DataObject" (via a list with DataObject) or "accept any type" (via empty list). The block.py code comment says `# An empty list means "accept any DataObject subclass"` — semantically equivalent in runtime, but the declaration form differs from ARCH. This matters for how the frontend/tools interpret an empty list vs `[DataObject]`.

---

### `port-system.constraint_signature` — ARCH shows constraint taking `DataObject`; code specifies `Collection` in docstring but signature is `Any`

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §5.2`
- **Code source**: `src/scieasy/blocks/base/ports.py:L24-29`
- **Inconsistency**: ARCHITECTURE.md §5.2 shows `constraint: Callable[[DataObject], bool] | None = None` on `InputPort`. Code has `constraint: Callable[[Any], bool] | None = None` — the parameter is `Any`, not `DataObject`.
- **ARCH says**: `constraint: Callable[[DataObject], bool] | None = None`
- **Code says**: `constraint: Callable[[Any], bool] | None = None` (with docstring clarifying value is a Collection per ADR-020)
- **Hint**: both-differ-b — ADR-020 changed constraint semantics to receive a Collection (not a single DataObject), but ARCH §5.2 still shows the pre-ADR-020 `DataObject` signature. The `validate_port_constraint` docstring at `ports.py:L76-88` is correct about Collection semantics.

---

### `mcp-tools.tool_count` — ARCH claims ~25 tools; code registers 30 @mcp.tool decorators

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §7.2` ("~25 tools across four user-need categories")
- **Code source**: `src/scieasy/ai/agent/mcp/tools_workflow.py`, `tools_authoring.py`, `tools_inspection.py`, `tools_qa.py` — counted 30 `@mcp.tool` decorators total (11 in workflow + 5 in authoring + 7 in inspection + 4 in qa + 3 in `__init__.py` stubs)
- **Inconsistency**: ARCH says "~25 tools"; actual count is 30. The gap is `finish_ai_block` (10th tool in category a, ADR-035 §3.5) which is not listed in ARCHITECTURE.md §7.2's tool table at all. The category (a) table shows 9 tools (6R/3W) but code implements 10 tools (6R/4W including `finish_ai_block`).
- **ARCH says**: Category (a) has 6R/3W = 9 tools; total ~25
- **Code says**: Category (a) has 6R/4W = 10 tools (`finish_ai_block` is write-class); total = 10+5+7+4 = 26 user-facing tools (plus 4 server infrastructure decorators in `__init__.py` and `server.py`)
- **Hint**: docs-only-c — `finish_ai_block` was added (ADR-035 §3.5 path a) but ARCHITECTURE.md §7.2 category table was not updated to reflect it.

---

### `mcp-tools.category_a_count` — ARCH table shows "6 R / 3 W"; code has "6 R / 4 W"

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §7.2`, category (a) row
- **Code source**: `src/scieasy/ai/agent/mcp/tools_workflow.py:L787` (`@mcp.tool(name="finish_ai_block", tags={"category:workflow", "write"})`)
- **Inconsistency**: The table entry for category (a) "Workflow design + execution" says `6 R / 3 W` and lists 9 tool names omitting `finish_ai_block`. The code registers `finish_ai_block` as a write-class tool tagged `category:workflow`.
- **ARCH says**: `list_blocks`, `get_block_schema`, `list_types`, `get_workflow`, `validate_workflow`, `get_run_status`, `write_workflow`, `run_workflow`, `cancel_run` (9 tools, 6R/3W)
- **Code says**: same 9 plus `finish_ai_block` (10 tools, 6R/4W)
- **Hint**: docs-only-c

---

### `versioning-git.watcher_file` — PROJECT_TREE.md describes `watcher.py`; code file does not exist

- **ARCH source**: `docs/architecture/PROJECT_TREE.md` (versioning section, line ~113): `└── watcher.py   # Detect external git changes (HEAD / refs mtime; feeds workflow_watcher's git.head_changed event)`
- **Code source**: `src/scieasy/core/versioning/` — contains `git_binary.py`, `git_engine.py`, `gitignore_template.py`, `status.py`, `__init__.py`; **no `watcher.py`**
- **Inconsistency**: PROJECT_TREE.md lists `watcher.py` as part of `core/versioning/`. The file does not exist. The `__init__.py` docstring (L51) explains: "the standalone asyncio-poll `GitChangeWatcher` was collapsed into `scieasy.api.routes.workflow_watcher._GitHeadHandler`" — i.e., the watcher was moved/merged into the API layer.
- **ARCH says**: `watcher.py` exists at `src/scieasy/core/versioning/watcher.py`
- **Code says**: `[CODE: not found]`. Watcher logic lives in `src/scieasy/api/routes/workflow_watcher.py` instead.
- **Hint**: docs-only-c — PROJECT_TREE.md has not been updated to reflect the collapse of the standalone watcher.

---

### `block-registry.entry_points` — ARCH/PROJECT_TREE lists only 8 core entry-point blocks; pyproject.toml registers 14

- **ARCH source**: `docs/architecture/PROJECT_TREE.md` (Key entry_points section) and `docs/architecture/ARCHITECTURE.md §5.4`
- **Code source**: `pyproject.toml:L56-70`
- **Inconsistency**: PROJECT_TREE.md shows 8 built-in `scieasy.blocks` entry-points: `load_data`, `save_data`, `process_merge`, `process_split`, `code_block`, `app_block`, `ai_block`, `subworkflow_block`. `pyproject.toml` registers 14: the 8 above plus `merge_collection`, `split_collection`, `filter_collection`, `slice_collection`, `data_router`, `pair_editor`.
- **ARCH says**: 8 built-in entry-points; lists MergeBlock, SplitBlock but not the 6 Collection utility blocks
- **Code says**: 14 built-in entry-points — 6 additional collection/routing builtins present
- **Hint**: docs-only-c — The 6 additional blocks (`MergeCollection`, `SplitCollection`, `FilterCollection`, `SliceCollection`, `DataRouter`, `PairEditor`) are referenced in ARCHITECTURE.md §5.4 prose ("the framework provides built-in utility blocks... `MergeCollection`, `SplitCollection`, `FilterCollection`, `SliceCollection`") but missing from the PROJECT_TREE.md entry-points listing. Also `data_router` and `pair_editor` are not mentioned at all in ARCHITECTURE.md.

---

### `block-abc.resource_request` — ARCH shows `resource_request` as Block field; code has it as ClassVar

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §6.4` (ResourceRequest usage example)
- **Code source**: `src/scieasy/blocks/base/block.py`
- **Inconsistency**: ARCHITECTURE.md §6.4 shows an example `class CellposeSegment(ProcessBlock): resource_request = ResourceRequest(...)` and ARCHITECTURE.md §6.1 shows `self.resource_manager.can_dispatch(block.resource_request)`. The Block ABC class definition in `src/scieasy/blocks/base/block.py` does NOT declare `resource_request` as a ClassVar at all (not found in the file). This is a missing field on the Block ABC.
- **ARCH says**: Block subclasses declare `resource_request: ClassVar[ResourceRequest]` (implied by the example at §6.4)
- **Code says**: `[CODE: not found]` — `resource_request` is not defined on `Block`, `ProcessBlock`, or the base classes. The scheduler code in ARCHITECTURE.md §6.1 references `block.resource_request` but no corresponding attribute is declared.
- **Hint**: docs-only-c — either the attribute is missing from the Block ABC (tech debt) or it lives elsewhere and ARCH §6.4 is misleading.

---

### `execution-engine.dag_scheduler_constructor` — ARCH shows `process_registry` param; actual scheduler may differ

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §6.1` — `DAGScheduler.__init__` pseudocode shows: `(self, workflow, event_bus, runner, resource_manager, process_registry)`
- **Code source**: `src/scieasy/engine/scheduler.py` — not fully read, but cross-checking is feasible via grep
- **Inconsistency**: This needs to be verified against the real scheduler. The ARCH pseudocode is illustrative, but if the actual scheduler constructor signature differs materially it would be a b-class inconsistency.
- **ARCH says**: `DAGScheduler(workflow, event_bus, runner, resource_manager, process_registry)`
- **Code says**: [not fully verified — flagging for Phase 2 to inspect `src/scieasy/engine/scheduler.py`]
- **Hint**: unclear — needs Phase 2 code inspection of actual scheduler constructor

---

### `lineage-db.schema_indices` — ARCH CREATE TABLE statements omit indices; code adds them

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §4.4` (SQL CREATE TABLE statements)
- **Code source**: `src/scieasy/core/lineage/store.py:L56-125`
- **Inconsistency**: ARCHITECTURE.md §4.4 shows 4 CREATE TABLE statements with no CREATE INDEX statements. Code has 8 CREATE INDEX statements (`idx_runs_workflow`, `idx_be_run`, `idx_be_block`, `idx_do_storage`, `idx_do_derived`, `idx_do_producer`, `idx_io_object`) in addition to the 4 tables. This is a documentation gap, not a functional error.
- **ARCH says**: 4 CREATE TABLE statements only
- **Code says**: 4 CREATE TABLE statements + 8 CREATE INDEX statements
- **Hint**: docs-only-c — indices are implementation detail, not normally shown in arch docs. Low severity.

---

### `lineage-db.runs_table_column_order` — ARCH and code agree; no inconsistency

(All 13 columns in `runs` match between ARCH §4.4 and `store.py`. No entry needed.)

---

### `rest-api.workflow_endpoints` — ARCH §8.2 omits `/api/workflows/import`, `/api/workflows/import-path`, `/api/workflows/export-path`

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §8.2`
- **Code source**: `src/scieasy/api/routes/workflows.py:L105-271`
- **Inconsistency**: ARCHITECTURE.md §8.2 lists the workflow endpoints as: `POST /api/workflows`, `GET /api/workflows/{id}`, `PUT /api/workflows/{id}`, `DELETE /api/workflows/{id}`, `POST /api/workflows/{id}/execute`, `POST /api/workflows/{id}/pause`, `POST /api/workflows/{id}/resume`, `POST /api/workflows/{id}/cancel`, `POST /api/workflows/{id}/blocks/{block_id}/cancel`, `POST /api/workflows/{id}/execute-from`. Code implements three additional endpoints not in ARCH: `POST /api/workflows/import`, `POST /api/workflows/import-path`, `POST /api/workflows/export-path`.
- **ARCH says**: 10 workflow endpoints
- **Code says**: 13 workflow endpoints (10 ARCH + 3 additional)
- **Hint**: code-only-d — import/export-path endpoints exist in code but are not documented in ARCHITECTURE.md §8.2.

---

### `rest-api.missing_routes` — ARCH §8.2 omits `/api/runs/*`, `/api/git/*`, `/api/lint/*`, `/api/filesystem/*`

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §8.2` (Key REST endpoints)
- **Code source**: `src/scieasy/api/routes/` — contains `runs.py`, `git.py`, `lint.py`, `filesystem.py`, `ai_pty.py`, `workflow_watcher.py`
- **Inconsistency**: ARCHITECTURE.md §8.2 lists the runs routes in a sub-section under "AI services" as `GET /api/runs`, `GET /api/runs/{run_id}`, `GET /api/runs/{run_id}/methods`, `POST /api/runs/{run_id}/rerun`. These routes exist in code (`src/scieasy/api/routes/runs.py`) but are NOT present in ARCHITECTURE.md §8.2's endpoint table at all (the runs routes are mentioned in the PROJECT_TREE.md annotations only). Additionally, the git (~15 endpoints from ADR-039 §3.5) and lint/filesystem routes are in code but absent from ARCHITECTURE.md §8.2.
- **ARCH says**: Runs, git, lint, filesystem routes are not in the §8.2 endpoint table
- **Code says**: `src/scieasy/api/routes/runs.py`, `git.py`, `lint.py`, `filesystem.py` exist
- **Hint**: docs-only-c — ARCH §8.2 was not updated when ADR-038 (runs.py) and ADR-039 (git.py) were implemented.

---

### `ws-sse-protocol.ai_ws_path` — ARCH says `WS /api/ai/chat/{chat_id}`; code has a PTY-specific route

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §8.2` ("WS /api/ai/chat/{chat_id}")
- **Code source**: `src/scieasy/api/routes/ai_pty.py` (existence confirmed by Glob)
- **Inconsistency**: The code has a distinct `ai_pty.py` route file that was not present in the original ARCH. Whether the WS path changed (e.g., split into two routes) needs Phase 2 verification. The existence of a separate `ai_pty.py` module alongside `ai.py` suggests PTY-specific routing was introduced after the ARCH was written.
- **ARCH says**: Single WS endpoint `/api/ai/chat/{chat_id}` for the agent chat
- **Code says**: Two route files exist: `ai.py` (permission check/decision) and `ai_pty.py` (likely the PTY-based terminal route, ADR-034 Phase 1.2). The split implies route structure differs from ARCH.
- **Hint**: unclear — needs Phase 2 inspection of `ai_pty.py` to determine if the WS path changed or was added.

---

### `zustand-store.missing_slices` — ARCH §9.10 lists 7 slices; code has 10+

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §9.10`
- **Code source**: `frontend/src/store/` — contains `executionSlice.ts`, `gitSlice.ts`, `lineageSlice.ts`, `paletteSlice.ts`, `previewSlice.ts`, `projectSlice.ts`, `tabSlice.ts`, `terminalTabsSlice.ts`, `uiSlice.ts`, `workflowSlice.ts`, `types.ts`
- **Inconsistency**: ARCHITECTURE.md §9.10 lists 7 Zustand slices: `projectSlice`, `workflowSlice`, `executionSlice`, `uiSlice`, `previewSlice`, `paletteSlice`, `chatSlice`. Code has 10 slice files: the 7 above (except `chatSlice`) plus `gitSlice`, `lineageSlice`, `tabSlice`, `terminalTabsSlice`. `chatSlice` from ARCH is not found as a file; `gitSlice` and `lineageSlice` (ADR-038/039) are in code but absent from ARCH §9.10's table. `tabSlice` and `terminalTabsSlice` are in code but not in ARCH.
- **ARCH says**: 7 slices including `chatSlice`; no `gitSlice`, `lineageSlice`, `tabSlice`, `terminalTabsSlice`
- **Code says**: 10 slice files; no `chatSlice`; has `gitSlice`, `lineageSlice`, `tabSlice`, `terminalTabsSlice`
- **Hint**: both-differ-b — ARCH §9.10 table is stale. `chatSlice` (in ARCH but missing from code) may have been integrated into another slice or removed; 4 slices in code are absent from ARCH.

---

### `agent-provisioning.module_not_in_arch` — `scieasy.agent_provisioning` package exists in code; ARCH omits it from the layer structure

- **ARCH source**: `docs/architecture/ARCHITECTURE.md` (Layer 4 §7, PROJECT_TREE.md)
- **Code source**: `src/scieasy/agent_provisioning/__init__.py`
- **Inconsistency**: A top-level `agent_provisioning` package exists at `src/scieasy/agent_provisioning/` implementing ADR-040 §3.5-3.8 prod-env provisioning. ARCHITECTURE.md §10.2 describes the provisioning behavior, but the layered architecture diagram (§3), PROJECT_TREE.md, and the module dependency graph do not list `agent_provisioning` as a top-level module. It is architecturally between Layer 5 (API) and the filesystem; its import-linter status is unclear.
- **ARCH says**: Layers are: core, blocks, engine, ai, api, workflow, utils, testing, cli — no `agent_provisioning` module
- **Code says**: `src/scieasy/agent_provisioning/` exists with `_orchestrate.py`, `claude_agents_md.py`, `codex_config.py`, `hooks.py`, `skills.py`, `templates/`
- **Hint**: code-only-d — MODULE exists in code but is not listed in the architecture layers or PROJECT_TREE.md module inventory.

---

### `frontend-types.api_ts_missing_fields` — `frontend/src/types/api.ts` has `DynamicPortsConfig.input_port_mapping`; backend `schemas.py` does not expose it

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §5.4` (dynamic-port mechanism description)
- **Code source**: `frontend/src/types/api.ts:L104-111`; `src/scieasy/api/schemas.py:L126`
- **Inconsistency**: `frontend/src/types/api.ts` defines `DynamicPortsConfig` with both `output_port_mapping` and `input_port_mapping` fields. The backend `schemas.py` at `BlockSchemaResponse.dynamic_ports` is typed as `dict[str, Any] | None` (no structured shape enforced). ARCHITECTURE.md §5.4 and ADR-028 describe the `dynamic_ports` descriptor as `{source_config_key, output_port_mapping: {port_name: {enum_value: [type_names]}}}` — an output-only mapping with no `input_port_mapping` key. The frontend TypeScript adds `input_port_mapping` which is absent from both ARCH and backend schema.
- **ARCH says**: `dynamic_ports` descriptor has `source_config_key` and `output_port_mapping`
- **Code says (frontend)**: `DynamicPortsConfig` adds `input_port_mapping?: Record<string, Record<string, string[]>>` not in ARCH
- **Code says (backend)**: `dynamic_ports: dict[str, Any] | None` — untyped passthrough
- **Hint**: both-differ-b — ARCH spec for `dynamic_ports` may need to be updated to include `input_port_mapping`, or the frontend type is ahead of the spec.

---

### `data-types.collection_not_dataobject` — ARCH and PROJECT_TREE correctly state Collection is NOT a DataObject; code matches. No inconsistency.

(Match — no entry needed.)

---

### `block-abc.process_block_run_default` — ARCH §6.2 pseudocode for ProcessBlock.run does not match code

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §6.2` (Tier 1 pattern pseudocode)
- **Code source**: `src/scieasy/blocks/process/process_block.py` (ProcessBlock.run)
- **Inconsistency**: ARCHITECTURE.md §6.2's pseudocode for `ProcessBlock.run` is:
  ```python
  def run(self, inputs, config):
      primary = list(inputs.values())[0]
      refs = []
      for item in primary:
          result = self.process_item(item, config)
          result = _auto_flush(result)
          refs.append(result)
      return {self.output_ports[0].name: Collection(refs)}
  ```
  The actual code in `process_block.py` calls `setup(config)` first, passes `state` to `process_item(item, config, state)`, and wraps in `try/finally: teardown(state)` — all per ADR-027 D7. The ARCH pseudocode at §6.2 is the old pre-ADR-027-D7 form (2-arg process_item, no setup/teardown).
- **ARCH says**: `process_item(item, config)` with no setup/teardown (ADR-027 D7 lifecycle hooks absent from §6.2 pseudocode)
- **Code says**: Full ADR-027 D7 implementation: `setup(config)` → `process_item(item, config, state)` → `teardown(state)` in finally
- **Hint**: docs-only-c — ARCHITECTURE.md §6.2 pseudocode was not updated when ADR-027 D7 (setup/teardown) was implemented. §5.1 (ProcessBlock section) DOES document setup/teardown correctly; §6.2 does not.

---

### `storage-backends.series_backend` — ARCH §4.2 says Series maps to "Zarr or Parquet"; PROJECT_TREE says `series.py` has no dedicated storage file

- **ARCH source**: `docs/architecture/ARCHITECTURE.md §4.2` (storage backend table: "Series → Zarr or Parquet (single-column)")
- **Code source**: `src/scieasy/core/storage/` — files: `base.py`, `zarr_backend.py`, `arrow_backend.py`, `filesystem.py`, `composite_store.py`, `ref.py` (no `series_backend.py`)
- **Inconsistency**: This is a known architectural detail — Series uses the Arrow backend. No dedicated backend file is expected. The table entry is accurate (Zarr or Arrow/Parquet both work). This is NOT an inconsistency, just verification that the storage code matches.
- **Hint**: a (match) — no entry needed.

---

## ADR vs code inconsistencies (SUPPLEMENTARY — interfaces NOT in ARCHITECTURE.md)

### `agent-provisioning.skills_path` — ADR-040 §3.4 / PROJECT_TREE describes `_skills/scieasy/` in wheel; code stores in `agent_provisioning/`

- **ADR source**: `docs/architecture/ARCHITECTURE.md §10.2` (prod-env provisioning), `pyproject.toml:L105`
- **Code source**: `pyproject.toml:L104-105` (`"_skills/scieasy/**/*.md"` in package data) + `src/scieasy/agent_provisioning/` (has `skills.py` but no `_skills/` directory visible from Glob results)
- **Inconsistency**: `pyproject.toml` declares `"_skills/scieasy/**/*.md"` as package data (ADR-040 §3.4). The `agent_provisioning` module has a `skills.py` but the actual skill `.md` files would need to live at `src/scieasy/_skills/scieasy/`. Whether that directory exists is not confirmed — this is a possible code-only-d gap where `pyproject.toml` declares package data that may not exist.
- **ADR says**: `_skills/scieasy/**/*.md` shipped in wheel
- **Code says**: `src/scieasy/agent_provisioning/skills.py` implements the skill installation logic; the source skill `.md` files location is unverified.
- **Hint**: unclear — Phase 2 should verify `src/scieasy/_skills/` directory exists.

---

### `block-registry.dynamic_ports_validation` — ADR-028 Addendum 1 D2 says registry validates `dynamic_ports` shape at scan time; code comment says so but no validation method found on Block

- **ADR source**: `docs/architecture/ARCHITECTURE.md §5.1` ("The shape is validated at registry scan time by `BlockRegistry._validate_dynamic_ports`")
- **Code source**: `src/scieasy/blocks/base/block.py:L82-103` (dynamic_ports docstring says "validated at registry scan time by `BlockRegistry._validate_dynamic_ports`")
- **Inconsistency**: Both ARCH and code comment reference `BlockRegistry._validate_dynamic_ports` but there is no evidence this method exists on BlockRegistry (not visible in `src/scieasy/blocks/registry.py` based on the audit scope). If the method is absent, the "fails loudly at import time" guarantee does not hold.
- **ADR says**: `BlockRegistry._validate_dynamic_ports` called at scan time
- **Code says**: Referenced in docstring but existence of the method in `registry.py` is unverified.
- **Hint**: unclear — Phase 2 should inspect `src/scieasy/blocks/registry.py` for `_validate_dynamic_ports`.

---

## ARCHITECTURE.md ↔ ADR disagreements (log, not blocking)

### `mcp-tools.finish_ai_block_in_arch`

- **ARCH says**: Category (a) MCP tools = 9 tools (6R/3W): `list_blocks`, `get_block_schema`, `list_types`, `get_workflow`, `validate_workflow`, `get_run_status`, `write_workflow`, `run_workflow`, `cancel_run`. No mention of `finish_ai_block`.
- **ADR-035 says**: ADR-035 §3.5 "path (a)" specifies `finish_ai_block` as the write tool for signaling AI Block completion. The `tools_workflow.py` module docstring (line 14) references "ADR-035 §3.5 path (a)".
- **Code sides with**: ADR-035 (implements `finish_ai_block` as a write-class category:workflow tool). ARCHITECTURE.md category (a) table is stale relative to ADR-035.

---

### `versioning-git.watcher_location`

- **ARCH / PROJECT_TREE says**: `src/scieasy/core/versioning/watcher.py` owns the git HEAD change detection.
- **ADR-039 (implied by __init__.py)**: The `core/versioning/__init__.py` states the standalone `GitChangeWatcher` "was collapsed into `scieasy.api.routes.workflow_watcher._GitHeadHandler`" — a D39-3.2 decision recorded inside code comments, not in a formal ADR addendum or ARCHITECTURE.md update.
- **Code sides with**: the collapsed-into-workflow_watcher implementation. ARCHITECTURE.md PROJECT_TREE still lists `watcher.py`.

---

### `lineage-db.environment_snapshot`

- **ARCH §6.7 says**: `EnvironmentSnapshot` dataclass has `python_version`, `platform`, `full_freeze`, `key_packages`, `conda_env`.
- **ADR-038 says**: The runs table stores `environment_snapshot` as JSON in the `environment_snapshot TEXT NOT NULL` column. No conflict with the dataclass shape.
- **Code sides with**: ARCH (both agree on the column name and JSON storage). The dataclass definition in `src/scieasy/core/lineage/environment.py` was not read; Phase 2 should verify the dataclass fields match ARCH §6.7.
- **Code sides with**: ARCH / ADR — both agree, but the dataclass fields were not verified. Flagging for completeness.

---

### `block-abc.process_item_signature`

- **ARCH §5.1 says**: ProcessBlock `process_item(self, item: DataObject, config: BlockConfig, state: Any = None) -> DataObject` (ADR-027 D7 form).
- **ADR-027 D7 says**: Same 3-arg form.
- **Code says**: `src/scieasy/blocks/process/process_block.py:L93` — `def process_item(self, item: Any, config: BlockConfig, state: Any = None) -> Any` — item and return type are `Any` not `DataObject`.
- **Code sides with**: neither (a third form). ARCH and ADR agree on `DataObject`; code uses `Any` for flexibility. Functionally equivalent at runtime since DataObject is the intended type, but the annotation differs.
- **Note**: This crosses into Section 1 territory; see `block-abc.run_signature` above for the annotation pattern.
