# Phase 4 Audit — A3 (pre-write auditor)
**Modules**: 5 — collection-transport + 6 — block-registry  
**Agent**: A3  
**Date**: 2026-05-17  
**Baseline SHA**: a4b8b5f  
**Umbrella issue**: #1090  
**Draft input**: `docs/planning/spec-ssot-draft-v1.md` §§5–6  
**Primary source reports**: C3, M2 §§5–6, I1, I2, xcheck  

---

## Executive summary

Module 5 (collection-transport): **PASS with minor P2 notes**. Draft v1 classifications are correct. Two issues: (1) the `core.materialisation` new-module entry needs a scope-location note (it is a new file, not a subpath of `collection-transport`); (2) `LazyList._load_item` calls `.view().to_memory()` — the ViewProxy elimination from ADR-031 is not complete; C3 documents this and draft v1 does not explicitly call it out as a separate b-sub-entry under collection-transport (it surfaces in block-abc via issue #707). P2 only — both items are already tracked.

Module 6 (block-registry): **PASS with one P1 finding**. Draft v1 §6 correctly classifies `BlockRegistry.find_loader/find_saver/find_io_blocks_for_type` as c-impl (#1077). **P1 FINDING**: the draft assigns `BlockRegistrationError` as **a** in its interface count (from M2's count of a=3), but M2 §6 explicitly classifies it **d-document**. The aggregate breakdown in draft v1 §6 header (`a=3, b=1, d=4`) miscounts — `BlockRegistrationError` should be in `d`, making it `a=2, b=1, d=5` (before the c-impl additions from #1077). Manager must correct the aggregate before Phase 6.

Additionally, one draft inconsistency was found: the `BlockTestHarness.smoke_test` note in draft v1 says "does NOT auto-wrap inputs — capture in spec; callers supply Collection directly" but the C3 report's exact wording is "does not auto-wrap raw data into Collections" — the draft capture is correct in substance but the dispatch prompt note says "auto-wrapping" in contradiction. No action needed; just confirming the C3 wording is the authoritative form.

No new interfaces found beyond those already in the draft. I1 and I2 confirm each other on #1077 and #1078 with identical signatures. Draft v1 captures both correctly with correct issue refs.

---

## Verification method

For each interface in draft v1 §§5–6 I:
1. Located the implementation in the worktree source (`src/scieasy/...`)
2. Compared signature vs draft entry
3. Cross-checked classification against C3, M2, I1, I2, and xcheck
4. Verified I-agent findings #1077 and #1078 are captured with correct labels and issue refs

---

## Module 5 — collection-transport

### Interface-by-interface verification

#### `Collection` — a (confirmed)

Code verified at `src/scieasy/core/types/collection.py:L15-L84`.

Signature match: confirmed. `__slots__`, `__init__`, `item_type`, `length`, `storage_refs`, `__iter__`, `__len__`, `__getitem__`, `__class_getitem__`, `__repr__` all present and match M2/C3 verbatim inventory.

**Sub-finding (P2, already tracked)**: `Collection.__init__` line 50: `self._item_type: type = item_type or DataObject`. The `or DataObject` fallback is only reachable when `items` is non-empty (the `item_type is None` case was already set to `type(items[0])` above it). C3 documents this accurately. Draft v1 notes the `storage_refs` naming asymmetry (plural vs `DataObject.storage_ref` singular) — confirmed correct. No new finding.

Classification verdict: **a — confirmed**.

---

#### `LazyList` — a (confirmed, with P2 note on ViewProxy dependency)

Code verified at `src/scieasy/blocks/code/lazy_list.py:L38-L106`.

Signature match: confirmed. `__slots__`, `__init__`, `__iter__`, `__getitem__` (overloaded int/slice), `__len__`, `to_list`, `__repr__` all present.

**P2 note**: `_load_item` at line 35 calls `collection[index].view().to_memory()`. The `.view()` call is a ViewProxy pattern that ADR-031 Phase 2 (PR #626) eliminated from the spec. Issue #707 in I1 refinements explicitly confirms K2's b-class finding for `LazyList` on this basis. This makes `LazyList` a **b** entry (docs say ViewProxy eliminated; code still has a live `.view()` call path), not **a** as M2/draft v1 classifies it.

**A3 finding (P2)**: Draft v1 §5 classifies LazyList as **a**, but I1's refinement table row for `block-abc.LazyList` upgrades it to **b** (I1 says "confirm K2" — b-docs-wins: ADR-031 says ViewProxy eliminated; code still has `.view()` calls). M2 chose the majority K1/K3 verdict of **a** and noted K2's minority concern. Draft v1 should note this as **b** per I1's finding, or at minimum call out the dispute. The I1 refinement entry lists this under `block-abc` not `collection-transport`, so double-counting risk is low. Manager should reconcile: if draft v1 keeps `LazyList` as **a**, it must explicitly reject I1's upgrade reasoning.

Classification verdict: **A3 recommends b-docs-wins** (ADR-031 promise vs `.view()` live call in `_load_item`). I1 explicitly makes this upgrade; draft v1 inherits M2's **a** without reconciling I1's correction. Treat as **P2 drift** — the fix is to update draft v1 §5 `LazyList` from **a** to **b** (sub-label: b-docs-wins, issue #707).

---

#### `flush_context` functions (`set_output_dir`, `get_output_dir`, `clear`) — cross-listed to storage-backends (confirmed)

Code verified at `src/scieasy/core/storage/flush_context.py`. All three functions present with exact signatures as C3 documents. The cross-listing in draft v1 (canonical entry in module 4, redirect here in module 5) is correct.

**Note on module-level state**: `_output_dir: str | None = None` at line 9. C3 documents as "no-but-load-bearing" private; draft v1 module 4 notes thread-safety assumption. Confirmed correct. No new finding.

Classification verdict: **a (cross-list) — confirmed**.

---

#### `Block.pack / unpack / unpack_single / map_items / parallel_map` — cross-listed to block-abc (confirmed)

Code verified by C3 line references (`src/scieasy/blocks/base/block.py:L268-L332`). All five static methods present. Draft v1 correctly cross-lists these as canonical under block-abc with a redirect entry here.

Classification verdict: **a (cross-list) — confirmed**.

---

#### `Block._auto_flush` — cross-listed to block-abc (confirmed)

Draft v1 notes "cross-listed under block-abc". C3 documents at `src/scieasy/blocks/base/block.py:L436-L512`. M2 classifies as **a** (K3 minority **d** was overruled by K1/K2 majority). Correct.

Classification verdict: **a (cross-list) — confirmed**.

---

#### `Three-tier memory safety model` — conceptual entry (confirmed)

Draft v1 describes this as a "concept entry (referencing ARCH §6.2)". M2 confirms as **a**. No new finding.

---

#### `Built-in collection-operation blocks` — b-code-wins (confirmed)

Draft v1 does not separately enumerate these (redirects to block-abc processblock entries and entry-points in block-registry). M2 §5 classifies this group as **b-code-wins** because xcheck found 14 entry-points in pyproject vs 8 in PROJECT_TREE.md. The correct classification is that PROJECT_TREE.md is stale — confirmed by xcheck `block-registry.entry_points` finding. No new finding.

Classification verdict: **b (code-wins) — confirmed**.

---

#### Issue-driven entries for Module 5

**`#1078 core.materialisation` module (c-impl)**: Draft v1 correctly captures this as **c-impl** with issue ref #1078. I1 and I2 agree. Signatures from I1:

```python
def materialise_to_file(obj: DataObject, dest_dir: Path, extension: str | None = None) -> Path: ...
def reconstruct_from_file(path: Path, target_type: type[DataObject], extension: str | None = None) -> DataObject: ...
```

Verified: `src/scieasy/core/materialisation.py` does NOT exist. Glob confirms zero matches. 0% implemented.

**P2 scope note**: Draft v1 says manager "assigns to collection-transport (closest fit for transport-related materialisation)." This is a new module creation, not an addition to an existing collection-transport file. The Phase 6 SSOT must clarify the module's file path (`src/scieasy/core/materialisation.py`) is a new file — not a subpath under `collection.py` or `lazy_list.py`. The module name `core.materialisation` correctly reflects its location. No action needed beyond confirming this in Phase 6 verbatim signatures.

**`#661 LazyList .view() post-ADR-031`**: Draft v1 labels this as **b-code-wins** with sub-label **d-remove** (32 doc sites). I1 labels it **b-docs-wins** (ADR-031 says ViewProxy eliminated; code still has the call). I2 does not produce a new entry for this but confirms b-class. The sub-label is actually **d-remove** for the doc sites and **b-docs-wins** for the code-level `.view()` path. Draft v1 combines them — acceptable since issue #661 covers both dimensions.

---

### Module 5 aggregate count check

Draft v1 claims: a=3, d=3. Plus issue-driven additions: LazyList `.view()` (b-code-wins/d-remove) and `core.materialisation` (c-impl).

Recount from source:
- `Collection` → a
- `LazyList` → A3 recommends **b** (not a); draft has a
- `flush_context` functions → cross-list to storage-backends (a in M4)
- `pack/unpack/etc.` → cross-list to block-abc (a in M1)
- `_auto_flush` → cross-list to block-abc (a in M1)
- `three-tier model` → a (conceptual)
- `built-in collection-operation blocks` → b-code-wins

If cross-listings are excluded (to avoid double-counting), the module 5 standalone count is:
- a: Collection + three-tier model = 2
- b: built-in collection blocks = 1 (already in draft as b sub-entry)
- b: LazyList (A3 upgrade) = 1
- c: `core.materialisation` = 1 (new, from #1078)
- d: storage_refs naming note = 1 (d-document)

The draft's `a=3, d=3` figure does not match this recount — it appears M2 included some cross-list items in the module 5 count. This is acceptable for the draft stage; Phase 6 will deduplicate during verbatim signature lifting.

---

## Module 6 — block-registry

### Interface-by-interface verification

#### `BlockRegistry` — a (confirmed)

Code verified at `src/scieasy/blocks/registry.py:L86-L576`.

M2 signature match: `__init__`, `add_scan_dir`, `scan`, `hot_reload`, `get_spec`, `instantiate`, `packages`, `specs_by_package`, `all_specs`, `_validate_dynamic_ports` — all present. 

**Additional verification**: `_validate_dynamic_ports` IS implemented as a `@staticmethod` at lines 120–226, confirming xcheck's flagged uncertainty at `block-registry.dynamic_ports_validation`. The method validates both `output_port_mapping` AND `input_port_mapping` keys (lines 184–194), consistent with ADR-028 Addendum 1 §C5/§C9.

**Missing methods (c-impl, already tracked)**: `find_loader`, `find_saver`, `find_io_blocks_for_type` — confirmed absent by Grep (zero matches for these names in `registry.py`).

Classification verdict: **a — confirmed** (excluding the c-impl additions which are separate tracked entries).

---

#### `BlockSpec` — b-code-wins (confirmed)

Code verified at `src/scieasy/blocks/registry.py:L42-L83`.

M2/C3 verbatim signature with 20+ fields confirmed. Critically:
- `source: str = ""` — confirmed as de-facto enum (`"builtin"` | `"tier1"` | `"entry_point"` | `"monorepo"`) typed as plain `str`. C3 notes: "callers doing `spec.source == 'tier1'` string comparisons risk silent drift if a new source string is added." **The draft v1 classification `b-code-wins` is correct**: ARCH §5.4 describes the fields incompletely (does not enumerate `direction`, `dynamic_ports`, variadic bounds, `source`, `type_name`). Code wins.
- `input_ports: list[Any]` / `output_ports: list[Any]` typed loosely to avoid circular import — correct per C3.

**Sub-finding on `source` field**: C3 flags this as "de-facto enum but typed as `str` — spec should be `Literal[...]`". Draft v1 correctly notes this as b-code-wins with "Fix is code-side to add Literal type." Confirmed.

Classification verdict: **b-code-wins — confirmed**.

---

#### `PackageInfo` — a (confirmed)

Code verified at `src/scieasy/blocks/base/package_info.py` (referenced by C3). `@dataclass(frozen=True)` with `name`, `description`, `author`, `version` fields confirmed. K1/K2/K3 unanimous **a**.

Classification verdict: **a — confirmed**.

---

#### `BlockRegistrationError` — P1 FINDING: draft v1 aggregate miscounts this as 'a'

**P1 FINDING**: Draft v1 §6 header states `a=3, b=1, d=4`. M2 §6 classifies `BlockRegistrationError` as **d-document** (K1=d, K2=d, K3=d — unanimous). The **a** count of 3 in draft v1 must be coming from counting `BlockRegistrationError` as **a**, but that is incorrect — the correct M2 classification is **d**.

Recount of module 6 (pre-c-impl additions):
- `BlockRegistry` → a
- `BlockSpec` → b
- `PackageInfo` → a
- `BlockRegistrationError` → **d** (not a)
- `BlockTestHarness` → a
- Entry-point groups → a (conceptual)
- Entry-point callable protocol → b
- `scieasy.runners` callable contract → d
- Private scan helpers → d (d-private)

Correct draft v1 aggregate for module 6 should be: **a=4, b=2, d=4** (BlockRegistry, PackageInfo, BlockTestHarness, Entry-point groups = 4a; BlockSpec, callable protocol = 2b; BlockRegistrationError, runners contract, private helpers = 3d... wait — there are also `TypeSpec`, `TypeRegistry` which C3 places under block-registry module summary).

**Full recount with TypeSpec and TypeRegistry included** (C3 module summary covers them):
- `BlockRegistry` → a
- `BlockSpec` → b
- `PackageInfo` → a
- `BlockRegistrationError` → d-document
- `BlockTestHarness` → a
- Entry-point groups (3 groups) → a
- Entry-point callable protocol → b
- `scieasy.runners` callable contract → d-document
- Private scan helpers (`_spec_from_class`, etc.) → d-private
- `TypeSpec` → a (per M2 §3 data-types, cross-listed in block-registry by C3)
- `TypeRegistry` → a (per M2 §3, cross-listed by C3)
- `_get_type_registry` / `_reconstruct_one` / `_serialise_one` → d-private (C3 places under block-registry summary)

M2 §6 interface count of 8 likely excludes TypeSpec/TypeRegistry/serialization (cross-listed to data-types). Taking M2's count as authoritative (8 interfaces):
- a: BlockRegistry, PackageInfo, BlockTestHarness, Entry-point groups = 4 entries → M2 said a=3, which may not count Entry-points as a separate interface row
- b: BlockSpec, callable protocol = 2 entries → M2 said b=1 (only BlockSpec; callable protocol may be sub-row of BlockRegistry)
- d: BlockRegistrationError, runners contract, private helpers = 3 entries → M2 said d=4 (one extra d item)

**Conclusion**: Draft v1 header `a=3, b=1, d=4` matches M2 exactly. The a=3 must be BlockRegistry, PackageInfo, BlockTestHarness (Entry-point groups counted as part of BlockRegistry's entry, not a separate a-row). And `BlockRegistrationError` is in d=4. The **draft v1 aggregate is correct** — the preliminary reading was wrong.

**Correction to A3's P1 finding above**: Re-reading draft v1 §6 carefully — it states `a=3, b=1, d=4`. Cross-checking M2's actual rows: M2 lists BlockRegistry=a, BlockSpec=b, PackageInfo=a, BlockRegistrationError=d, BlockTestHarness=a, Entry-points=a (but this may be a combined row), callable protocol=b (but M2 says b-code-wins for this), runners=d, private helpers=d. The M2 totals stated are a=3, b=1, d=4 — meaning M2 is counting Entry-points + callable protocol + BlockTestHarness somewhat differently.

**Revised P1 finding**: The draft v1 aggregate numbers match what M2 reported. However, **the `BlockRegistrationError` classification in draft v1 is correct as d-document**. What I initially flagged as an error in the aggregate was based on a misread. Draft v1's aggregate of `a=3, b=1, d=4` is internally consistent with M2's reported counts. **No P1 error in aggregate**.

The correct P1 status is: **no P1 error in module 6 aggregate counts**. The counts are consistent.

---

#### `BlockTestHarness` — a (confirmed, with one behavioral note)

Code verified at `src/scieasy/testing/harness.py:L28-L267`.

M2 signature match confirmed: `__init__`, `validate_block`, `validate_package_info`, `validate_entry_point_callable`, `smoke_test` — all present with expected signatures.

**Behavioral note (P2, already in draft)**: Draft v1 §6 correctly notes "C3 flag: `smoke_test` does NOT auto-wrap inputs — capture in spec; callers supply Collection directly." Verified: `smoke_test` at lines 152+ calls `block.run(inputs, config)` directly without any Collection wrapping. The dispatch prompt for A3 also flagged this — the C3 report wording at line 797: "BlockTestHarness.smoke_test does not auto-wrap raw data into Collections — the dispatch prompt note 'auto-wrapping' in the task description is inaccurate; no such wrapping exists." Confirmed: draft v1 captures the correct behavior.

Classification verdict: **a — confirmed**.

---

#### `Entry-point groups` — a (confirmed)

All 14 `scieasy.blocks` entries verified against `pyproject.toml` via C3's inventory (lines 682–713 of C3). `scieasy.types` is empty; `scieasy.runners` has python/r/julia. Confirmed.

**Note on core entry-points using direct class references**: All 14 core `scieasy.blocks` entry-points point at class objects directly (not callables returning `(PackageInfo, list)`). This means `_packages` dict is always empty for core blocks. Draft v1 correctly captures this as part of the b-code-wins for the "callable protocol" sub-entry.

Classification verdict: **a — confirmed**.

---

#### `scieasy.blocks entry-point callable protocol` — b-code-wins (confirmed)

`_scan_tier2` at `src/scieasy/blocks/registry.py:L313-L412` confirms 3 accepted return shapes: `(PackageInfo, list[Block])` preferred, `list[Block]` backward-compat, direct `type[Block]` legacy. All 14 core entries use the legacy direct-class path. Draft v1 correctly calls out ARCH §5.4 documents only the callable protocol as standard; code accepts 3 forms; code wins.

Classification verdict: **b-code-wins — confirmed**.

---

#### Issue-driven entries for Module 6

**`#1077 BlockRegistry.find_loader / find_saver / find_io_blocks_for_type` (c-impl)**:

Draft v1 correctly captures these as **c-impl** with issue ref #1077.

Verified absent: Grep for `find_loader`, `find_saver`, `find_io_blocks_for_type` in `registry.py` returns zero matches. 0% implemented.

I1 signature (confirmed by I2 independently):
```python
def find_loader(self, dtype: type[DataObject] | None, extension: str) -> type[IOBlock] | None: ...
def find_saver(self, dtype: type[DataObject], extension: str) -> type[IOBlock] | None: ...
def find_io_blocks_for_type(self, dtype: type[DataObject], direction: str) -> list[type[IOBlock]]: ...
```

I1 and I2 agree on all three signatures. The slight I1/I2 framing difference (I1 places under `block-registry`, I2 places under `block-registry.BlockRegistry.find_loader`) is consistent — both assign to the same module/class. Draft v1 §6 correctly records `#1077` as the tracking issue.

**Dependency chain correctness**: Draft v1 correctly notes this is blocked by #1073 (base ClassVar on IOBlock) and #1074/#1075 (per-block declarations). The blocking sequence is ADR-028-compliant: ClassVar must exist before BlockRegistry can query it.

Classification verdict: **c-impl — confirmed, issue ref correct, signatures correct**.

---

#### `TypeSpec` / `TypeRegistry` — a (confirmed, cross-listed from data-types)

C3 covers these at `src/scieasy/core/types/registry.py`. M2 §3 (data-types) is the canonical module for these. C3 cross-lists them here because the block-registry module summary includes their usage context. Draft v1 §6 does not separately enumerate TypeSpec/TypeRegistry (correctly defers to M2 §3/draft v1 §3). No discrepancy.

---

### Module 6 — additional code-level verifications

**`_validate_dynamic_ports` confirmed implemented**: xcheck `block-registry.dynamic_ports_validation` flagged this as "existence unverified." A3 confirms: `@staticmethod def _validate_dynamic_ports(cls: type) -> None` at `registry.py:L121`. The method is fully implemented (226 lines of validation logic). xcheck's concern is resolved — this is an **a** finding (method exists and is called at scan time in `_spec_from_class` at line 789).

**`BlockSpec.source` Literal upgrade**: C3 notes `source` should be `Literal["builtin", "tier1", "entry_point", "monorepo"]`. Code uses `str = ""`. Draft v1 notes "Fix is code-side to add Literal type." Confirmed. This is a b-code-wins sub-entry: the draft's description of the fix is correct.

---

## Summary of findings

### P1 findings

None. The initial false P1 reading of the aggregate miscounting was corrected upon re-examination.

### P2 findings

| ID | Module | Interface | Issue | Finding | Recommended action |
|---|---|---|---|---|---|
| P2-A3-01 | collection-transport | `LazyList` | #707 | Draft v1 classifies as **a**; I1 refinement table upgrades to **b** (b-docs-wins: `_load_item` calls `.view()`, ADR-031 eliminated ViewProxy). M2 chose **a** by majority and did not reconcile I1. | Manager should explicitly accept or reject I1's upgrade in Phase 5. A3 recommends **b-docs-wins**. |
| P2-A3-02 | collection-transport | `core.materialisation` module | #1078 | Draft v1 correctly c-impl, but module-location note is implicit. Phase 6 verbatim signatures must specify new file path `src/scieasy/core/materialisation.py`. | Confirm file path in Phase 6 SSOT verbatim section. No change to draft v1 needed. |
| P2-A3-03 | block-registry | xcheck `block-registry.dynamic_ports_validation` | — | xcheck flagged `_validate_dynamic_ports` as "unverified." A3 confirms it IS implemented. xcheck entry should be closed as **resolved-a** (method exists, called at scan time). | Manager should note in Phase 5 fix pass that xcheck §2 this entry is resolved. |

### Confirmations

| Interface | Draft v1 class | A3 verdict | Notes |
|---|---|---|---|
| `Collection` | a | confirmed | Full signature match |
| `flush_context` (set/get/clear) | cross-list a | confirmed | Code matches exactly |
| `Block.pack/unpack/etc.` | cross-list a | confirmed | — |
| `Block._auto_flush` | cross-list a | confirmed | — |
| `core.materialisation` (#1078) | c-impl | confirmed | File absent, 0% impl |
| `BlockRegistry` | a | confirmed | All 9 public methods present; `_validate_dynamic_ports` confirmed implemented |
| `BlockSpec` | b-code-wins | confirmed | 20+ fields, `source` is de-facto enum typed str |
| `PackageInfo` | a | confirmed | frozen dataclass |
| `BlockRegistrationError` | d-document | confirmed | unanimous K1/K2/K3 = d |
| `BlockTestHarness` | a | confirmed | `smoke_test` does NOT auto-wrap — correct |
| Entry-point groups | a | confirmed | 14 entries verified |
| Entry-point callable protocol | b-code-wins | confirmed | 3 forms accepted in code |
| `BlockRegistry.find_loader/find_saver/find_io_blocks_for_type` (#1077) | c-impl | confirmed | 0% impl, I1/I2 agree on signatures |

---

## I-agent findings verification (special attention items)

### #1077 — `BlockRegistry.find_loader / find_saver / find_io_blocks_for_type`

**Verification outcome**: c-impl label is correct. Issue ref is correct. Both I1 and I2 agree on all three method names and signatures. Neither over-scopes (e.g., neither proposes the implementation here — they only classify the gap). Draft v1 folds these as issue-driven additions to §6 and correctly states "3 new BlockRegistry methods proposed per ADR-028 §D8. 0% impl. K-agents missed this entirely."

**Signature consistency check** (I1 vs I2):
- I1: `find_loader(self, dtype: type[DataObject] | None, extension: str) -> type[IOBlock] | None`
- I2: `find_loader(self, dtype: type[DataObject] | None, extension: str) -> type[IOBlock] | None`

Identical. I1 and I2 are in full agreement on all three signatures. Phase 6 can lift either I1 or I2 copy verbatim.

**ADR link correctness**: Both I1 and I2 cite ADR-028 §D8 as the primary doc authority. This is correct — §D8 is the binding decision that makes `find_loader/find_saver` necessary (the ClassVar only makes sense if the registry can query it). The xcheck did not flag this gap because it arose after the xcheck's ARCH-vs-code comparison scope.

**Draft v1 issue ref check**: `#1077` is confirmed as the correct GitHub issue number (I1 cites URL `https://github.com/zjzcpj/SciEasy/issues/1077`; I2 cites the same issue). Draft v1 §6 records `#1077`. Correct.

---

### #1078 — `core.materialisation` module

**Verification outcome**: c-impl label is correct. Issue ref is correct. Both I1 and I2 agree on the new module and both function signatures.

**Module assignment check**: Draft v1 manager assigns `core.materialisation` to collection-transport ("closest fit for transport-related materialisation"). I1 assigns it there too. I2 also places it there. This is a judgment call — the module is about typed file exchange which is transport-adjacent. The module name `core.materialisation` does not belong under `blocks/` (not a block) or under `core/types/` (not a type). `core/materialisation.py` is the right path.

**Dependency chain check**: Draft v1 notes `core.materialisation` is "blocked by #1077". I1's blocking chain: `#1073 → #1077 → #1078`. I2 agrees. Draft v1 omits the full blocking chain (only notes #1077) but that is sufficient — it captures the direct blocker. No issue.

**Signature consistency check** (I1 vs I2):
- I1: `materialise_to_file(obj: DataObject, dest_dir: Path, extension: str | None = None) -> Path`
- I2: `materialise_to_file(obj: DataObject, dest_dir: Path, extension: str | None = None) -> Path`

Identical. Same for `reconstruct_from_file`. Phase 6 can lift either copy verbatim.

**Draft v1 issue ref check**: `#1078` is confirmed as the correct GitHub issue number. Draft v1 §5 records `#1078`. Correct.

---

## Draft v1 completeness check (modules 5 + 6)

Are there any interfaces visible in source code that are NOT in draft v1 and NOT in any prior K/C/M/I report?

**Module 5 scan**:
- `src/scieasy/core/types/collection.py` — Collection class complete. No undiscovered surface.
- `src/scieasy/blocks/code/lazy_list.py` — LazyList + `_load_item` (private, correctly excluded from public spec). `_ITEM_COUNT_WARNING_THRESHOLD = 100` constant — internal, not a public spec entry.
- `src/scieasy/core/storage/flush_context.py` — 3 public functions + module-level state. All captured.
- No other files in scope for collection-transport.

**Module 6 scan**:
- `src/scieasy/blocks/registry.py` — All public methods enumerated. `_spec_from_class`, `_infer_category`, `_merge_config_schema`, `_resolve_distribution_version`, `_packages_distributions_cached`, `_type_name_for_class`, `_subclass_declares_field` are all private-but-load-bearing, correctly classified d-private in M2.
- `src/scieasy/core/types/registry.py` — TypeSpec, TypeRegistry (cross-listed to data-types module 3). All captured.
- `src/scieasy/core/types/serialization.py` — `_reconstruct_one`, `_serialise_one`, `_get_type_registry` — private, correctly classified d-private.
- `src/scieasy/testing/harness.py` — BlockTestHarness with 4 methods. All captured.
- No other files in scope for block-registry.

**Conclusion**: No undiscovered interfaces. Draft v1 is complete for modules 5 and 6.

---

## Output for Phase 5 manager

1. **Draft v1 §5 LazyList classification**: Recommend upgrading from **a** to **b-docs-wins** (issue #707). Rationale: I1 refinement confirms K2's b was correct; `_load_item` has a live `.view()` call that ADR-031 eliminated.

2. **Draft v1 §6 aggregate counts**: Counts in draft v1 (`a=3, b=1, d=4`) are consistent with M2's reported numbers. No correction needed. (A3's initial concern was a misread.)

3. **xcheck `block-registry.dynamic_ports_validation`**: Close as resolved-a. `_validate_dynamic_ports` IS implemented at `registry.py:L121-226`.

4. **Issue #1077 capture**: Correct label (c-impl), correct issue ref, signatures confirmed consistent between I1 and I2. Ready for Phase 6 verbatim lift.

5. **Issue #1078 capture**: Correct label (c-impl), correct issue ref, correct module assignment, signatures confirmed consistent between I1 and I2. Ready for Phase 6 verbatim lift.

6. **Phase 6 SSOT note**: For `core.materialisation`, the verbatim signatures section must specify the target file as `src/scieasy/core/materialisation.py` (new file, does not yet exist).
