# A4-2 Audit — M03/M04 Manager ABCD Label Verification
Date: 2026-05-17  
Auditor: Codex (A4-2)  
Scope: **Phase-3 manager draft + code only** for M03/M04

## Inputs reviewed
- Manager draft: `docs/audit/codex/phase-3/2026-05-17-manager-draft.md`
- M03 code surface: `src/scieasy/engine/**`
- M04 code surface: `src/scieasy/ai/**`, `src/scieasy/blocks/ai/**`, `src/scieasy/api/routes/ai.py`, `src/scieasy/api/routes/ai_pty.py`

---

## Verification outcome

### M03

1. **DAG lifecycle protocol** — manager label: **A**  
   **Audit verdict: KEEP A (no correction).**  
   Reason: DAG build/sort/readiness + event-driven lifecycle transitions are explicit and coherent in code (`dag.py`, `scheduler.py`, `events.py`), with no obvious contract contradiction inside the examined scope.

2. **Checkpoint wire schema details** — manager label: **B (docs-fix)**  
   **Audit verdict: KEEP B (no correction).**  
   Reason: checkpoint wire shapes (collection envelope + storage-ref-like dict fields) are implemented, but schema authority is code-scattered (`checkpoint.py`) rather than visibly centralized in one normative contract artifact within reviewed inputs.

3. **Scheduler responsibility width** — manager label: **B (code-change)**  
   **Audit verdict: KEEP B (no correction).**  
   Reason: `DAGScheduler` still owns orchestration + state transitions + dispatch gating + interactive completion handling + cancellation propagation + lineage hookups, indicating broad responsibility. This supports boundary-tightening code work, but not enough evidence (from scoped inputs) to escalate to C.

### M04

1. **AI proposal vs runtime validation boundary** — manager label: **A**  
   **Audit verdict: KEEP A (no correction).**  
   Reason: runtime-side control remains explicit in code paths reviewed (AI block execution mode, output handling, and server/tool boundaries), with no observed inversion where proposal path bypasses runtime ownership.

2. **Dual runtime paths auditability** — manager label: **B (docs-fix)**  
   **Audit verdict: KEEP B (no correction).**  
   Reason: code shows multiple cooperating surfaces (AI block runtime, PTY routing/mapping, MCP server/tool layer, provider status probing). Behavior exists, but end-to-end decision/audit path is not self-evident from one canonical contract source in the reviewed scope.

---

## Corrections to manager draft

**None.** For M03/M04, A4-2 finds the Phase-3 manager ABCD labels currently consistent with the examined code.

## Final correction matrix (A4-2)

| module | interface_item | manager_label | corrected_label | correction_needed |
|---|---|---|---|---|
| M03 | DAG lifecycle protocol | A | A | no |
| M03 | checkpoint wire schema details | B | B | no |
| M03 | scheduler responsibility width | B | B | no |
| M04 | AI proposal vs runtime validation boundary | A | A | no |
| M04 | dual runtime paths auditability | B | B | no |
