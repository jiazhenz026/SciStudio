# Audit report — Skeleton (track/adr-035/ai-block-pty)
Date: 2026-05-14
PR reviewed: #856 (`feat/issue-844/skeleton` → `track/adr-035/ai-block-pty`, NOT YET merged)
Checklist rows verified: 10 / 10

## Summary
**pass** — every Skeleton (S35) row maps to a real artifact in PR #856; ADR section references are accurate; comment blocks are sufficient for Phase 2 implementation agents to start without re-reading the ADR; CI is green; no out-of-scope file modified; no drift into other phases.

## Checklist drift
None. Verified via `git show origin/feat/issue-844/skeleton:docs/planning/adr-035-036-checklist.md`:

- All 10 rows under "Skeleton (Owner: S35)" are ticked with `→ PR #844-skeleton …` artifact notes.
- Zero rows ticked under Phase 2A / 2B / 2C / Audit & Fix / ADR-036 / e2e sections.
- Pre-existing `[x] Pre-flight Phase 0 complete` (line 163) untouched.

The `→ PR #844-skeleton (sha pending commit)` placeholder annotation is acceptable per the dispatcher's note (post-merge sweep will replace with real PR URL/sha). Recommend the dispatcher sweep these 10 rows after merge.

## ADR compliance + comment sufficiency

For every NotImplementedError stub, comment block contains: Purpose, Signature contract, Implementation steps, Edge cases, Test plan, References (ADR § + analogous sibling-file line ranges where helpful). Spot-checked all 7 backend files + 3 frontend files.

| File | ADR §s referenced | Stubs | Sufficiency |
|---|---|---|---|
| `src/scistudio/blocks/ai/ai_block.py` | §3.1, §3.2, §3.3, §3.5, §3.6, §3.7, §3.8, §3.9, §8 | `run`, `validate_config`, `_build_spawn_argv` | **Sufficient** — `run()` plan enumerates 12 steps + 6 edge cases + 12 tests; references `app_block.py` as analogue |
| `src/scistudio/blocks/ai/run_dir.py` | §3.2, §3.4, §3.5, §6.1 | `__init__`, `create`, `write_manifest`, `mcp_signal_path`, `mark_done_signal_path`, `copy_transcript` | **Sufficient** — manifest schema reproduced inline at module top; references `bridge.py:31-142` for atomic-write pattern |
| `src/scistudio/blocks/ai/completion.py` | §3.3, §3.5, §3.9 | `__init__`, `wait`, `cancel` | **Sufficient** — three signal paths + precedence explicit; references `app/watcher.py` to reuse FileWatcher rather than reinvent |
| `src/scistudio/engine/pty_control.py` | §3.10; ADR-034 §8 | `request_pty_tab`, `notify_block_pty_event` | **Sufficient** — IPC message shapes specified; references `engine/runners/worker.py` IPC bootstrap |
| `src/scistudio/api/routes/ai_pty.py` | §3.10; ADR-034 §8 | `open_engine_initiated_tab` | **Sufficient** — call out that the existing route lines 62-226 are sibling, not modified; flags that I35b will need to extend the existing route to look up pre-spawned PTYs by tab_id (good forward-pointer) |
| `src/scistudio/ai/agent/mcp/tools_workflow.py` | §3.5(a), §8 OQ-1 | `finish_ai_block` | **Sufficient** — three error envelopes spelled out; references `_context.get_context` for the AI-block-context lookup |
| `frontend/src/components/AIChat/TerminalTab.tsx` | §3.5(c), §3.9 | `AiBlockStatusBadge`, `MarkDoneButton` (return null) | **Sufficient** — both functions have full I35c implementation plan + 4-test plan in JSDoc |
| `frontend/src/components/AIChat/TerminalTabs.tsx` | §3.10 | `handleBlockPtyOpened`, `handleBlockPtyClosed` (throw) | **Sufficient** — payload shape declared in TS types; calls out the new `addAiBlockTerminalTab` store action I35c needs to add |
| `frontend/src/hooks/useWebSocket.ts` | §3.10, §6.1 | `block_pty_opened` / `block_pty_closed` switch cases (return early) | **Sufficient** — flags the Logs-panel append for lineage |

ADR section numbers spot-verified against `docs/adr/ADR-035.md` headings — every referenced § exists (§§3.1–3.10, §4 Delete, §6.1, §8 OQ-1/OQ-2 are all real).

No divergence from ADR-035 in the surface scaffolding.

## CI status
- **ruff format --check .**: pass (463 files already formatted)
- **ruff check .**: pass (All checks passed!)
- **pytest tests/ai/ tests/blocks/ai/ tests/engine/ --timeout=60**: 491 passed, 6 skipped (platform), 69 xfailed (skeleton). Matches PR description verbatim.
- **pytest full suite --timeout=60**: 13 unrelated test failures + 1 timeout (`test_appblock_exchange_dir_cleanup`) — all reproduced on baseline `origin/track/adr-035/ai-block-pty` before this PR is applied. **Pre-existing, NOT introduced by S35.** Failing groups: `tests/api/test_projects.py` (Permission), `tests/blocks/code/test_codeblock_python_integration.py`, `tests/cli/test_mcp_bridge.py`, `tests/integration/test_multimodal_workflow.py` (6), `tests/integration/test_phase2_mcp_end_to_end.py`. These are tracking-branch debt, not S35 regressions.
- **mypy --ignore-missing-imports** on the 7 changed source files: pass (Success: no issues found in 7 source files).
- **Frontend `npm run build`**: pass (built in 16.18s; standard pre-existing chunk-size warning).
- **Frontend `vitest run`**: pass (17 files / 128 passed / 13 skipped).
- **Live Chrome smoke**: N/A — skeleton phase has no behavior; per dispatch, kicks in for Phase 2.5.

## Findings (P1/P2/P3)
- **None (P1)**.
- **None (P2)**.
- **P3 — checklist artifact resolution**: 10 ticked rows annotate `→ PR #844-skeleton (sha pending commit)`. Recommend dispatcher sweeps the actual PR URL (`https://github.com/zjzcpj/SciStudio/pull/856`) and merge-commit sha into these annotations after merge into `track/adr-035/ai-block-pty`. Cosmetic only; does not block.
- **P3 — pre-existing tracking-branch test debt**: 13 + 1 = 14 unrelated failures on the tracking branch. Not introduced by S35 and out of scope for this audit, but worth a separate triage issue before Phase 2 implementation agents stumble on them. Suggested issue title: "ADR-035 tracking branch — pre-existing test debt baseline (13 fails + 1 timeout)".

## Codex reconciliation
- **PR #856 has 0 Codex review comments** at audit time (`gh api repos/zjzcpj/SciStudio/pulls/856/comments` → `[]`; `gh api repos/zjzcpj/SciStudio/issues/856/comments` → 0 comments). Codex auto-review presumably skipped a pure-skeleton PR (every body is `raise NotImplementedError`); nothing to accept/defer/reject. Per memory `audit_p1_override` no P1 carry-over because there are no Codex comments to defer.

## Recommendation for fix agent (if any)
**No fix agent needed for the skeleton phase.** Skeleton is mergeable as-is.

Sequence:
1. Dispatcher merges PR #856 into `track/adr-035/ai-block-pty`.
2. Dispatcher sweeps the 10 `→ PR #844-skeleton (sha pending)` annotations to point at PR #856 URL + merge sha.
3. (Optional, not blocking) Dispatcher opens a follow-up issue to triage the 14 pre-existing tracking-branch failures before I35a/I35b/I35c kick off.
4. Dispatch Phase 2 implementation agents (I35a backend runtime, I35b engine PTY + MCP, I35c frontend tab integration) with explicit pointers to the comment blocks in each skeleton file as their per-function spec.

## References
- PR diff: `https://github.com/zjzcpj/SciStudio/pull/856`
- Tracking branch: `track/adr-035/ai-block-pty`
- Umbrella issue: `https://github.com/zjzcpj/SciStudio/issues/842`
- Sub-issue: `https://github.com/zjzcpj/SciStudio/issues/844`
- ADR: `docs/adr/ADR-035.md`
- Checklist: `docs/planning/adr-035-036-checklist.md` (Skeleton (S35) section)
