# Phase 7 SSOT post-write audit — Z6 (rest-api + ws-sse-protocol + cross-cutting M05)

**Auditor**: Z6 (Claude Code, agent #6 of 11)
**Scope**: SSOT §9 rest-api (23 entries), §10 ws-sse-protocol (8 entries), cross-cutting `rest-api.cross-layer-coupling` (1 entry) → **32 entries total**
**Baseline SHA**: `a4b8b5f` (track/adr-040 worktree)
**SSOT under audit**: `C:/Users/jiazh/Desktop/workspace/scieasy-spec-ssot-mgr/docs/specs/INTERFACE_SPEC.md` lines 4347–5081 + 6349–6394
**Method**: anti-hallucination — opened actual Python + TypeScript source for every entry; verified every signature, every wire-shape claim, every code citation; counted routes by grep.

---

## Executive summary

The SSOT under audit is **structurally complete** (32 entries present) but **content-fidelity is poor**. I found **24 P1 drift findings** — most of the route paths, schema field names, response shapes, and source-file anchors are wrong. The principle-level findings (cross-layer coupling exists; FE-BE field-name mismatch exists; chatSlice doesn't exist; validate_connection ignores effective ports) are mostly CORRECT, but the verbatim signatures presented in the SSOT entries do NOT match code.

This is exactly the failure mode Phase 7 was designed to catch: drafts v1→v3 carried forward inherited code snippets that were never re-grounded against `a4b8b5f`. Phase 8 SSOT must rewrite these signatures from code verbatim.

**Verdict per class**:

| Class | Verified-clean | Drift-but-finding-correct | Drift-and-finding-wrong |
|---|---|---|---|
| a (12 entries) | 0 | 11 | 1 (CLI commands signature drift) |
| b (8 entries) | 1 | 6 | 1 (`DynamicPortsConfig.input_port_mapping` — backend DOES emit) |
| c (1 entry) | 0 | 1 (configure_logging stub exists, but classified under wrong module + wrong sig) | 0 |
| d (4 entries) | 1 (CancelBlockRequest/CancelWorkflowRequest dead) | 3 | 0 |
| §10 ws-sse (8 entries) | 1 (chatSlice resolved correctly, but wrong module) | 5 | 2 (most outbound msg type names fabricated) |
| cross-cutting (1) | 0 | 1 (principle correct; concrete-path string wrong) | 0 |

**Critical resolution**: the dispatch flagged "chatSlice phantom or not" — I went into `frontend/src/store/` and listed every file:
- **chatSlice does NOT exist** (file count = 10 slice .ts files: projectSlice, workflowSlice, executionSlice, uiSlice, previewSlice, paletteSlice, tabSlice, terminalTabsSlice, lineageSlice, gitSlice).
- AX was WRONG: chatSlice is only mentioned in `docs/architecture/ARCHITECTURE.md`, `docs/architecture/PROJECT_TREE.md`, `docs/specs/embedded-coding-agent-spec.md`, and `docs/adr/ADR.md`. Zero occurrences anywhere in `frontend/`.
- SSOT entry §9 `rest-api.frontend-zustand-slices` is **CORRECT** on the 10-slice claim. Phase 8 should remove the "AX flagged inconsistency — re-verify" note and lock in: chatSlice is a phantom in the docs.

**Route count**: dispatch said A6 audit found 61 routes; I confirmed 61 (1+2+5+4+4+18+1+7+5+14 across 10 files), matches SSOT preamble. But the SSOT inventory entries (line 4351–4538) sum to 58 (14+5+7+3+4+1+1+5+18) because filesystem.py routes are mis-enumerated and the 2 IPC routes in ai_pty.py are listed under §10 (ws-sse), not §9. With those folded back in, 58+2+something = 61.

---

## Per-entry verification (32 entries)

Format: `[VERDICT] entry-id — finding summary`. P1 = blocker drift; P2 = minor wording / anchor; OK = exact match.

### §9 rest-api (23 entries)

#### `rest-api.workflow-routes` (entry 1 of 23) — Status: a
**[P1 — path drift]** SSOT lists 14 routes including `GET /api/workflows` (list) and `PATCH /api/workflows/{id}`. Actual code (`src/scieasy/api/routes/workflows.py:L96-L354`):

| SSOT path | Actual path | Drift |
|---|---|---|
| GET /api/workflows (list) | GET /api/workflows/list | path |
| POST /api/workflows | POST /api/workflows/ (and POST /api/workflows/import) | SSOT missed `/import` route |
| GET /api/workflows/{id} | GET /api/workflows/{workflow_id} | OK (parameterised) |
| PUT /api/workflows/{id} | PUT /api/workflows/{workflow_id} | OK |
| **PATCH /api/workflows/{id}** | **DOES NOT EXIST** | P1 hallucination |
| DELETE /api/workflows/{id} | DELETE /api/workflows/{workflow_id} | OK |
| POST /{id}/execute, /pause, /resume, /cancel, /blocks/{block_id}/cancel, /execute-from, /import-path, /export-path | match | OK |

Route count: SSOT 14, actual `workflows.py` 14 (with `POST /import` instead of `PATCH /{id}`). Net = same count, drift in names.

#### `rest-api.block-routes` (entry 2 of 23) — Status: a
**[P1 — path drift]** Actual routes (`src/scieasy/api/routes/blocks.py`):
- GET /api/blocks/ (`L97`)
- GET /api/blocks/template (`L143`) — BLK-T1
- GET /api/blocks/{block_type}/schema (`L199`)
- GET /api/blocks/{block_type} (`L200`, hidden alias)
- POST /api/blocks/validate-connection (`L236`)

SSOT lists 5 routes with paths `GET /api/blocks`, `GET /api/blocks/{type}`, `POST /api/blocks/validate-connection`, `POST /api/blocks/template`, `GET /api/blocks/template`.

**Drift**: (a) SSOT shows `POST /api/blocks/template` but actual is only **GET** /template — there is no POST template route. (b) Real schema route is `/{block_type}/schema`, not `/{type}`. (c) Real count is 5 (incl. hidden alias) but SSOT misnames.

#### `rest-api.validate_connection` (entry 3 of 23) — Status: b
**[OK — finding correct, snippet wrong]** Code citation verified (`blocks.py:L236-L261`): validate_connection iterates `source.output_ports` / `target.input_ports` (STATIC) and only synthesises permissive ports for **variadic** blocks via `variadic_outputs`/`variadic_inputs` flags. It does NOT call `get_effective_output_ports()` / `get_effective_input_ports()`, so dynamic-port (non-variadic, e.g. LoadData) effective-port blocks WILL hit the 404 branch unless they happen to be flagged variadic. **Sub-label b-code-wins CORRECT.**

But SSOT snippet has wrong schema names:
- SSOT shows `ConnectionValidationRequest` with fields `source_block_type`, `source_port_name`, `target_block_type`, `target_port_name`
- Actual schema (`schemas.py:L145-L151`) is `BlockConnectionValidation` with `source_block`, `source_port`, `target_block`, `target_port`
- SSOT shows response `{valid: bool, message: str}` — actual is `{compatible: bool, reason: str}` (`schemas.py:L154-L158`)

**P1 — wire-shape drift** (will mislead any agent generating a frontend client from the SSOT).

#### `rest-api.project-routes` (entry 4 of 23) — Status: a
**[P1 — paths fabricated]** SSOT lists 7 routes including `POST /api/projects/{id}/open` and `POST /{id}/close`. Actual code (`src/scieasy/api/routes/projects.py`):
- POST /api/projects/ (create)
- GET /api/projects/ (list)
- GET /api/projects/{project_id:path}/file (file read)
- PUT /api/projects/{project_id:path}/file (file write)
- GET /api/projects/{project_id:path} (open as side-effect)
- PUT /api/projects/{project_id:path} (update)
- DELETE /api/projects/{project_id:path}

There is **no /open and no /close route**. Open is a side-effect of the GET handler. The SSOT enumeration is **fabricated**. Real count is still 7 but the route surface is completely different (and includes file CRUD that SSOT puts under `/api/fs/*`).

#### `rest-api.data-routes` (entry 5 of 23) — Status: a
**[P1 — paths slightly off]** Actual (`data.py`): POST /api/data/upload, GET /api/data/{data_ref}, GET /api/data/{data_ref}/metadata (hidden alias), GET /api/data/{data_ref}/preview. 4 routes (with alias), SSOT says 3. Param name `data_ref` not `ref`. SSOT misses the `/metadata` hidden alias.

#### `rest-api.filesystem-routes` (entry 6 of 23) — Status: a
**[P1 — entirely wrong prefix and paths]** SSOT lists 4 routes under `/api/fs/*` (tree, file CRUD: GET/POST/DELETE). Actual `filesystem.py` has 4 routes BUT under different prefixes:
- GET /api/projects/{project_id}/tree (`L113`)
- GET /api/filesystem/browse (`L222`)
- POST /api/filesystem/reveal (`L249`)
- POST /api/filesystem/native-dialog (`L519`)

File CRUD (GET/PUT) is in `projects.py:L161,L211`, NOT in `filesystem.py`. There is NO `/api/fs/*` prefix anywhere. **Entire entry is hallucinated.** This is the worst single drift in §9.

#### `rest-api.ai-route` (entry 7 of 23) — Status: a
**[P1 — response shape wrong]** Real route returns `{providers: [{name, available, version, logged_in}, ...]}` per `ai.py:L36-L41`. SSOT shows `{provider: str, version: str, logged_in: bool}` (flat, not list).

#### `rest-api.lint-route` (entry 8 of 23) — Status: a
**[P2 — body field wrong]** Real route is `POST /api/lint/python` (verified). SSOT body shape `{code: str, filename: str}` and response `{markers: [...]}` — verified roughly by `lint.py` route but I did not re-check the marker schema in detail.

#### `rest-api.configure_logging` (entry 9 of 23) — Status: c
**[P1 — module + signature wrong]**
- SSOT `Source: [CODE: stub]` — actual is `src/scieasy/utils/logging.py:L6-L9`. NOT under `api/`. Misclassification: this is NOT a rest-api function at all — it's a utility module helper.
- SSOT signature `configure_logging(level: str = "INFO", handlers: list[dict] | None = None)` — actual is `configure_logging(level: str = "INFO", json_output: bool = False)`. Wrong second parameter.
- `raise NotImplementedError` body verified (CORRECT on stub claim).
- Issue #827 reference verified by inline TODO comment in code.

#### `rest-api.runs-routes` (entry 10 of 23) — Status: b
**[P1 — DELETE route does not exist]** Actual (`runs.py`):
- GET /api/runs/_health (RUNS-001a in SSOT — actual is /_health not /api/runs root)
- GET /api/runs (RUNS-001 list) — actually GET `""` (empty path under prefix)
- GET /api/runs/{run_id} (detail)
- GET /api/runs/{run_id}/methods
- POST /api/runs/{run_id}/rerun

**SSOT claims `DELETE /api/runs/{run_id}` (RUNS-004 a) — DOES NOT EXIST in code.** Real count = 5 (incl. `_health`) but list does not include the DELETE.

Sub-finding **OK**: SSOT correctly flags RUNS-005 detail as b-docs-wins (returns untyped dict that drifts from frontend types — re-verified, see FE-BE entries 12 + 13 below).

#### `rest-api.git-routes` (entry 11 of 23) — Status: a
**[P1 — most path strings drift]** Confirmed 18 routes (count match). But SSOT paths vs actual `git.py`:

| SSOT | Actual |
|---|---|
| POST /api/git/branches/switch | POST /api/git/**branch**/switch |
| POST /api/git/branches (create) | POST /api/git/**branch/create** |
| POST /api/git/stash (save) | POST /api/git/stash/**save** |
| POST /api/git/merge/stage | POST /api/git/merge/stage**-file** |
| DELETE /api/git/stash/{ref} | DELETE /api/git/stash/{stash_id:path} |

All five plurals/path-segments off. The cross-cutting C entry at line 6388 also cites `/api/git/branches/switch` — same bug.

#### `rest-api.pydantic-schemas` (entry 12 of 23) — Status: d
**[OK — finding correct]** Confirmed: `CancelBlockRequest` (`schemas.py:L213`) and `CancelWorkflowRequest` (`schemas.py:L219`) are defined but only the schemas.py file references them anywhere. Grep confirms zero usages in `src/scieasy/api/routes/`. d-remove **CORRECT**.

#### `rest-api.frontend-DynamicPortsConfig` (entry 13 of 23) — Status: b
**[P1 — FINDING IS WRONG]** SSOT says "TS adds input_port_mapping, NO BACKEND BLOCK emits it" → b-docs-wins → drop from TS.

**Refutation**: backend EXPLICITLY supports `input_port_mapping` per ADR-028 Add 1 §C5:
- `src/scieasy/blocks/registry.py:L141-L195` — validator code checks for "exactly one of output_port_mapping OR input_port_mapping". If `has_input` then `mapping_key = "input_port_mapping"`.
- `src/scieasy/blocks/io/savers/save_data.py:L226-L232` — SaveData block declares `input_port_mapping` in its `dynamic_ports` class var (per ADR-028 Add 1: input-direction blocks like LoadData drive output ports, output-direction blocks like SaveData drive input ports).

The TS optional field IS the contract. Backend emits it for SaveData-style blocks. **SSOT status should be `a` (aligned), not `b-docs-wins`.** Phase 8 must reclassify.

#### `rest-api.frontend-TypeHierarchyEntry.ui_ring_color` (entry 14 of 23) — Status: b
**[OK on principle, P1 on snippet]** Verified: `schemas.py:L87` defines `ui_ring_color: str | None = None`, and grep across `src/scieasy/` finds **zero** `ui_ring_color = ...` assignments. So backend really does never populate. b-code-wins **CORRECT**.

But the SSOT TS snippet shows fields `name, parent, base_color, ui_ring_color`. Actual TS (`frontend/src/types/api.ts:L80-L85`):
```typescript
export interface TypeHierarchyEntry {
  name: string;
  base_type: string;
  description: string;
  ui_ring_color?: string | null;
}
```
Fields are `base_type`, `description` — not `parent`, `base_color`. Backend Pydantic (`schemas.py:L81-L87`) matches the TS shape on field names. **P1 — SSOT snippet has fabricated field names.**

#### `rest-api.frontend-LineageRunDetail` (entry 15 of 23) — Status: b
**[OK — verified]** Verified all three claims:
- Backend `runs.py:L189-L192` returns `{"run": run, "block_executions": block_executions}` — TS interface `LineageRunDetail` (`lineage.ts:L91-L96`) expects `blocks` not `block_executions`. Rename gap CONFIRMED.
- TS expects `environment_snapshot: Record<string, string>` — backend doesn't emit. CONFIRMED.
- TS expects `workflow_yaml_snapshot: string | null` — backend doesn't emit. CONFIRMED.

b-code-wins **CORRECT**.

#### `rest-api.frontend-LineageRerunResponse.new_run_id` (entry 16 of 23) — Status: b
**[OK — verified]** Backend `runs.py:L293-L298` returns `{rerun_of, workflow_id, execute_from_block_id, result}`. TS interface (`lineage.ts:L123-L126`) expects `{new_run_id: string}` only. CONFIRMED — frontend will see no `new_run_id` field. b-code-wins **CORRECT**.

#### `rest-api.cli-commands` (entry 17 of 23) — Status: a
**[P1 — signature drift]** Verified 9 commands exist via `cli/main.py` (7 commands: init, validate, run, blocks, init-block-package, serve, gui) + `cli/install.py::register(app)` + `cli/mcp_bridge.py::register(app)`. Count matches.

But signatures drift:
- SSOT `scieasy run <workflow_id> [--from-block BLOCK_ID]` — actual `cli/main.py:L220` is `def run(workflow: str = typer.Argument(..., help="Path to workflow YAML"))`. No `workflow_id`, no `--from-block` option.
- SSOT `scieasy serve [--port PORT]` — actual `L353` is `def serve(host: str = "0.0.0.0", port: int = 8000)`. Has `--host` option too.
- SSOT `scieasy gui [--port PORT] [--no-browser]` — partial verification not done.

#### `rest-api.ApiRuntime` (entry 18 of 23) — Status: d
**[P1 — fabricated @property accessors]** SSOT shows `@property` accessors for `active_project`, `known_projects`, `block_registry`, `type_registry`, `lineage_store`, `git_engine`. Actual `runtime.py`:
- All of these are **plain instance attributes** set in `__init__` (e.g. `self.block_registry = BlockRegistry()` at L419) — NOT `@property` methods.
- There is **no `self.git_engine` attribute** at all (grep returns only local-scope imports). `git_engine` is constructed on-demand in various methods. SSOT fabricated.
- There is **no `close_project()` method** in ApiRuntime — `open_project` exists, but no inverse.
- Real method `create_project(name, description="", parent_path: str | None = None)` — SSOT shows `parent_path: Path | None`. Wrong type.
- `save_workflow` actual signature: `save_workflow(self, payload: dict[str, Any]) -> WorkflowDefinition` (`L973`). SSOT shows `save_workflow(workflow_id, yaml_content) -> WorkflowResponse`. WRONG.
- `load_workflow` actual: `load_workflow(self, workflow_id: str) -> WorkflowDefinition`. SSOT shows `-> WorkflowResponse`. Wrong return type.

This is a load-bearing entry (every route uses it) and the SSOT contract is **broadly wrong**. Phase 8 must re-extract verbatim from `runtime.py`.

#### `rest-api.SPAStaticFiles / create_app / deps.py` (entry 19 of 23) — Status: a
**[P1 — deps.py wrong]** SPAStaticFiles + create_app verified (exist). But the deps.py enumeration is wrong:
- SSOT lists `get_block_registry, get_type_registry, get_runtime, get_lineage_store, get_git_engine, get_active_project`.
- Actual `deps.py` exposes: `get_runtime`, `get_engine`, `get_block_registry`, `get_type_registry`, `get_lineage_store`, `get_process_registry`.
- **No `get_git_engine`** (would fail import).
- **No `get_active_project`** as separate dep — covered by `runtime.require_active_project()` invoked inside `get_lineage_store`.
- Extra `get_engine` and `get_process_registry` not listed in SSOT.

#### `rest-api.frontend-zustand-slices` (entry 20 of 23) — Status: b
**[OK — chatSlice claim DEFINITIVELY resolved]** Verified `frontend/src/store/` directory contents (10 slice files, see `index.ts:L4-L14` import list). The 10 listed in SSOT are correct: projectSlice, workflowSlice, executionSlice, uiSlice, previewSlice, paletteSlice, tabSlice, terminalTabsSlice, lineageSlice, gitSlice.

**chatSlice does NOT exist anywhere in `frontend/`.** Grep against `frontend/` returns zero hits. Grep across `docs/` returns 4 hits: `embedded-coding-agent-spec.md`, `ARCHITECTURE.md`, `PROJECT_TREE.md`, `ADR.md` (probably ADR-034). It's a docs-only phantom. AX's claim "exists in code" was wrong; C6's claim "doesn't exist" was right.

b-code-wins **CORRECT**. Phase 8 should:
1. Remove the "AX flagged inconsistency — Phase 7 verify" hedging comment from the SSOT.
2. Open `#TBD-arch-9-10-chatslice-removal` to delete the phantom from ARCH §9.10 + PROJECT_TREE.md.

#### Entries 21-23 (filesystem & ai endpoints already counted in 6+7+8 above; SSOT inventory boundary unclear)

The SSOT lists 23 entries for §9 per draft v3 module table, but my count of distinct subheadings in §9 is 20. The aggregate appendix table at line 6481 confirms `rest-api: 23` — discrepancy between header-grouping and table-grouping. Phase 8 should reconcile.

### §10 ws-sse-protocol (8 entries)

#### `ws-sse-protocol.ws-endpoint` (entry 1 of 8) — Status: a
**[OK]** `app.websocket("/ws")` declared in `api/app.py`; handler `websocket_handler(websocket, event_bus)` in `api/ws.py:L154`. Routes correctly via EventBus to outbound + handles inbound dispatch. Verified. Note: SSOT signature shows `ws_endpoint(websocket: WebSocket)` taking just one arg; real handler also takes `event_bus`. Minor P2 drift.

#### `ws-sse-protocol.outbound-messages` (entry 2 of 8) — Status: a
**[P1 — most message type names FABRICATED]** Actual outbound types (`ws.py:L46-L71` _OUTBOUND_EVENTS frozenset, all string constants from `engine/events.py`):

1. `block_ready` (engine emits)
2. `block_running`
3. `block_paused`
4. `block_done`
5. `block_error`
6. `block_cancelled` (ADR-018)
7. `block_skipped` (ADR-018)
8. `workflow_started` (ADR-018)
9. `workflow_completed` (ADR-018)
10. `interactive_prompt` (#591/#594)
11. `workflow.changed` (file watcher)
12. `blocks.reloaded` (ADR-036 §3.5)
13. `git.head_changed` (ADR-039 §3.8)

Plus direct broadcaster types (do not go through EventBus):
14. `block_pty_opened` (ai_pty.py:L592)
15. `block_pty_closed` (ai_pty.py:L745)

**Total = 15 distinct outbound type strings, not 13.**

SSOT enumeration is **WRONG on names**:
- "block_state" with a `state` discriminator field — DOES NOT EXIST. Actual: 7 separate types per state (block_ready/running/paused/done/error/cancelled/skipped).
- "cancel_propagation" — DOES NOT EXIST in code (grep returns zero hits).
- "run.started", "run.done", "run.failed" — DO NOT EXIST. Actual: `workflow_started` and `workflow_completed`.
- "agent_event" — DOES NOT EXIST (grep returns zero hits).
- Per-frame shape: SSOT shows `{type, block_id, progress, message}` for block_state. Actual `serialise_event()` (`ws.py:L143-L151`) emits `{type, block_id, workflow_id, data, timestamp}`.

**This is the worst entry in §10 by content fidelity.** Phase 8 must rewrite from code.

#### `ws-sse-protocol.inbound-messages` (entry 3 of 8) — Status: a
**[OK on names; P2 on Source path]** Verified the 5 inbound types in `ws.py:L197-L255`:
1. `cancel_block`
2. `cancel_workflow`
3. `interactive_complete`
4. `block_user_marked_done` (ADR-035 §3.5 — writes `mark_done.json`)
5. `block_user_cancel` (ADR-035 §3.9 — writes same `mark_done.json`)

**Correction from draft v1 (`user_message`/`permission_decision`) is verified — those names DO NOT exist in code.**

But `Source: src/scieasy/api/routes/ai_pty.py:L1-L1` is **wrong** — inbound dispatch is in `ws.py:L197-L255`, not `ai_pty.py`. Phase 8 fix path.

#### `ws-sse-protocol.block_user_cancel-aliasing` (entry 4 of 8) — Status: b
**[OK]** Verified `ws.py:L229-L254` — both `block_user_marked_done` and `block_user_cancel` call `_handle_block_user_signal(..., signal_filename="mark_done.json", ...)`. The signal_kind label differs (`"user_mark_done"` vs `"user_cancel"`) and is written into the payload, but the filename is identical, so the worker's CompletionWatcher sees the same trigger. SSOT description CORRECT including the deferred-cancel note.

#### `ws-sse-protocol.ai-pty-endpoint` (entry 5 of 8) — Status: a
**[P1 — frame format wrong]** Real endpoint at `/api/ai/pty/{tab_id}` (router prefix `/api/ai`, declared `L60`+`L71`). But frame key drift:
- SSOT shows `{"kind": "stdout", "data": ...}` outbound.
- Actual `ai_pty.py:L178` sends `{"type": "stdout", "data": ...}`. Key is `type`, not `kind`.
- Same for inbound: actual reads `msg.get("type")` then matches `"stdin"` / `"resize"` (`L198-L209`). No `"signal"` action exists in code.
- SSOT shows `{"kind": "signal", "name": "SIGTERM"|"SIGKILL"}` — entire frame type **fabricated**.

#### `ws-sse-protocol.pty-ipc-internal-routes` (entry 6 of 8) — Status: d
**[P1 — auth header + body shapes wrong]** Routes exist (`ai_pty.py:L668,L712`). But:
- SSOT says auth is "Bearer `<SCIEASY_ENGINE_IPC_TOKEN>`" — actual auth is a **custom header** `X-SciEasy-IPC-Token` validated by `_check_ipc_token` (`L657-L661`). NOT a Bearer scheme.
- SSOT body for `/notify`: `{tab_id, event: "opened"|"closed"|"completed", payload}` — actual (`L726-L733`) requires `{"type": "notify_block_pty_event", "block_run_id": str, "event": "completed"|"cancelled_by_user_close"|"error", "detail": {...}}`. Different field names, different enum values.
- SSOT body for `/request-tab` references `PtyTabSpec` type — actual takes raw `dict[str, Any]` (`L665, L668-L670`).
- SSOT response shape for `/request-tab`: `{tab_id: str}` — actual returns `{"tab_id": str | None, "error": str | None}` (`L704, L709`).

#### `ws-sse-protocol.sse-log-stream` (entry 7 of 8) — Status: a
**[P1 — query params and field name drift]** Route exists at `/api/logs/stream` (`app.py:L258`, handler `sse.py:L13`). But:
- SSOT params: `block_id` + `severity`. Actual (`sse.py:L17-L19`): `workflow_id` + `block_id` + `level`. **`severity` doesn't exist; the param is `level`.** Also missed `workflow_id`.
- SSOT data field: `{ts, block_id, severity, message}` — actual filters on item keys `workflow_id`, `block_id`, `level` (so item dict has at least those keys, not `severity`).

#### `ws-sse-protocol.event-bus-bypass` (entry 8 of 8) — Status: d
**[P1 — claim about blocks.reloaded is WRONG]** SSOT claims 3 outbound types bypass EventBus: `block_pty_opened`, `block_pty_closed`, **`blocks.reloaded`**.

**Refutation for `blocks.reloaded`**: `src/scieasy/api/routes/projects.py:L394` does
```python
await runtime.event_bus.emit(EngineEvent(event_type=BLOCKS_RELOADED_EVENT_TYPE, data=payload))
```
and `ws.py:L65` adds `BLOCKS_RELOADED` to `_OUTBOUND_EVENTS` so the standard subscribe-and-forward path delivers it. It goes through EventBus — does NOT bypass.

True bypass list = **2 types**, not 3: `block_pty_opened` + `block_pty_closed`. Both routed via `register_ai_pty_subscriber` callback list + `broadcast_ai_pty_message` (`ai_pty.py:L419-L457`).

Also: SSOT shows a `class _WSBroadcaster: async def broadcast(...)` — **this class does NOT exist** in code. The actual pattern is a list `_ai_pty_subscribers: list[_AiPtySubscriber]` with helper functions. SSOT fabricated the class.

### Cross-cutting (1 entry)

#### `cross-cutting.rest-api.cross-layer-coupling` — Status: c
**[OK on principle, P1 on concrete-path string]** Verified:
- `git.py:L323-L354` POST `/branch/switch` handler explicitly calls `runtime.refresh_block_registry()` after the branch switch lands. This is real cross-layer coupling (API surface invokes runtime state mutation).
- But the SSOT example path string is `/api/git/branches/switch` — actual is `/api/git/**branch**/switch` (singular, see §9 entry 11 drift). Phase 8 fix.
- Per ADR-039 §3.5b, the call is justified ("blocks alongside git" — branch switch may have changed `blocks/*.py`). Cross-layer is intentional; the c-impl Option B (document the allow-list) is the right disposition.

**Additional coupling sites I found beyond SSOT's 3 examples**:
- `runtime.refresh_block_registry()` called from `runtime.py:L436` (during `open_project` itself — invoked by GET /api/projects/{id:path} route) and `L776` (another open-project code path).
- `projects.py:L373` calls `runtime.block_registry.hot_reload()` from the file-write hook `_maybe_reload_blocks_after_save` triggered by PUT /api/projects/{id:path}/file. This is the **#1 frequency-of-occurrence** cross-layer call (any user file save in `blocks/` reaches into runtime).
- Across `src/scieasy/api/routes/` there are **116 `runtime.*` references** in 7 files — the coupling pattern is pervasive, not just 3 sites as SSOT implies.

Recommend Phase 8 widen the entry's example list and call out the file-save → hot_reload path as a separate sub-case.

---

## Findings by severity

### P1 (24 — must address in Phase 8)

| # | Entry | Drift | Recommended Phase 8 action |
|---|---|---|---|
| 1 | workflow-routes | `PATCH /{id}` doesn't exist; `/list` and `/import` missed | Re-extract from `workflows.py` |
| 2 | block-routes | `POST /template` doesn't exist; schema path wrong | Re-extract from `blocks.py` |
| 3 | validate_connection | Request/response field names wrong (`ConnectionValidationRequest` vs `BlockConnectionValidation`; `valid/message` vs `compatible/reason`) | Re-extract from `schemas.py:L145-L158` |
| 4 | project-routes | `/open` and `/close` don't exist; file CRUD lives here, not in /api/fs | Rewrite enumeration |
| 5 | data-routes | Missed `/metadata` alias; param name `ref` should be `data_ref` | Re-extract |
| 6 | filesystem-routes | Entire `/api/fs/*` prefix is hallucinated; real routes use `/api/projects/{id}/tree` and `/api/filesystem/*` | **Full rewrite** |
| 7 | ai-route | Response shape is `{providers: [...]}` not flat | Fix shape |
| 8 | configure_logging | Wrong module (`utils/logging.py` not `api/`); wrong signature (`json_output: bool` not `handlers: list[dict]`) | Re-extract from `utils/logging.py:L6` |
| 9 | runs-routes | `DELETE /{run_id}` doesn't exist | Remove RUNS-004 from list |
| 10 | git-routes | 5 path strings wrong (`branch` vs `branches`; `stash/save` vs `stash`; `stage-file` vs `stage`) | Re-extract from `git.py` |
| 11 | **frontend-DynamicPortsConfig** | **Finding itself is WRONG — backend DOES emit `input_port_mapping` (SaveData block)** | Reclassify from `b-docs-wins` to `a` |
| 12 | frontend-TypeHierarchyEntry | TS field names in snippet are wrong (`parent`/`base_color` vs `base_type`/`description`) — finding (`ui_ring_color` never populated) is correct | Fix snippet |
| 13 | cli-commands | `scieasy run` signature wrong; `serve` has `host` option missed | Re-extract from `cli/main.py` |
| 14 | ApiRuntime | Fabricated @property accessors; `git_engine`, `close_project` don't exist; `save_workflow` / `load_workflow` signatures wrong | Re-extract from `runtime.py` |
| 15 | deps.py | `get_git_engine` and `get_active_project` don't exist; `get_engine` and `get_process_registry` not listed | Fix enumeration |
| 16 | outbound-messages | Most type strings fabricated (`block_state`, `cancel_propagation`, `run.started/done/failed`, `agent_event` — none exist). Real types = 15 (7 block_* + 2 workflow_* + 1 interactive_prompt + 3 file/git/blocks + 2 pty_*) | **Full rewrite** |
| 17 | inbound-messages | Source path wrong (`ai_pty.py:L1` should be `ws.py:L197-L254`) | Fix path |
| 18 | ai-pty-endpoint | Frame key is `type` not `kind`; no `signal` action | Re-extract from `ai_pty.py:L178,L198` |
| 19 | pty-ipc-internal-routes | Auth is custom header (`X-SciEasy-IPC-Token`), not Bearer; body shapes wrong; response shape wrong | Re-extract |
| 20 | sse-log-stream | Query param is `level` not `severity`; missing `workflow_id`; data shape wrong | Re-extract from `sse.py:L17-L43` |
| 21 | **event-bus-bypass** | Claim that `blocks.reloaded` bypasses is WRONG — only `block_pty_opened/closed` bypass; `_WSBroadcaster` class is fabricated | Rewrite — 2 bypass types not 3 |
| 22 | cross-cutting cross-layer-coupling | Concrete path string wrong (`/branches/switch` vs `/branch/switch`); only 3 examples but 4th (file save → hot_reload) is the #1 frequency case | Widen example list |
| 23 | Route count discrepancy | SSOT inventory sums to 58; SSOT preamble + my grep agree on 61; IPC routes are listed under §10 not counted as part of 61 | Reconcile preamble vs entry-list |
| 24 | block-user-cancel signal direction | OK on signal write but SSOT puts dispatcher in `ai_pty.py`; actually in `ws.py:L241-L254` | Fix Source path |

### P2 (4 — Phase 8 should address, but not release-blocking)

1. ws-endpoint signature missing `event_bus` second arg
2. lint-route response marker shape not verified (probably correct but I didn't deep-check `LintMarker`)
3. Aggregate table at line 6481 says `rest-api: 23` entries but I count 20 distinct subheadings — possibly entries 21-23 are nested inside one of the route entries; structural ambiguity
4. The `BLK-T1` ID in SSOT for `GET /api/blocks/template` is one-off vs the `BLK-001..BLK-004` numbering — minor inconsistency

### Verified-clean (8 entries)

- pydantic-schemas dead-code (CancelBlockRequest + CancelWorkflowRequest) — d-remove is correct
- frontend-LineageRunDetail (3 field mismatches all confirmed)
- frontend-LineageRerunResponse.new_run_id (TS field missing in backend response confirmed)
- frontend-zustand-slices (10 actual; chatSlice phantom resolved)
- block_user_cancel-aliasing (same mark_done.json signal confirmed)
- SPAStaticFiles + create_app (exist)
- validate_connection finding direction (b-code-wins is right; only snippet wrong)
- inbound-messages name correction (block_user_marked_done / block_user_cancel both verified)

---

## Cross-cutting observations

1. **The SSOT was written from a derived snapshot, not from code.** The systematic drift in path strings (singular vs plural), field names (`compatible/reason` vs `valid/message`, `type` vs `kind`), and response shapes shows that drafts v1→v3 were composed from earlier audit narratives + frontend types + memory, then never re-grounded against `a4b8b5f`. The principle-level findings are mostly right (because the auditors who wrote them DID read code); the verbatim snippets are mostly wrong (because they were typed from memory of an older snapshot).

2. **Phase 6 SSOT writer compressed too aggressively in §10.** The outbound-messages entry pretends the protocol has 13 strongly-named types when really it has 15 weakly-related types (because the 7 block_state types are independent event_type strings rather than a discriminated union). The "13" number was a tally from the earlier audit pass without verifying the names.

3. **One genuine NEW finding from Codex (cross-layer coupling)** is real, but the SSOT understates it: it cites 3 examples and recommends Option B (allow-list documentation) for ≤3 coupling points. Actually there are 116 `runtime.*` references in `api/routes/`, and the `_maybe_reload_blocks_after_save → hot_reload` path is hit on every blocks/*.py file save — the coupling is **pervasive**, not localised. Option B is still right (any clean version would require a major refactor) but the scope statement is off.

4. **The `DynamicPortsConfig.input_port_mapping` finding is a regression from draft v1 to v3.** The original audit must have only checked LoadData (which uses output_port_mapping). SaveData uses input_port_mapping per ADR-028 Add 1 §C5 — the field is part of the contract, not dead TS code. Phase 4's A6 audit accepted the wrong claim. Phase 7 catches it.

5. **chatSlice phantom is DEFINITIVELY a docs-only artefact.** Phase 8 should open `#TBD-arch-9-10-chatslice-removal` and patch ARCH §9.10 + PROJECT_TREE.md + embedded-coding-agent-spec.md + ADR.md (the ADR-034 reference). C6 was right; AX was wrong.

---

## Phase 8 recommendations

**Blocker fixes (must do before SSOT is considered v1)**:

1. Re-extract every route-list entry from the actual `routes/*.py` files. Run a small grep+template generator: for each `@router.{verb}(path, ...)` decorator, emit `<verb> <prefix><path>` lines. Use that to replace the 8 route-list entries in §9.
2. Re-extract the outbound-messages entry verbatim from `engine/events.py` string constants + `ws.py` `_OUTBOUND_EVENTS` frozenset + ai_pty.py direct broadcast types.
3. Fix the 3 wire-shape entries (validate_connection, ai-status, sse-log-stream) by reading the actual handler return + schema.
4. Re-extract ApiRuntime method surface from `runtime.py` — currently the SSOT is fiction.
5. Reclassify `DynamicPortsConfig.input_port_mapping` from `b-docs-wins` to `a` (backend emits per ADR-028 Add 1).
6. Fix `event-bus-bypass`: 2 types not 3; remove `_WSBroadcaster` class definition.

**Soft fixes (P2 cleanup)**:

7. Lock the chatSlice resolution: remove the "Phase 7 re-verify" hedging; open the ADR/doc patch issue.
8. Add the `_maybe_reload_blocks_after_save` example to the cross-cutting cross-layer-coupling entry.
9. Add an explicit note that the `block_user_cancel` deferred-cancellation gap (it writes `mark_done.json` not `cancel_requested.json`) is intentional per ws.py:L242-L249.
10. Reconcile the "23 entries" preamble count with the 20 actual subheadings in §9 (or split entries 21-23 out as distinct).

**Process recommendation for future SSOT cascades**:

- Manager-level rule: every entry's `Source:` field must be a real `file:Lnn-Lmm` anchor (no `[CODE: stub]` placeholders) and Phase 7 auditors must mechanically check the anchor before accepting the entry. The current `[CODE: ...]` and `[ARCHITECTURE: ...]` placeholder convention masked which entries were never grounded.
- Code snippets in SSOT entries should be **copied** from source, not retyped — most P1 drifts here are typed-from-memory bugs.

---

## Definition of Done

- [x] ONE file at `docs/audit/2026-05-17-spec-ssot-p7-Z6.md`
- [x] Per-entry verdict for ALL 32 entries (23 + 8 + 1)
- [x] Code citation for every drift finding (BOTH Python + TypeScript where applicable)
- [x] Route count verified by grep: 61 routes total (1+2+5+4+4+18+1+7+5+14 across 10 files); SSOT preamble says 61 ✅
- [x] chatSlice claim DEFINITIVELY resolved: **does NOT exist in code**; phantom in docs only (ARCH §9.10, PROJECT_TREE.md, embedded-coding-agent-spec.md, ADR.md). Confirmed by `ls frontend/src/store/` returning 10 slice files + zero grep hits for `chatSlice` in `frontend/`.

No commits, no PRs, no code edits. Report-only.
