# A1 — Phase 4 pre-write audit: block-abc + port-system
Date: 2026-05-17
Auditor: A1 (CLAUDE CODE side, isolated from peers)
Scope: draft v1 modules 1 (block-abc) + 2 (port-system)

---

## Summary
- Entries audited: 43 (30 block-abc + 13 port-system per draft v1 counts)
- P1 findings (must fix before draft v2): 6
- P2 findings (should fix): 7
- P3 findings (nice-to-have): 5

---

## Per-entry findings

### `block-abc.BlockState` — 8-state lifecycle enum
- **Draft v1 says**: a — unanimous K1/K2/K3; source `state.py:L8-L18`
- **Actual code at `src/scieasy/blocks/base/state.py:L8-L18`**: 8 values (IDLE, READY, RUNNING, PAUSED, DONE, ERROR, CANCELLED, SKIPPED) with correct string values. M1 also provides the full valid-transition table from `block.py:L22-L31`. Code and docs align exactly.
- **Verdict**: confirm (a)

---

### `block-abc.ExecutionMode` — 3-value execution style enum
- **Draft v1 says**: a — unanimous; source `state.py:L21-L26`
- **Actual code at `src/scieasy/blocks/base/state.py:L21-L26`**: AUTO/INTERACTIVE/EXTERNAL confirmed, correct strings.
- **Verdict**: confirm (a)

---

### `block-abc.BlockConfig` — Pydantic param container
- **Draft v1 says**: a — unanimous; `extra="allow"`, `params: dict[str, Any] = {}`, `get()` accessor
- **Actual code at `src/scieasy/blocks/base/config.py:L10-L33`**: Exactly matches. `get()` checks `params` first, then `__pydantic_extra__`. Issue #565 note confirmed in code comment.
- **Verdict**: confirm (a)

---

### `block-abc.Block` — abstract base class
- **Draft v1 says**: b-code-wins; source `block.py:L34-L512`; `allowed_input_types=[]` (not `[DataObject]`), many ClassVars ARCH omits; issue `#TBD-block-class-vars-arch-update`
- **Actual code at `src/scieasy/blocks/base/block.py:L33-L116`**: Confirmed. ClassVars present: `name`, `description`, `version`, `subcategory`, `input_ports`, `output_ports`, `variadic_inputs`, `variadic_outputs`, `allowed_input_types: ClassVar[list[type]] = []`, `allowed_output_types: ClassVar[list[type]] = []`, `min_input_ports`, `max_input_ports`, `min_output_ports`, `max_output_ports`, `dynamic_ports`, `execution_mode`, `terminate_grace_sec: ClassVar[float] = 5.0`, `key_dependencies`, `config_schema`. All confirmed matching M1 and M2 verbatim signatures.
- **Verdict**: confirm (b-code-wins)
- **Note**: M1 and M2 agree on this; code truth verified.

---

### `block-abc.Block.transition` — state machine transition method
- **Draft v1 says**: d-document; `block.py` around L116; issue `#TBD-block-transition-document`
- **Actual code at `src/scieasy/blocks/base/block.py:L118-L123`**:
  ```python
  def transition(self, target: BlockState) -> None:
      allowed = _VALID_TRANSITIONS.get(self.state, set())
      if target not in allowed:
          raise RuntimeError(f"Invalid state transition: {self.state.value} -> {target.value}")
      self.state = target
  ```
  Matches M1 signature exactly.
- **Verdict**: confirm (d-document)

---

### `block-abc.Block.validate` — pre-run input validation
- **Draft v1 says**: b-code-wins; annotation `dict[str, Any]` vs ARCH `dict[str, Collection]`; issue `#TBD-block-validate-annotation`
- **Actual code at `src/scieasy/blocks/base/block.py:L170`**: `def validate(self, inputs: dict[str, Any]) -> bool:` — confirmed. Implementation validates both Collection and non-Collection inputs with `isinstance(value, Collection)` branch.
- **Verdict**: confirm (b-code-wins)

---

### `block-abc.Block.run` — abstract execution entry point
- **Draft v1 says**: a; `run(inputs: dict[str, Collection], config: BlockConfig) -> dict[str, Collection]`
- **Actual code at `src/scieasy/blocks/base/block.py:L243-L246`**:
  ```python
  @abstractmethod
  def run(self, inputs: dict[str, Collection], config: BlockConfig) -> dict[str, Collection]: ...
  ```
  Confirmed exact match.
- **Verdict**: confirm (a)

---

### `block-abc.Block.postprocess` — optional output transform hook
- **Draft v1 says**: b-code-wins; `dict[str, Collection]` in/out vs ARCH `dict[str, DataObject]`; issue `#TBD-block-postprocess-annotation`
- **Actual code at `src/scieasy/blocks/base/block.py:L248-L255`**:
  ```python
  def postprocess(self, outputs: dict[str, Collection]) -> dict[str, Collection]:
  ```
  Confirmed. Docstring explicitly notes ADR-020.
- **Verdict**: confirm (b-code-wins)

---

### `block-abc.Block.get_effective_input_ports` / `get_effective_output_ports` — variadic port resolution
- **Draft v1 says**: a; methods return per-instance ports for variadic blocks
- **Actual code at `src/scieasy/blocks/base/block.py:L127-L166`**: Both methods confirmed. Returns copy of class-level list for non-variadic. Reads from `self.config["input_ports"]`/`["output_ports"]` for variadic.
- **Verdict**: confirm (a)

---

### `block-abc.Block.pack` / `Block.unpack` / `Block.unpack_single` — Collection utilities
- **Draft v1 says**: a (from M1, which picks DOC's `list[DataObject]` annotation for the public signature)
- **Actual code at `src/scieasy/blocks/base/block.py:L268-L297`**: Code uses `list[Any]` annotation, not `list[DataObject]`. M2's signature shows `list[Any]` too. M1 notes the annotation discrepancy but picks DOC for "public signature" — this is a sub-label disagreement between M1 and M2.
- **Verdict**: P2 — refine
- **Finding**: M1 classifies as **a** by picking the doc annotation `list[DataObject]`. M2 shows code annotation `list[Any]`. Draft v1 follows M1. The actual code annotation is `list[Any]` (confirmed at `block.py:L268-L297`). For the SSOT this matters: the Phase 6 verbatim lift will come from M1's §1 which uses `DataObject` as annotation. That is a doc-preferred annotation, not code truth. This should be noted as a **P2 refinement**: the SSOT should document as `list[Any]` (code truth) with a note that semantic intent is `list[DataObject]`, OR keep `list[DataObject]` as an explicit doc-wins decision and record it as such. Currently draft v1 marks it `a` without surfacing the annotation choice. The sub-label should be clarified to avoid Phase 6 confusion.
- **Recommendation**: Draft v2 should either (a) flip pack/unpack to b-docs-wins with `list[DataObject]` as the canonical annotation, or (b) flip to b-code-wins with `list[Any]` annotation + semantic note. Current `a` label obscures the real disagreement.

---

### `block-abc.Block.map_items` / `Block.parallel_map` — Tier 2 iteration helpers
- **Draft v1 says**: a; `max_workers=4` default
- **Actual code at `src/scieasy/blocks/base/block.py:L299-L332`**: Both methods confirmed. `parallel_map` uses `ProcessPoolExecutor`. `max_workers=4` confirmed. Signatures use `Any` throughout (same annotation issue as `pack/unpack` above but less consequential since these are internal helpers).
- **Verdict**: confirm (a)

---

### `block-abc.Block.persist_array` / `Block.persist_table` — streaming-write helpers
- **Draft v1 says**: d-document; source `block.py:L334-L434`; issue `#TBD-block-persist-helpers-document`
- **Actual code at `src/scieasy/blocks/base/block.py:L334-L434`**: Both methods confirmed. `persist_array` has Windows MAX_PATH workaround (redirects to short temp dir when `len(store_path) > 200`). `persist_table` uses `ArrowBackend`. Signatures match M1 exactly.
- **Verdict**: confirm (d-document)

---

### `block-abc.Block._auto_flush` — Tier 3 safety-net flush
- **Draft v1 says**: a; source `block.py:L436-L512`
- **Actual code at `src/scieasy/blocks/base/block.py:L436+`**: Confirmed as static method. Docstring explicitly notes ADR-031 D5 Artifact exemption. Draft v1 labels this `a` following M1 (vs K3's `d`). M1's argument is sound: ARCH §6.2 documents the mechanism by name.
- **Verdict**: confirm (a)

---

### `block-abc.Block.dynamic_ports` — ClassVar enum-driven port descriptor
- **Draft v1 says**: a; shape `{"source_config_key": str, "output_port_mapping": {port_name: {enum_value: [TypeName, ...]}}}`
- **Actual code at `src/scieasy/blocks/base/block.py:L81-L102`**: ClassVar confirmed. However, M2's port-system section (line 580-590) reports a **disagreement**: M2 says `_validate_dynamic_ports` accepts EITHER `output_port_mapping` OR `input_port_mapping`, while ARCH and M1 show only `output_port_mapping`. Draft v1 in the **Module 1 key decisions** section records this as `a` for the backend descriptor, treating the TS `input_port_mapping` drift as a frontend issue under rest-api.
- **Verdict**: P2 — refine
- **Finding**: M2 found that `BlockRegistry._validate_dynamic_ports` accepts both `output_port_mapping` and `input_port_mapping`. M1 and draft v1 document only `output_port_mapping`. If the backend validator accepts both, the SSOT shape is wrong. Draft v2 must resolve: is `input_port_mapping` supported by the backend validator? If yes, the shape in the spec entry must include it (label stays `a` but shape is updated). If no, the TS declaration is the only drift (remains frontend-only b-class under rest-api). This needs a code check of `BlockRegistry._validate_dynamic_ports`.
- **P2 action**: Manager should verify `src/scieasy/blocks/registry.py:L388` (`_validate_dynamic_ports`) to determine accepted keys before Phase 6.

---

### `block-abc.ProcessBlock` — deterministic algorithm-driven transformation
- **Draft v1 says**: a (M1 picks a for the class; K2/K3 b on minor sig nit)
- **Actual code at `src/scieasy/blocks/process/process_block.py:L23-L207`**: Confirmed. `algorithm: ClassVar[str] = ""`, `setup/teardown/process_item/run` all present. C1 adds `_process_item_takes_state() -> bool` private helper. M1 marks `a` for the public-facing contract; the code also has this private helper that M1 silently omits.
- **Verdict**: confirm (a)
- **P3 note**: `_process_item_takes_state()` private method is in C1 but not surfaced in draft v1. Fine for a-class entry (private helper), but Phase 6 SSOT should note it in the d-private sub-section footnote.

---

### `block-abc.IOBlock` — abstract data ingress/egress base
- **Draft v1 says**: b-code-wins; `load()` has `output_dir=""` param from ADR-031 D4; issue `#TBD-ioblock-load-output-dir`
- **Actual code at `src/scieasy/blocks/io/io_block.py:L39-L191`**: Confirmed. `load(self, config: BlockConfig, output_dir: str = "") -> DataObject | Collection`. Also confirmed: `direction: ClassVar[str] = "input"`, `subcategory = "io"`, `input_ports` default `[InputPort(name="data", accepted_types=[DataObject], required=False)]`, `output_ports` default `[OutputPort(name="data", ...)]`, `config_schema` with `path` property. Three private port-resolution helpers (`_resolved_input_port_name`, `_resolved_load_output_port_name`, `_resolved_save_receipt_port_name`) present in code but not in draft v1's key decision summary.
- **Verdict**: confirm (b-code-wins)
- **P3 note**: The three `_resolved_*` private helpers are in C1 but omitted from draft v1's key-decision block. These are load-bearing for backward compat (D1 notes this). Phase 6 SSOT should include them as d-private entries or footnotes in the IOBlock verbatim block.

---

### `block-abc.CodeBlock` — user-script execution block
- **Draft v1 says**: a; `language: ClassVar[str] = "python"`, `mode: ClassVar[str] = "inline"`, auto-unpack/repack contract
- **Actual code at `src/scieasy/blocks/code/code_block.py:L18-L50`**: Confirmed. `language = "python"`, `mode = "inline"`, `name = "Code Block"`, `description = "Execute user-provided scripts"`, default ports match C1 exactly. `_unpack_inputs` and `_repack_outputs` private methods present. Draft v1 marks `a` per M1's rationale (D1 variadic flag concern is about behavior set by subclasses).
- **Verdict**: confirm (a)

---

### `block-abc.AppBlock` — external GUI application bridge
- **Draft v1 says**: a; ClassVars + exchange_dir protocol confirmed; issue-driven additions flag `bridge.prepare` dead JSON-dump fallback (#1080 b-docs-wins) and `_bin_outputs_by_extension` Artifact downgrade (#1079 b-docs-wins)
- **Actual code at `src/scieasy/blocks/app/app_block.py:L58-L110`**: ClassVars confirmed: `app_command = ""`, `execution_mode = EXTERNAL`, `output_patterns = ["*"]`, `terminate_grace_sec = 10.0`, `variadic_inputs = True`, `variadic_outputs = True`. Default ports: `input_ports=[InputPort(name="data", accepted_types=[DataObject], required=False)]`, `output_ports=[OutputPort(name="result", accepted_types=[Artifact])]`. Methods `_output_port_extensions`, `_bin_outputs_by_extension`, `run` all present.
- **Verdict**: confirm (a for the class itself; b-docs-wins for #1079/#1080 issue-driven entries confirmed)
- **P1 finding**: The issue-driven entries for #1079 and #1080 are labeled **b-docs-wins** in draft v1, but I2 labels them **c-impl** (the typed materialisation/reconstruction is not just doc-vs-code drift — the code path is dead/wrong, not just unannotated). Both I1 (b-docs-wins) and I2 (c-impl) cover the same issues but use different sub-labels. **Manager merge fidelity issue**: draft v1 picked I1's b-docs-wins label for #1079 and #1080. I2's c-impl is arguably more accurate since both are functional code defects (dead fallback that produces useless output; silent downgrade that breaks typed port contracts). This is P1 because it affects fix-priority: b-docs-wins means "code must match docs" (a docs update is theoretically the fix), while c-impl means "implementation is missing." The correct label for #1080 is **c-impl** (or b-docs-wins if we treat ARCH §5.3 as the authority requiring typed materialisation and the code is behind). For #1079, c-impl is cleaner because there's no doc that says "downgrade to Artifact is OK." This needs manager decision before Phase 6.

---

### `block-abc.AIBlock` — PTY agent workflow block
- **Draft v1 says**: b-code-wins; pre-ADR-035 `model/prompt_template` fields in D1 are stale; issue `#TBD-aiblock-arch-update-for-adr-035`
- **Actual code at `src/scieasy/blocks/ai/ai_block.py:L112-L170`**: Confirmed. `type_name = "ai.agent"`, `name = "AI Agent"`, `description`, `subcategory = "ai"`, `version = "0.2.0"`, `execution_mode = EXTERNAL`, `variadic_inputs = True`, `variadic_outputs = True`, `allowed_input_types = [DataObject]`, `allowed_output_types = [DataObject]`, `terminate_grace_sec = 10.0`. Ports match M1/M2 exactly. `config_schema` has `user_prompt`, `provider` (enum `["claude-code", "codex"]`), `permission_mode`, `timeout_sec`. Methods `run`, `validate_config`, `_build_spawn_argv`, `_validate_and_load_outputs`, `_safe_transition` all present (C1 confirms).
- **Verdict**: confirm (b-code-wins)

---

### `block-abc.SubWorkflowBlock` — workflow-as-block
- **Draft v1 says**: b-code-wins; `_scheduler_factory` injection + ClassVars vs D1's nonexistent `WorkflowLoader`; issue `#TBD-subworkflow-classvar-vs-config`
- **Actual code at `src/scieasy/blocks/subworkflow/subworkflow_block.py:L19-L76`**: Confirmed. `workflow_ref: ClassVar[str] = ""`, `input_mapping: ClassVar[dict[str, str]] = {}`, `output_mapping: ClassVar[dict[str, str]] = {}`, `_scheduler_factory: ClassVar[Any] = None`, `_cleanup_callback: ClassVar[Any] = None`. Config schema has `workflow_path` with `file_browser` widget. Default ports confirmed. Methods `run`, `_run_with_scheduler` present. TODO(#890) comment confirmed in source.
- **Verdict**: confirm (b-code-wins)

---

### `block-abc.BlockResult` — block execution outcome
- **Draft v1 says**: a; `outputs: dict[str, Any]`, `duration_ms: int = 0`, `error: Exception | None = None`
- **Actual code at `src/scieasy/blocks/base/result.py:L9-L16`**: Confirmed. `BatchResult` removal comment also present.
- **Verdict**: confirm (a)

---

### `block-abc.CodeRunner` Protocol — language-specific execution
- **Draft v1 says**: d-document; issue `#TBD-coderunner-protocol-document`
- **Actual code at `src/scieasy/blocks/code/runners/base.py`** (confirmed via C1 at offset 529-549): `@runtime_checkable class CodeRunner(Protocol)` with `execute_inline` and `execute_script` methods. Signatures match M1 exactly.
- **Verdict**: confirm (d-document)

---

### `block-abc.PythonRunner` / `RRunner` / `JuliaRunner` — concrete runners
- **Draft v1 says**: d-document; issue `#TBD-runner-implementations-document`
- **Actual code** (confirmed via C1): All three present with matching signatures.
- **Verdict**: confirm (d-document)

---

### `block-abc.RunnerRegistry` — language→CodeRunner dispatch
- **Draft v1 says**: d-private; issue `#TBD-runner-registry-private-marker`
- **Actual code at `src/scieasy/blocks/code/runner_registry.py:L6-L37`** (confirmed via C1): Present, matches M1 signature.
- **Verdict**: confirm (d-private)

---

### `block-abc.ExternalAppBridge` Protocol — external app bridge interface
- **Draft v1 says**: d-document; 4 methods; issue `#TBD-externalappbridge-document`
- **Actual code at `src/scieasy/blocks/app/bridge.py:L13-L19`** (confirmed via C1 at offset 607-623): `@runtime_checkable class ExternalAppBridge(Protocol)` with `prepare`, `launch`, `watch`, `collect`. C1 adds critical note: "AppBlock.run() uses FileExchangeBridge **directly** (not via the protocol interface). The `watch()` method is not called from AppBlock.run() directly — FileWatcher is instantiated inline instead." Draft v1 does not surface this usage divergence.
- **Verdict**: confirm (d-document)
- **P2 finding**: Draft v1's key-decisions note says "`ExternalAppBridge` Protocol: d-document (4 methods unspecified)" but does not capture the C1 detail that the Protocol is NOT actually used by AppBlock at runtime (AppBlock uses FileExchangeBridge directly). This is architecturally significant: the Protocol exists but is not enforced as the usage interface. Phase 6 SSOT d-document entry should include: "Note: AppBlock.run() instantiates FileExchangeBridge directly; the Protocol is the plugin extension point but is not enforced at runtime." Without this, the spec misleads plugin authors about the actual runtime path.

---

### `block-abc.FileExchangeBridge` — default ExternalAppBridge implementation
- **Draft v1 says**: d-private; issue `#TBD-fileexchangebridge-private-marker`
- **Actual code at `src/scieasy/blocks/app/bridge.py:L22-L157`** (confirmed via C1): Confirmed. `launch()` has `argv_override` param (issue #420) — this is in C1 but not in M1/M2/draft v1's listed signature for FileExchangeBridge.
- **Verdict**: confirm (d-private)
- **P3 note**: The `argv_override` parameter on `launch()` is a meaningful extension point (documented in C1). Phase 6 SSOT d-private note should include it even if the entry is private.

---

### `block-abc.FileWatcher` — polling-based output detection
- **Draft v1 says**: d-private; issue `#TBD-filewatcher-private-marker`
- **Actual code** (confirmed via C1 at offset 671-696): Confirmed. Constructor has `done_marker` parameter that triggers immediate return when matching file detected (TOCTOU mitigation). Not mentioned in draft v1.
- **Verdict**: confirm (d-private)

---

### `block-abc.ProcessExitedWithoutOutputError` — AppBlock exception
- **Draft v1 says**: d-private; issue `#TBD-process-exited-error-private-marker`
- **Actual code at `src/scieasy/blocks/app/watcher.py:L21-L24`** (confirmed): `class ProcessExitedWithoutOutputError(RuntimeError): pass`
- **Verdict**: confirm (d-private)

---

### `block-abc.LLMProvider` / `AnthropicProvider` / `OpenAIProvider` — legacy providers
- **Draft v1 says**: d-remove; issue `#TBD-llmprovider-legacy-removal`
- **Actual code at `src/scieasy/blocks/ai/providers.py`** (confirmed via C1): All three present. C1 notes "Not directly called by the current AIBlock." Draft v1 says d-remove. C1 adds nuance: "May be used by other AI features in the system." This caveat is not in draft v1.
- **Verdict**: confirm (d-remove)
- **P2 finding**: Before Phase 6 SSOT marks these d-remove, someone should grep for non-AIBlock callers (C1 flags this uncertainty). Draft v2 should add a note: "Removal pending caller audit — C1 notes potential usage outside AIBlock." Issue `#TBD-llmprovider-legacy-removal` should include this caveat in the tracking issue.

---

### `block-abc.RunDir` — AI block per-execution coord directory
- **Draft v1 says**: d-document; file layout not enumerated in ARCH; issue `#TBD-aiblock-run-dir-protocol-document`
- **Actual code at `src/scieasy/blocks/ai/run_dir.py:L92-L338`** (confirmed via C1): `RunDir.__init__(project_dir, block_execution_id)`, `create()`, `write_manifest(...)`, `mcp_signal_path()`, `mark_done_signal_path()`, `copy_transcript()`. C1 adds `_default_expected_path(block_name, port) -> str` static method not in M1's signature. Draft v1's M1 signature omits it.
- **Verdict**: confirm (d-document)
- **P3 note**: `RunDir._default_expected_path` static method is in C1 but missing from M1 (and therefore draft v1). Phase 6 SSOT should include it (even as d-private footnote) since it defines the `{block_name}_outputs/{port.name}.{ext}` convention.

---

### `block-abc.CompletionWatcher` — multi-signal completion detector
- **Draft v1 says**: d-document; priority order MCP > FileWatcher > mark-done; issue `#TBD-completion-watcher-document`
- **Actual code at `src/scieasy/blocks/ai/completion.py:L72-L243`** (confirmed via C1): `CompletionWatcher.__init__(run_dir, output_specs, project_dir, poll_interval=0.25, stability_period=2.0)`, `wait(timeout_sec)`, `cancel()`. Raises: `TimeoutError`, `WatcherCancelledError`, `ValueError` (malformed MCP signal JSON). Draft v1 signature matches M1 exactly.
- **Verdict**: confirm (d-document)

---

### `block-abc.CompletionEvent` / `CompletionSource` / `WatcherCancelledError` — internals
- **Draft v1 says**: d-private; issue `#TBD-completion-internals-private-marker`
- **Actual code** (confirmed via C1): All three present. `CompletionSource` is an enum with 3 values (MCP_FINISH_TOOL, FILE_WATCHER, USER_MARK_DONE). `CompletionEvent` is a frozen dataclass with `source`, `outputs: dict[str, Path]`, `detail`. `WatcherCancelledError` extends `RuntimeError`. These are in C1 with full detail but draft v1 groups them as single d-private entry.
- **Verdict**: confirm (d-private)

---

### `block-abc._PopenProcessAdapter` — ADR-019 compat shim
- **Draft v1 says**: d-private; issue `#TBD-popen-adapter-private-marker`
- **Actual code at `src/scieasy/blocks/app/app_block.py:L39-L56`** (confirmed): Confirmed. `pid` property, `is_alive()` method.
- **Verdict**: confirm (d-private)

---

### `block-abc._sequential_execute` — SubWorkflowBlock fallback
- **Draft v1 says**: d-private; issue `#890` (existing)
- **Actual code at `src/scieasy/blocks/subworkflow/subworkflow_block.py:L158-L187`** (confirmed via C1): Module-level function (not a method). `def _sequential_execute(blocks: list[Block], ...)`.
- **Verdict**: confirm (d-private)

---

### Issue-driven additions (block-abc) — ADR-028 §D8 cluster

#### `block-abc.IOBlock.supported_extensions` ClassVar + `_detect_format` helper
- **Draft v1 says**: c-impl; issue `#1073`; 0% implemented
- **Actual code at `src/scieasy/blocks/io/io_block.py`**: Confirmed 0% implemented. No `supported_extensions` ClassVar on `IOBlock`, no `_detect_format` helper.
- **Verdict**: confirm (c-impl)
- **P1 finding**: Draft v1 presents this as a single c-impl entry combining both `supported_extensions` ClassVar AND `_detect_format` helper, but issues #1073 (base ClassVar + helper) and #1074 (per-block declarations on LoadData/SaveData) are separate issues. The draft's "Issue-driven additions" section correctly lists them as distinct entries but the "Key decisions" section does not enumerate them as separate interface entries. Phase 6 SSOT will need TWO distinct entries: (1) `IOBlock.supported_extensions: ClassVar[dict[str, str]] = {}` and (2) `IOBlock._detect_format(self, path: Path) -> str | None` — both c-impl, both tracking issue #1073.

#### `block-abc.IOBlock._detect_format` helper
- **Draft v1 says**: c-impl; issue `#1073`
- **Verdict**: confirm (c-impl) — see P1 finding above on splitting from ClassVar entry.

#### `block-abc.IOBlock per-subclass supported_extensions` (LoadData, SaveData, LoadImage, SaveImage)
- **Draft v1 says**: c-impl; issues `#1075`/`#1076`
- **Actual code**: Confirmed 0% implemented.
- **Verdict**: confirm (c-impl)

#### `block-abc.iterate_over_axes` O(one-slice) contract drift
- **Draft v1 says**: b-docs-wins; issue `#888`; code calls `to_memory()` on full source
- **Verdict**: confirm (b-docs-wins)

#### `block-abc.SubWorkflowBlock._scheduler_factory` injection contract
- **Draft v1 says**: c-defer; issue `#890`
- **Verdict**: confirm (c-defer); TODO(#890) comment confirmed in source code.

#### `block-abc.AppBlock.bridge.prepare` dead JSON-dump fallback
- **Draft v1 says**: b-docs-wins; issue `#1080`
- **I2 says**: c-impl
- **Verdict**: P1 — sub-label conflict (see finding under AppBlock above and Manager merge fidelity section below)

#### `block-abc.AppBlock._bin_outputs_by_extension` Artifact downgrade
- **Draft v1 says**: b-docs-wins; issue `#1079`
- **I2 says**: c-impl
- **Verdict**: P1 — sub-label conflict (see Manager merge fidelity section below)

---

## Port-system per-entry findings

### `port-system.Port` — connection endpoint base
- **Draft v1 says**: M1 source-of-record; 13 total entries; a=7, b=2, c=0, d=4
- **Actual code at `src/scieasy/blocks/base/ports.py:L12-L21`**:
  ```python
  @dataclass(kw_only=True)
  class Port:
      name: str
      accepted_types: list[type]
      is_collection: bool = False
      description: str = ""
      required: bool = True
  ```
  Draft v1 says: a (unanimous K1/K2/K3). M1 places `is_collection` in a separate d-document entry. M2 places it inline in InputPort. The base `Port` class itself is `a`.
- **Verdict**: confirm (a for Port base class)
- **P1 finding**: Draft v1 module 2 breakdown says a=7, b=2, c=0, d=4. M1 has 13 entries for port-system. Let me count what draft v1's summary claims vs what M1 enumerates:
  - M1 entries: Port, Port.is_collection(d), InputPort(b), InputPort.constraint_description(a), OutputPort(a), constraint-semantics(b), validate_port_constraint(d), port_accepts_type+accepts_signature+validate_connection(d), ports_from_config_dicts(d), Two-phase validation(a), Dynamic port descriptor(a-crossref), ConstraintFn(d), constraint factory functions(a).
  - That is: 3a direct + 2b + 5d + 1a(constraint_description) + 2a(two-phase + constraint factories) = roughly 6a, 2b, 0c, 5d = 13. Close to a=7, b=2, d=4 but M1's count gives 6a not 7, and d=5 not 4 (the extra d is the `#TBD-port-is-collection-document` entry).
  - **P1**: Draft v1 says d=4 for port-system but M1 has AT LEAST 5 d-class entries (Port.is_collection, validate_port_constraint, port_accepts_type/accepts_signature/validate_connection as one grouped entry, ports_from_config_dicts, ConstraintFn). The count 4 likely collapsed `port_accepts_type / port_accepts_signature / validate_connection` as one entry and missed either `Port.is_collection` or `ConstraintFn`. Draft v2 must fix the breakdown.

---

### `port-system.Port.is_collection` — informational field
- **Draft v1 says**: d-document; issue `#TBD-port-is-collection-document`
- **Actual code at `src/scieasy/blocks/base/ports.py:L18`**: `is_collection: bool = False` confirmed on `Port` base class (not just InputPort).
- **Verdict**: confirm (d-document)
- **P2 finding**: Draft v1 says "Port.is_collection field: d-document (code has it; D1 doesn't list)" — this is correct. However, the `is_collection` field is on the **Port base class**, not just InputPort. Both M1 and M2 treat it as a Port-level field. M2 places it inline in InputPort signature, which is technically inaccurate (it's on Port). Phase 6 SSOT should make clear it is declared on Port (and inherited by InputPort and OutputPort).

---

### `port-system.InputPort` — input connection endpoint
- **Draft v1 says**: b-code-wins; `constraint: Callable[[Any], bool]` vs ARCH `Callable[[DataObject], bool]`; issue `#TBD-inputport-constraint-annotation`
- **Actual code at `src/scieasy/blocks/base/ports.py:L23-L29`**:
  ```python
  @dataclass(kw_only=True)
  class InputPort(Port):
      default: Any | None = None
      constraint: Callable[[Any], bool] | None = None
      constraint_description: str = ""
  ```
  Confirmed. Note: `is_collection` is NOT on InputPort (it's on Port base) — M2's port-system entry places `is_collection: bool = False` inside InputPort's signature, which is inaccurate.
- **Verdict**: confirm (b-code-wins)
- **P2 finding**: M2's InputPort signature puts `is_collection: bool = False` inside the InputPort block. This is wrong — `is_collection` is declared on the **Port base class**, not on InputPort. Draft v1 correctly lists Port.is_collection as a separate d-document entry. Phase 6 SSOT should NOT repeat `is_collection` inside InputPort's verbatim block; it's inherited from Port.

---

### `port-system.InputPort.constraint_description` — UI tooltip
- **Draft v1 says**: a (M1 upgrades from K1's d after verifying D1 does list it)
- **Actual code**: `constraint_description: str = ""` confirmed.
- **Verdict**: confirm (a)

---

### `port-system.OutputPort` — output connection endpoint
- **Draft v1 says**: a; no extra fields beyond Port
- **Actual code at `src/scieasy/blocks/base/ports.py:L32-L33`**: `@dataclass(kw_only=True) class OutputPort(Port): pass` — confirmed.
- **Verdict**: confirm (a)

---

### `port-system.constraint` callable semantics — free entry
- **Draft v1 says**: b-code-wins; callable receives Collection per ADR-020; issue `#TBD-port-constraint-callable-semantics-arch-update`
- **Actual code at `src/scieasy/blocks/base/ports.py:L76-L99`** (`validate_port_constraint`): The docstring explicitly says "ADR-020: value is a Collection (not an individual DataObject)." Code annotation is `Callable[[Any], bool]`. Confirmed.
- **Verdict**: confirm (b-code-wins)

---

### `port-system.validate_port_constraint` — constraint validator
- **Draft v1 says**: d-document; issue `#TBD-validate-port-constraint-document`
- **Actual code at `src/scieasy/blocks/base/ports.py:L76-L99`**: Signature `def validate_port_constraint(port: InputPort, value: Any) -> tuple[bool, str]` confirmed. Returns `(True, "")` or `(False, description)`. Exceptions caught and returned as `(False, "Constraint raised X: msg")`.
- **Verdict**: confirm (d-document)

---

### `port-system.port_accepts_type` / `port_accepts_signature` / `validate_connection` — port helpers
- **Draft v1 says**: d-document; issue `#TBD-port-helpers-document`
- **Actual code at `src/scieasy/blocks/base/ports.py:L37-L170`**: All three confirmed. `port_accepts_type` has ADR-020-Add6 Collection transparency logic (if `data_type` is a Collection instance, checks `item_type`). `validate_connection` does bidirectional subclass check (either `issubclass(src, tgt)` or `issubclass(tgt, src)`). M1 lists all three under a grouped d-document entry. Draft v1 follows M1.
- **Verdict**: confirm (d-document)
- **P2 finding**: `validate_connection` has a semantics subtlety: the bidirectional subclass check (`issubclass(src, tgt) or issubclass(tgt, src)`) means a connection is valid if either side is a subtype of the other. This is more permissive than a strict "source must be subtype of target" rule. Draft v1 and M1 do not surface this. Phase 6 SSOT d-document entry for `validate_connection` should include this behavior note.

---

### `port-system.ports_from_config_dicts` — variadic port materialization
- **Draft v1 says**: d-document; issue `#TBD-ports-from-config-dicts-document`
- **Actual code at `src/scieasy/blocks/base/ports.py:L102-L140`**: Confirmed. "Unknown type names fall back to DataObject. Port names dedup last-wins." Code says "duplicates are silently de-duplicated (last wins)" — wait, reading again: `if port_name in seen: continue` — this is **first-wins**, not last-wins. M1 says "Port names dedup last-wins" but code does `continue` on duplicate (skips second occurrence), making it **first-wins**.
- **Verdict**: P1 — code-truth error in M1/draft v1
- **Finding**: M1 documents `ports_from_config_dicts` as "Port names dedup **last-wins**." Code at `ports.py:L128-L133` is:
  ```python
  if port_name in seen:
      continue  # skip duplicate — first-wins, not last-wins
  seen[port_name] = None
  ```
  This is **first-wins deduplication**, not last-wins. Draft v1 inherits this error from M1. Phase 6 SSOT entry must say "Port names deduplicated: first occurrence wins."

---

### `port-system.Two-phase connection validation`
- **Draft v1 says**: a; design-time + pre-execution phases
- **Verdict**: confirm (a)

---

### `port-system.Dynamic port descriptor mechanism` — cross-ref entry
- **Draft v1 says**: a (covered under block-abc.Block.dynamic_ports)
- **Verdict**: confirm (a cross-ref)

---

### `port-system.ConstraintFn` — type alias
- **Draft v1 says**: d-document; issue `#TBD-constraintfn-document`
- **Actual code at `src/scieasy/utils/constraints.py:L65`**: `ConstraintFn = Callable[[Any], bool]` confirmed.
- **Verdict**: confirm (d-document)

---

### `port-system.constraint factory functions` — `has_axes`, `has_exact_axes`, `has_shape`, `has_min_shape`, `has_dtype`, `is_2d`, `is_3d`
- **Draft v1 says**: a; source `constraints.py:L68-L243`
- **Actual code at `src/scieasy/utils/constraints.py:L68+`** (confirmed): `has_axes`, `has_exact_axes` confirmed with correct signatures. The `ConstraintFn` docstring describes "callable that takes a Collection... and returns True iff every item satisfies the constraint." Factories return named closures with `__name__` and `__doc__` set.
- **Verdict**: confirm (a)

---

## Missing entries

Interfaces present in C1 / D1 / I1 / I2 but absent from draft v1's module 1+2 sections:

| Interface | Found in | Manager should add as | Severity |
|---|---|---|---|
| `block-abc.PackageInfo` dataclass | C1 (offset 461-479) — `@dataclass(frozen=True)` with `name, description, author, version` | d-document or a (it is cross-listed under block-registry in draft v1) | P2 — clarify cross-list |
| `block-abc.LazyList` (in CodeBlock auto-unpack path) | C1 (offset 483-504) | The draft lists LazyList under collection-transport but C1 places it in block-abc module. The "module" boundary is ambiguous. | P3 — add cross-ref note |
| `block-abc.AIBlock.auto_complete` config field | I2 (offset 167-173) — d-document, issue #882 | d-document — proposed AIBlock config field; I2 found it, draft v1 did not fold it | P2 — missing I2 finding |
| `block-abc.AIBlock._build_spawn_argv` method | C1 — method confirmed in AIBlock source | d-private footnote | P3 — minor |
| `block-abc.AIBlock._validate_and_load_outputs` method | C1 — method confirmed in AIBlock source | d-private footnote | P3 — minor |
| `block-abc.AIBlock._safe_transition` method | C1 — method confirmed in AIBlock source | d-private footnote | P3 — minor |
| `block-abc.IOBlock._resolved_input_port_name` | C1 — present in io_block.py | d-private footnote in IOBlock | P3 — minor |
| `block-abc.IOBlock._resolved_load_output_port_name` | C1 — present in io_block.py | d-private footnote in IOBlock | P3 — minor |
| `block-abc.IOBlock._resolved_save_receipt_port_name` | C1 — present in io_block.py | d-private footnote in IOBlock | P3 — backward-compat logic is load-bearing, should be noted |
| `block-abc.RunDir._default_expected_path` static method | C1 — confirmed in run_dir.py | d-private footnote in RunDir d-document entry | P3 |
| `block-abc.SubWorkflowBlock._run_with_scheduler` | C1, M2 — method in subworkflow_block.py | Mentioned in M2 but omitted from draft v1's SubWorkflowBlock key decisions | P2 — load-bearing stub |
| `block-abc.CompletionSource` (enum) | C1 — 3-value enum at completion.py | Draft v1 groups as d-private but doesn't enumerate the enum values | P3 — Phase 6 SSOT should enumerate values |
| `block-abc.ProcessBlock._process_item_takes_state` | C1 — private method for backward-compat 2-arg override detection | d-private footnote in ProcessBlock | P3 |
| `block-abc.CodeBlock._unpack_inputs` / `_repack_outputs` | C1 — private methods | d-private footnote in CodeBlock | P3 |
| `block-abc.AppBlock._output_port_extensions` | C1 + M2 — method in app_block.py | d-private footnote in AppBlock | P3 |
| `port-system.InputPort.default` field typing | C1: `default: Any | None = None`; D1: `default: DataObject | None = None` | Draft v1 doesn't surface this annotation divergence for `default` field | P2 — annotation differs |

---

## Manager merge fidelity issues

Cases where M1 or M2 had a detail that draft v1's delta lost:

### M1 lost: `ExternalAppBridge` usage contract
M1 notes (via C1) that "AppBlock.run() uses FileExchangeBridge **directly** (not via the protocol interface). The `watch()` method is not called from AppBlock.run() directly." Draft v1's key-decisions block for ExternalAppBridge says only "d-document (4 methods unspecified)" without capturing this critical architectural note. Phase 6 SSOT entry will be misleading without it.

### M1 lost: `ports_from_config_dicts` dedup semantics
M1 says "Port names dedup last-wins." Code is first-wins. This error survived into draft v1. (P1 finding above.)

### I1/I2 conflict unresolved: #1079 and #1080 sub-labels
I1 calls `AppBlock.bridge.prepare` dead fallback (#1080) **b-docs-wins** and `_bin_outputs_by_extension` downgrade (#1079) **b-docs-wins**. I2 calls both **c-impl**. Draft v1 picked I1's label without explaining the disagreement or resolving it. The correct label matters because b-docs-wins means "update code to match docs" while c-impl means "new implementation needed." Given that:
- #1080: the fallback is dead code (never reached by real AppBlock subclasses); the fix requires implementing a new materialisation path that doesn't exist yet → **c-impl** is more accurate
- #1079: the typed reconstruction mechanism doesn't exist yet; `Artifact` downgrade is the only code path → **c-impl** is more accurate

Draft v1 should have noted the I1/I2 disagreement and chosen. This is a P1 manager-merge-fidelity failure because it affects implementation priority.

### I2-only entry not folded: `AIBlock.auto_complete` config field (#882)
I2 found issue #882 (d-document; proposed `auto_complete: bool` config field for conditional-AND completion semantics). I1 did not find it. Draft v1's block-abc section does not include this finding. Given I2 found it independently, it should be folded as a d-document entry for block-abc.AIBlock under "Issue-driven additions." Manager missed this during fold.

### M2 port-system: `is_collection` placed inside InputPort (wrong)
M2's port-system section places `is_collection: bool = False` inside InputPort's signature block. Draft v1 correctly follows M1's structure (separate d-document entry). But Phase 6 must not lift from M2's §2 for this field — only from M1.

### M1/M2 both incomplete on `Block.pack` annotation
Both M1 and M2 note the annotation discrepancy (`list[DataObject]` in docs vs `list[Any]` in code) but M1 resolves to `a` (picks doc annotation). Draft v1 says `a` without surface-level notation of the annotation choice. Phase 6 verbatim lift from M1 will propagate the doc annotation; this should be an explicit b-docs-wins or b-code-wins decision, not silent `a`.

### `InputPort.default` typing not surfaced
D1 says `default: DataObject | None = None`. Code says `default: Any | None = None`. Neither M1 nor draft v1 surface this discrepancy. C1 reports `default: Any | None = None` (code truth). The SSOT for InputPort.default should be `Any | None` (code wins) with a note that semantic intent is `DataObject | None`.

---

## Sub-label re-classifications

| Entry | Current draft v1 label | Recommended label | Severity | Reason |
|---|---|---|---|---|
| `block-abc.Block.pack` / `Block.unpack` / `Block.unpack_single` | a | b-docs-wins (keep `list[DataObject]` as canonical) OR b-code-wins (use `list[Any]`) | P2 | Silent `a` obscures real annotation divergence; must be explicit |
| `block-abc.AppBlock.bridge.prepare` dead fallback (#1080) | b-docs-wins | c-impl | P1 | Fix requires new materialisation implementation, not just doc update |
| `block-abc.AppBlock._bin_outputs_by_extension` downgrade (#1079) | b-docs-wins | c-impl | P1 | Typed reconstruction mechanism does not exist; fix is new code |
| `port-system.Port.is_collection` | d-document (correct) | — (no change needed) | — | Confirm correct |
| `block-abc.dynamic_ports` | a | Verify: may need b-code-wins if `_validate_dynamic_ports` supports both `output_port_mapping` and `input_port_mapping` | P2 | M2 found backend validator accepts both; M1 shows only output-mapping |
| `block-abc.AIBlock.auto_complete` config (#882) | (missing) | d-document | P2 | I2 found this; not folded by manager |

---

## Recommended fixes for Phase 5 draft v2

### P1 fixes (must resolve before draft v2)

1. **`ports_from_config_dicts` dedup direction**: Change "last-wins" to "first-wins" in the d-document entry. Verify directly in `src/scieasy/blocks/base/ports.py:L128-L133`.

2. **#1079 and #1080 sub-label resolution**: Manager must decide b-docs-wins vs c-impl for `AppBlock._bin_outputs_by_extension` (#1079) and `AppBlock.bridge.prepare` (#1080). Auditor recommends **c-impl** for both (the required implementation — typed materialisation via BlockRegistry — does not exist). Record the decision in draft v2 explicitly.

3. **Port-system breakdown count correction**: Draft v1 says `a=7, b=2, c=0, d=4` for port-system. Actual M1 count gives at minimum `a=7, b=2, c=0, d=5` (Port.is_collection + validate_port_constraint + port_accepts_type/accepts_signature/validate_connection grouped + ports_from_config_dicts + ConstraintFn = 5 d-entries). Fix the breakdown table.

4. **`AIBlock.auto_complete` config field (#882)**: Fold I2's finding as a new d-document entry under block-abc.AIBlock issue-driven additions. Add to open issue placeholder list.

5. **`ExternalAppBridge` usage contract note**: Add to the d-document entry: "Note: AppBlock.run() uses FileExchangeBridge directly and does not go through this Protocol at runtime. The Protocol is the plugin extension point but is not runtime-enforced."

6. **`block-abc.dynamic_ports` shape verification**: Before Phase 6, verify `src/scieasy/blocks/registry.py:L388` (`_validate_dynamic_ports`) to determine if `input_port_mapping` is accepted by the backend. If yes, change label from `a` to `b-code-wins` with both mapping keys in the shape. If no, label stays `a` for the backend, and frontend drift is the only b-class issue.

### P2 fixes (should fix before draft v2)

7. **`Block.pack` / `unpack` / `unpack_single` sub-label**: Change from `a` to explicit `b-docs-wins` (keep `list[DataObject]`) or `b-code-wins` (use `list[Any]`). Recommended: b-docs-wins since DataObject is the semantic intent and the mismatch is annotation-only.

8. **`InputPort.default` annotation**: Add note that code has `Any | None = None`, D1 has `DataObject | None = None`. Recommend b-code-wins (`Any | None`) to match code truth.

9. **`validate_connection` bidirectional semantics**: Add behavior note to d-document entry: "Accepts connection if `issubclass(src, tgt)` OR `issubclass(tgt, src)` — more permissive than strict subtype check."

10. **`LLMProvider` d-remove caveat**: Add note "Removal requires caller audit — C1 flags potential non-AIBlock usage. Grep for callers before scheduling removal."

11. **`SubWorkflowBlock._run_with_scheduler`**: Add as explicit entry in SubWorkflowBlock's block, either d-private or included in the SubWorkflowBlock verbatim signature block in Phase 6. Currently mentioned in M2 but omitted from draft v1 key decisions.

12. **`PackageInfo` cross-list clarification**: Clarify that C1 places PackageInfo under block-abc but draft v1 assigns it to block-registry. Add a cross-reference redirect in the block-abc module.

13. **`Port.is_collection` is on Port base, not InputPort**: Verify Phase 6 SSOT places this correctly on Port base (not repeated in InputPort). Correct M2's placement.

### P3 fixes (nice-to-have)

14. **Private method footnotes in Phase 6**: Add d-private footnotes for: `ProcessBlock._process_item_takes_state`, `CodeBlock._unpack_inputs`/`_repack_outputs`, `AppBlock._output_port_extensions`, `IOBlock._resolved_{input,load_output,save_receipt}_port_name`, `AIBlock._build_spawn_argv`/`_validate_and_load_outputs`/`_safe_transition`, `RunDir._default_expected_path`, `FileExchangeBridge.launch` `argv_override` param.

15. **`CompletionSource` enum values**: Phase 6 SSOT d-private entry for CompletionSource should enumerate: `MCP_FINISH_TOOL`, `FILE_WATCHER`, `USER_MARK_DONE`.

16. **`FileWatcher.done_marker` param**: Note in d-private entry that `done_marker` triggers immediate return of non-marker output files.

17. **`LazyList` module assignment**: Clarify whether LazyList belongs to block-abc (C1 scope) or collection-transport (draft v1 assignment). The source file is `src/scieasy/blocks/code/lazy_list.py` — it lives in blocks/code, making block-abc or a CodeBlock sub-entry more accurate than collection-transport.

18. **`block-abc` total count**: M1 says 28 entries; draft v1 says 30 (M1's 28 + 2 from manager curation, but states "none — M1's 28 entries are complete"). This is contradictory. Manager should reconcile: the "+2 from manager curation" in the header contradicts the body text. Likely the 2 extras are the issue-driven entries that were folded. Fix wording.

---

## Issue traceability check

| Placeholder / Issue | Relevant? | Note |
|---|---|---|
| `#TBD-block-class-vars-arch-update` | Yes | Still needs GitHub issue |
| `#TBD-block-transition-document` | Yes | Still needs GitHub issue |
| `#TBD-block-validate-annotation` | Yes | Still needs GitHub issue |
| `#TBD-block-postprocess-annotation` | Yes | Still needs GitHub issue |
| `#TBD-block-persist-helpers-document` | Yes | Still needs GitHub issue |
| `#TBD-ioblock-load-output-dir` | Yes | Still needs GitHub issue |
| `#TBD-aiblock-arch-update-for-adr-035` | Yes | Still needs GitHub issue |
| `#TBD-subworkflow-classvar-vs-config` | Yes | Still needs GitHub issue |
| `#TBD-coderunner-protocol-document` | Yes | Still needs GitHub issue |
| `#TBD-runner-implementations-document` | Yes | Still needs GitHub issue |
| `#TBD-runner-registry-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-externalappbridge-document` | Yes | Still needs GitHub issue |
| `#TBD-fileexchangebridge-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-filewatcher-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-process-exited-error-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-llmprovider-legacy-removal` | Yes | Still needs GitHub issue; add caller audit caveat |
| `#TBD-aiblock-run-dir-protocol-document` | Yes | Still needs GitHub issue |
| `#TBD-completion-watcher-document` | Yes | Still needs GitHub issue |
| `#TBD-completion-internals-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-popen-adapter-private-marker` | Yes | Still needs GitHub issue |
| `#TBD-port-is-collection-document` | Yes (from M1, not in draft v1's placeholder list) | Draft v1's module 2 key decisions mention it but it's missing from the `#TBD-*` placeholder list in module 2. Add `#TBD-port-is-collection-document` to placeholder list. |
| `#TBD-inputport-constraint-annotation` | Yes (from M1, not in draft v1's module 2 placeholder list) | Same issue — M1 has `#TBD-inputport-constraint-annotation` but draft v1's module 2 section lists no `#TBD-*` placeholders at all. |
| `#TBD-port-constraint-callable-semantics-arch-update` | Yes | Same — missing from draft v1's module 2 `#TBD-*` list |
| `#TBD-validate-port-constraint-document` | Yes | Same |
| `#TBD-port-helpers-document` | Yes | Same |
| `#TBD-ports-from-config-dicts-document` | Yes | Same |
| `#TBD-constraintfn-document` | Yes | Same |
| `#888` (iterate_over_axes) | Yes — open issue confirmed relevant | b-docs-wins confirmed |
| `#890` (SubWorkflowBlock scheduler) | Yes — TODO in source confirmed | c-defer confirmed |
| `#1073` (supported_extensions base) | Yes — 0% impl confirmed | c-impl confirmed |
| `#1074` (LoadData/SaveData declarations) | Yes | c-impl confirmed |
| `#1075`/`#1076` (plugin IO blocks) | Yes | c-impl confirmed |
| `#1079` (AppBlock downgrade) | Yes — sub-label disputed | See P1 above |
| `#1080` (bridge.prepare fallback) | Yes — sub-label disputed | See P1 above |

**Critical gap**: Draft v1's module 2 (port-system) section has **zero** `#TBD-*` placeholders listed. M1 has at least 7 TBD placeholders for port-system (port-is-collection, inputport-constraint-annotation, constraint-callable-semantics, validate-port-constraint, port-helpers, ports-from-config-dicts, constraintfn). This is a **P1 omission** — Phase 9 cannot create GitHub issues for port-system if the placeholder list is empty in draft v1.
