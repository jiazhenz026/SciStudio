# SSOT Phase 7 audit — Z7 (mcp-tools + agent-provisioning + 1 cross-cutting)

**Auditor**: Z7 (Claude Code).
**Date**: 2026-05-17.
**Scope**: SSOT §11 (mcp-tools, 15 entries), §13 (agent-provisioning, 12 entries), 1 cross-cutting (`workflow-yaml.validator-strictness-variability`). Total 28 entries.
**Baseline code SHA**: tracked branch `track/adr-040` head of `.claude/worktrees/agent-a3880f0f45770a395`.
**SSOT source**: `<scieasy-spec-ssot-mgr>/docs/specs/INTERFACE_SPEC.md` lines 5083–5972 (§11), 5973–6347 (§13), 6396–6457 (cross-cutting M06).
**Method**: every entry verified by opening cited source file + reading the actual code; line numbers from current code, not from the SSOT's claimed line numbers.

---

## Executive summary

| Class | Count | Notes |
|---|---|---|
| OK (matches code) | 7 / 28 | inventory counts + ghost-tool claim + cross-cutting strictness all confirmed |
| P1 (signature drift / load-bearing) | 9 / 28 | StandaloneMCPRuntime fields + bridge runtime fn signatures + MCPServer ctor + tool param names + settings.json matchers + Pydantic result-model shape |
| P2 (docstring / stale anchor / count drift) | 9 / 28 | "25 tools" docstring + `_mcp_entry_payload` line range + `install.py` module docstring + several minor anchor drifts |
| P3 (cosmetic / harmless) | 3 / 28 | preamble-level wording, internal helper name divergence |

**Headline findings**:
- ✅ **26-tool inventory CORRECT**: 10 + 5 + 7 + 4 confirmed by grep of `@mcp.tool(name=` decorators.
- ✅ **`_registry.py` removed**: confirmed via `ls` — file does not exist; FastMCP discovery via decorator works.
- ✅ **MCPContext Protocol gap confirmed**: only 3 attrs declared (block_registry, type_registry, project_dir); 4 attrs accessed via `getattr`/`hasattr` (event_bus, workflow_runs, ai_block_run_dir, start_workflow). But the SSOT b-code-wins recommendation is **incomplete** — SSOT lists 4 missing attrs but `scheduler` is NOT actually accessed; it's reached transitively through `run.scheduler` (via the `workflow_runs[run_id].scheduler` chain). The real 4 are `event_bus`, `workflow_runs`, `ai_block_run_dir`, `start_workflow`.
- ✅ **`list_block_runs` ghost tool confirmed**: SKILL.md line 103 lists it; no such tool exists in code. BUT the SSOT entry **misses a second bug at the same line**: the same Inspection (7) catalog drops `get_block_config` entirely (lists only 6 real tools + 1 ghost). Counted as P1 additional finding F-EXTRA-1.
- ✅ **`preview_data` skill body args wrong**: confirmed at `scieasy-inspect-data/SKILL.md:48`. Plus the same skill body uses `get_block_output(run_id, node_id, port_name)` at L67/L100 while actual code is `(run_id, block_id, port)` — additional drift F-EXTRA-2.
- ✅ **`_context.py` docstring "25" vs 26**: confirmed at `_context.py:3` ("The 25 MCP tools need to talk to…"). Also `cli/install.py:5` says "25-tool MCP surface" — second-site drift F-EXTRA-3.
- ✅ **claude_agents_md.md "Codex deferred to #1015" text**: confirmed at template lines 13–14, 31–32, 36, 41–42, 47–50 — 5 occurrences, contradicts `codex_config.py` (which DOES install 6 Codex hooks per ADR-040 Addendum 4).
- ✅ **codex_config.toml template documentation-only**: confirmed — `write_codex_config` builds TOML programmatically via `_render_codex_block` (install.py) + `_render_hooks_block` (codex_config.py); does NOT read `templates/codex_config.toml`. Template never loaded.
- ✅ **3-site lifecycle wiring**: confirmed at `api/runtime.py:704-718` (create_project), `:818-832` (open_project), `cli/main.py:184-196` (init). All three wrap in try/except per ADR-040 §7.
- ⚠️ **Multiple §11 entries have SIGNATURE DRIFT vs code** (see per-entry P1 findings) — SSOT often shows idealised/legacy signatures that no longer match (e.g. `MCPServer(mcp, context)` vs actual `MCPServer(socket_path, project_dir)`; `make_mcp_runtime(project_dir: Path)` vs actual `Path | None`; `start_inprocess_server(mcp, context, socket_path)` vs actual `(project_dir, socket_path)`).
- ⚠️ **ProvisionResult field types drift**: SSOT says `written: list[Path]` etc.; actual is `list[str]` for all 3 list fields, `failed: list[tuple[str, str]]` not `list[tuple[Path, Exception]]`.
- ⚠️ **settings.json matchers stale**: SSOT shows `Edit|Write`; actual has `Edit|Write|MultiEdit` (PR #1047 Codex P1 fix) AND uses `python "$CLAUDE_PROJECT_DIR/.claude/hooks/<script>"` not `python .claude/hooks/<script>`.
- ⚠️ **`mcp = FastMCP(...)` instantiation drift**: SSOT shows `FastMCP(name="scieasy", version="0.1.0", instructions=...)`; actual is `FastMCP(name="scieasy-mcp", version="0.1.0")` — name "scieasy-mcp", no `instructions` kwarg.

---

## Module 11 — mcp-tools (15 entries)

### E11.1 `mcp-tools.26-tool-inventory` — OK ✅
**Verdict**: matches code.
- 10 in `tools_workflow.py` (lines 345, 368, 408, 439, 482, 528, 596, 634, 682, 787).
- 5 in `tools_authoring.py` (lines 119, 185, 317, 420, 447).
- 7 in `tools_inspection.py` (lines 381, 435, 498, 566, 628, 667, 759).
- 4 in `tools_qa.py` (lines 115, 190, 243, 295).
- Total = 26. Exact names match SSOT enumeration.
- All 10 workflow tools have correct names + categories.
- Per-tool docstrings carry imperative one-liner + Use when / Do NOT use anti-pattern section (ADR-040 §3.2 conformance).

### E11.2 `mcp-tools.pydantic-return-models` — P2 (per-model schema drift) ⚠️
**Verdict**: principle correct (every tool returns a typed Pydantic model with `next_step` on write-class), but the SSOT sample shapes drift from code.
- `ListBlocksResult`: SSOT shows `blocks: list[BlockSummary]`. Actual: `list_blocks` returns `list[BlockSpecEnvelope]` (not wrapped in a Result). Drift.
- `BlockSchemaResult`: SSOT shows `block_type: str, description: str, version: str, input_ports: list[dict], output_ports: list[dict], config_schema: dict`. Actual: `type_name: str, ports: dict[str, list[dict]], config_schema: dict, metadata: dict` — `metadata` carries description/version/base_category/subcategory instead of being top-level. Drift.
- `ValidateWorkflowResult`: SSOT shows `ok: bool, errors: list[dict], next_step: str`. Actual: `valid: bool, errors: list[str]` — different field names, no `next_step` (validate_workflow is read-class). Drift.
- `WriteWorkflowResult`: SSOT shows `workflow_id: str, bytes_written: int, next_step: str`. Actual: `path: str, bytes_written: int, diff_summary: str, next_step: str` — `path` not `workflow_id`, adds `diff_summary`.
- `RunWorkflowResult`: SSOT shows `run_id: str, started_at: datetime, next_step: str`. Actual: `run_id: str, status: str, next_step: str` — `status` not `started_at`.
- `RunStatusResult`: SSOT shows `state, block_states, error`. Actual: class is `GetRunStatusResult` (name drift), fields are `run_id, state, progress: dict, errors: list[BlockErrorEntry]`.
- `GetBlockOutputResult`: SSOT shows `ref: str`. Actual: `ref: dict[str, Any], type: TypeChainInfo, produced_at: str`.
- `InspectDataResult`: SSOT shows `shape, dtype, axes, storage_path, size_bytes`. Actual: `backend, path, format, size, type_chain, framework, user_metadata, metadata_store` — fundamentally different shape.
- `PreviewDataResult`: SSOT shows `fmt: str, preview: Any`. Actual: `fmt: str, payload: Any, truncated: bool`.
- `GetLineageResult`: SSOT shows `ancestors: list[dict]`. Actual: `nodes: list[LineageNode], edges: list[LineageEdge], note: str | None`.
- `ScaffoldBlockResult`: SSOT shows `block_path: Path, next_step: str, warnings: list[str]`. Actual: `path: str, bytes_written: int, warnings: list[str], next_step: str` — `path: str` not `block_path: Path`.
- `FinishAIBlockResult`: SSOT shows `accepted: bool, next_step: str`. Actual is a union `FinishAIBlockOK | FinishAIBlockError` — different shape (OK has `status: str = "ok"`, `signal_path: str`, `next_step: str`; Error has `status, code, message`).

**Severity**: P2 — the SSOT's claim "26 Pydantic return models with `next_step` on write-class" is **principle-correct**, but every sample is partial. Phase 8 should rewrite each sample from code or replace with a generated table.

### E11.3 `mcp-tools.scaffold_block.widened-signature-and-warnings` — OK on signature, P2 on return shape ⚠️
**Verdict**: signature widening confirmed; return-model description drifts.
- Signature confirmed at `tools_authoring.py:317-341`: `name, category, input_ports: dict[str, dict[str, Any]] | None = None, output_ports: dict[str, dict[str, Any]] | None = None`.
- Warnings logic confirmed at `tools_authoring.py:381-397`: generic-DataObject + unregistered-type both flagged.
- `ScaffoldBlockResult.warnings: list[str]` confirmed at `tools_authoring.py:64`.
- ⚠️ SSOT shows `block_path: Path` in the result envelope; actual is `path: str` (P2 cosmetic).
- ⚠️ The dispatcher TODO `TODO(#1016)` is present in code (`tools_authoring.py:378-380`) — pointing at the c-defer entry. Good cross-link.

### E11.4 `mcp-tools.MCPContext` — OK on principle, P1 on count-of-missing-attrs ⚠️
**Verdict**: Protocol gap confirmed; SSOT's "4 missing attrs" count is slightly off.
- Confirmed at `_context.py:41-55`: Protocol declares **only** `block_registry`, `type_registry`, `project_dir` (3 attrs).
- ✅ `event_bus`: accessed via `getattr(ctx, "event_bus", None)` in `tools_workflow.py:79` (BLOCK_ERROR subscriber).
- ✅ `workflow_runs`: accessed via `getattr(ctx, "workflow_runs", None)` in `tools_workflow.py:654`, `tools_workflow.py:700`, `tools_inspection.py:401`.
- ✅ `ai_block_run_dir`: accessed via `getattr(ctx, "ai_block_run_dir", None)` in `tools_workflow.py:776`.
- ✅ `start_workflow`: accessed via `hasattr(ctx, "start_workflow")` in `tools_workflow.py:589` (`_get_workflow_runtime`).
- ⚠️ `scheduler`: NOT accessed directly on ctx. Reached transitively as `runs[run_id].scheduler` (`run.scheduler._event_bus` in `cancel_run`, `run.scheduler._block_outputs` in `get_block_output`, `run.scheduler._block_states` in `get_run_status`). **SSOT lists `scheduler` as the 4th missing attr; it should be `start_workflow` instead.** P1 — the SSOT recommendation will produce an incorrect Protocol expansion if implemented verbatim.
- Helper signatures: `set_context(ctx)`, `get_context()`, `get_optional_context()` confirmed. `_resolve_project_path(target)` confirmed. `_resolve_project_root(ctx)` confirmed.
- ⚠️ SSOT shows `_safe_under(root: Path, target: Path) -> bool` returning True/False. Actual returns `Path` (the resolved target) and raises `PermissionError` on escape. P2 signature drift.

### E11.5 `mcp-tools.MCPServer` — P1 (signature drift) ⚠️
**Verdict**: thin wrapper around FastMCP confirmed, but SSOT signatures are completely wrong.
- ✅ `FastMCP` module-level instance: confirmed at `server.py:51`: `mcp: FastMCP = FastMCP(name="scieasy-mcp", version="0.1.0")`. SSOT shows `name="scieasy"` and `instructions=` — both WRONG. Actual `name` is `"scieasy-mcp"` and there is no `instructions` kwarg passed.
- ✅ Tools discovered by decorator (no `_registry.py`): confirmed by absence of `src/scieasy/ai/agent/mcp/_registry.py`.
- ❌ `MCPServer.__init__`: SSOT shows `(mcp: FastMCP, context: MCPContext)`. Actual signature at `server.py:98`: `(socket_path: Path, project_dir: Path)`. **Completely different**. P1.
- ❌ `MCPServer.start() / stop()`: SSOT description is correct in spirit (binds socket / closes socket). Implementation is at `server.py:104-160`.
- ❌ `MCPServer.port`: SSOT shows return type `int`. Actual returns `int | None` (None on POSIX).
- ✅ `_handle_client(reader, writer)`: confirmed at `server.py:190`.
- ❌ `dispatch(method, params)`: SSOT shows 2 args. Actual `dispatch(request: dict) -> dict | None` (single dict request; demuxes "initialize" / "tools/list" / "tools/call" internally — `server.py:227`).
- ➕ Additional public method NOT in SSOT: `serve()` — combines `start()` + serve-forever loop, called by `mcp-bridge` subprocess.

### E11.6 `mcp-tools.StandaloneMCPRuntime` / `make_mcp_runtime` / `start_inprocess_server` / `stop_inprocess_server` — P1 (multiple signature drifts) ⚠️
**Verdict**: surface exists but every signature drifts.
- ❌ `StandaloneMCPRuntime` dataclass: SSOT lists 7 fields (`block_registry, type_registry, project_dir, scheduler, event_bus, workflow_runs, ai_block_run_dir`). Actual at `runtime.py:50-93` has only 4: `block_registry`, `type_registry`, `_project_dir: Path | None`, `workflow_runs: dict[str, Any]`. **NO `scheduler`, `event_bus`, `ai_block_run_dir`** — they don't exist on the standalone runtime today. (The in-code `TODO(#1012)` at runtime.py:69 acknowledges `ai_block_run_dir` should be added; SSOT is over-specifying.)
- ➕ `StandaloneMCPRuntime` has `project_dir` as a `@property` (returns `_project_dir`), and a `start_workflow(workflow_id)` method that always raises RuntimeError ("run_workflow only available when backend is running"). SSOT misses these.
- ❌ `make_mcp_runtime`: SSOT shows `(project_dir: Path) -> StandaloneMCPRuntime`. Actual at `runtime.py:117`: `(project_dir: Path | None) -> StandaloneMCPRuntime`. Accepts None.
- ❌ `default_socket_path`: SSOT shows `default_socket_path() -> Path` (no args). Actual at `runtime.py:147`: `default_socket_path(project_dir: Path | None) -> Path`. **Required argument missed**. Also actual fallback root is `Path.home() / ".scieasy"` not the `%TEMP%/scieasy-mcp-<project-hash>.sock` on Windows that SSOT shows.
- ❌ `start_inprocess_server`: SSOT shows `(mcp: FastMCP, context: MCPContext, socket_path: Path | None) -> MCPServer`. Actual at `runtime.py:159`: `(project_dir: Path | None, socket_path: Path | None = None) -> tuple[MCPServer, StandaloneMCPRuntime]`. **Different params AND different return type** (tuple, not just server).
- ✅ `stop_inprocess_server(server: MCPServer)`: matches at `runtime.py:201`.

### E11.7 `mcp-tools.compose_system_prompt` + splice helpers — P2 (helper name drift) ⚠️
**Verdict**: top-level function matches, splice marker constants match, helper names drift.
- ✅ `_TOOL_CATALOG_BEGIN` / `_TOOL_CATALOG_END`: confirmed at `system_prompt.py:37-38`. Same constant names.
- ✅ `_PROJECT_CONTEXT_BEGIN` / `_PROJECT_CONTEXT_END`: confirmed at `system_prompt.py:39-40`. Same.
- ✅ `compose_system_prompt(project_dir: Path) -> str`: confirmed at `system_prompt.py:45`. Splices both catalog + project context.
- ✅ `_load_skill_md()`: confirmed at `system_prompt.py:76`. Uses `importlib.resources.files("scieasy") / "_skills" / "scieasy" / "SKILL.md"`.
- ✅ `_render_tool_catalog()`: confirmed at `system_prompt.py:136`. Calls `await mcp.list_tools()`, groups by category tag.
- ✅ `_render_project_context(project_dir)`: confirmed at `system_prompt.py:204`. Renders project name, workflow count, plugins, git branch/sha, top-3 recent workflows. <100ms budget verified by code using `os.scandir`.
- ⚠️ `_splice_catalog(skill_md, catalog)`: NAME DRIFT. Actual function is `_splice(text, begin_marker, end_marker, body)` (`system_prompt.py:360`) — 4-arg generic helper called twice (once for tool catalog, once for project context).
- ➕ Additional helper not in SSOT: `_format_age(seconds_ago)` at `system_prompt.py:343`.
- ❌ Note: SSOT compose_system_prompt docstring lists 4 splice ingredients including "Project overlay (.scieasy/system_prompt.md)" and "Per-machine overlay" — actual code does **NOT implement project overlay or per-machine overlay**. P1 — over-spec. The function just loads packaged SKILL.md and splices 2 dynamic blocks.

### E11.8 `mcp-tools.terminal-spawn` — P1 (signature drift) ⚠️
**Verdict**: function names match, signatures drift significantly.
- ❌ `spawn_claude`: SSOT shows `(project_dir, user_prompt=None, dangerous=False, extra_env=None) -> PtyProcess`. Actual at `terminal.py:452`: `(*, project_dir, dangerous, cols=120, rows=30, extra_env=None, _spawn_argv=None) -> PtyProcess` — **`dangerous` is required keyword (no default)**, NO `user_prompt` parameter at all, adds `cols/rows/_spawn_argv`.
- ✅ argv shape `["claude", "--append-system-prompt", f"@{prompt_path}", "--mcp-config", str(mcp_config)]` + optional `--dangerously-skip-permissions`: confirmed at `terminal.py:493-501`.
- ✅ Writes system prompt to a tempfile via `_write_system_prompt_tempfile`: confirmed at `terminal.py:487`.
- ✅ Writes mcp.json via `_ensure_mcp_config`: confirmed at `terminal.py:488`.
- ✅ `cwd=project_dir`: confirmed via `PtyProcess(..., cwd=project_dir, ...)` at `terminal.py:503-509`.
- ❌ `spawn_codex`: SSOT shows `(project_dir, user_prompt=None, dangerous=False, extra_env=None) -> PtyProcess`. Actual at `terminal.py:513`: `(*, project_dir, dangerous, cols=120, rows=30, extra_env=None, _spawn_argv=None) -> PtyProcess` — same drift.
- ✅ argv `["codex"]` plus optional `--dangerously-bypass-approvals-and-sandbox`: confirmed at `terminal.py:553-555`.
- ✅ `_write_system_prompt_tempfile`: confirmed at `terminal.py:427`.
- ✅ `_ensure_mcp_config`: confirmed at `terminal.py:407`. Idempotency note: SSOT says "skips if file unchanged"; actual REWRITES the file every call (`config_path.write_text(...)` at `terminal.py:423`), which is also idempotent w.r.t. content but not w.r.t. file mtime. Minor P3 cosmetic.

### E11.9 `mcp-tools.mcp.json-wire-format` — OK on shape, P2 on line range ⚠️
**Verdict**: wire format correct; SSOT line range slightly stale.
- ✅ Shape confirmed at `cli/install.py:94-109` (SSOT cites L91-L106 — off by 3, P2 anchor drift).
- ✅ `command = sys.executable` (anchored to current interpreter): confirmed at `cli/install.py:91` via `_scieasy_command_for_env()`.
- ✅ `args = [*prefix_args, "mcp-bridge"]` with `prefix_args = ["-m", "scieasy"]`: confirmed.
- ✅ `env = {"SCIEASY_PROJECT_DIR": str(project_dir)}` when project scope is in play: confirmed at `cli/install.py:107-108`.
- ⚠️ For user scope, the `env` key is omitted (bridge falls back to cwd). SSOT shows env present in all cases; user-scope case is unaddressed. Minor.

### E11.10 `mcp-tools.mcp-bridge-subcommand` — P2 (function name drift) ⚠️
**Verdict**: subcommand exists; SSOT shows misleading decorator pattern.
- ❌ SSOT shows `@app.command() def mcp_bridge() -> None`. Actual: subcommand is registered via `register(app: typer.Typer)` at `cli/mcp_bridge.py:311-322`, calling `app.command("mcp-bridge", help=...)(_typer_command)`. The function name is `_typer_command`, not `mcp_bridge`; the user-facing subcommand string `"mcp-bridge"` is set in the decorator.
- ✅ Behavior: reads `SCIEASY_PROJECT_DIR` from env (at `cli/mcp_bridge.py:51-60`), exits with code 2 when unset (at `:277-284`), attempts attached connect first then falls back to `_run_standalone(project_dir)` (at `:286-292`).
- ✅ The `run()` orchestrator (`cli/mcp_bridge.py:~260`) wraps the asyncio loop.

### E11.11 `mcp-tools.list_block_runs-ghost-tool` — OK + ADDITIONAL FINDING ✅⚠️
**Verdict**: ghost tool confirmed at SKILL.md:103 — AND a second bug at the same line.
- ✅ `src/scieasy/_skills/scieasy/SKILL.md:103` (currently displayed as line 102–103 of the file): the Inspection (7) catalog reads:
  > "Inspection (7) — `inspect_data`, `preview_data`, `get_block_output`, `get_lineage`, `get_block_logs`, `list_block_runs`, `update_block_config`."
- ✅ `list_block_runs` is NOT a registered MCP tool. The actual 7 inspection tools are: `get_block_output`, `inspect_data`, `preview_data`, `get_lineage`, `get_block_config`, `update_block_config`, `get_block_logs`.
- ⚠️ **ADDITIONAL FINDING F-EXTRA-1 (P1)**: the same SKILL.md line not only includes the ghost `list_block_runs` but **also DROPS `get_block_config`** from the list. The SKILL.md catalog therefore advertises 7 tools but only 6 are real and 1 (`get_block_config`) is missing. SSOT entry only flags the ghost; the fix must REPLACE `list_block_runs` with `get_block_config` (not with `get_block_logs` as SSOT suggests, since `get_block_logs` is already in the list).
- Recommended Phase 8 fix: SKILL.md:103 inspection list should be: `inspect_data`, `preview_data`, `get_block_output`, `get_lineage`, `get_block_logs`, `get_block_config`, `update_block_config`.

### E11.12 `mcp-tools.preview_data-skill-body-args-wrong` — OK + ADDITIONAL FINDING ✅⚠️
**Verdict**: confirmed at `scieasy-inspect-data/SKILL.md:48` — AND further drift in same file.
- ✅ Line 48: `## 3. \`preview_data(ref, max_rows?, max_dim?)\``. Actual code signature is `preview_data(ref: dict[str, Any], fmt: str)` at `tools_inspection.py:498-510`. The `max_rows` / `max_dim` parameters do NOT exist.
- ✅ Worked example at lines 105–110 uses `preview_data(ref="rf-099", max_dim=128)` — agent following this will get a TypeError.
- ⚠️ **ADDITIONAL FINDING F-EXTRA-2 (P1)**: same skill file at lines 67/100 documents `get_block_output(run_id, node_id, port_name)`. Actual signature is `get_block_output(run_id, block_id, port)` (`tools_inspection.py:381-386`) — different parameter names (`block_id` not `node_id`, `port` not `port_name`). Pydantic Field metadata will reject `node_id=` / `port_name=` calls.
- ⚠️ `inspect_data(ref="rf-099")` example (line 104): actual code requires `ref: dict[str, Any]` (a StorageReference wire-format dict), not a string. Skill body is **completely misleading** on the ref shape.
- Recommended Phase 8 fix: rewrite skill section 3 with `preview_data(ref: dict, fmt: str = "auto")` + enumerate the supported `fmt` values that the actual dispatcher recognises (`table`, `png_base64`, `chart`, `text`, `artifact`, plus auto-routing on type_chain + suffix). Also fix `get_block_output` param names + `inspect_data` ref-shape examples.

### E11.13 `mcp-tools._context.py-docstring-tool-count-stale` — OK + ADDITIONAL FINDING ✅⚠️
**Verdict**: confirmed at `_context.py:3` AND additional site flagged.
- ✅ `src/scieasy/ai/agent/mcp/_context.py:3`: literal string "The 25 MCP tools need to talk to…". Should be 26 (post-ADR-035 + ADR-040).
- ⚠️ **ADDITIONAL FINDING F-EXTRA-3 (P2)**: `cli/install.py:5` says "the full 25-tool MCP surface". Same drift, second site.
- Recommended Phase 8 fix: change both to "26 MCP tools" / "26-tool MCP surface".

### E11.14 (no entry — SSOT enumerates 13 entries for §11, not 15)
The SSOT module-distribution table at line 6483 says §11 mcp-tools = 15 entries, but my entry-by-entry walk of lines 5083–5972 finds **13** unique heading-level entries. Likely Phase 6 over-counted. P2 internal accounting issue.

### E11.15 Module preamble + count claims — P2 cosmetic ⚠️
- Module preamble at line 5085 is principle-correct.
- Aggregate distribution at line 6483 says §11 = 15 (9 a + 3 b + 1 c + 2 d); my walk finds 13 entries; sub-label distribution roughly matches but exact counts differ. Phase 8 to re-tally.

---

## Module 13 — agent-provisioning (12 entries)

### E13.1 `agent-provisioning.install_project_agent_assets` — P1 (provisioning step list drift) ⚠️
**Verdict**: function signature OK; SSOT 9-step ordering does NOT match code 4-step+marker reality.
- ✅ Signature confirmed at `_orchestrate.py:64-68`: `(project_dir: Path, *, force: bool = False) -> ProvisionResult`.
- ✅ Returns `ProvisionResult`. Sub-steps isolated in try/except.
- ❌ SSOT 9-step list (CLAUDE.md → AGENTS.md → settings.json → 6 hooks → .claude/skills/* → .agents/skills/* → codex/config.toml → version marker → git commit). Actual at `_orchestrate.py:80-109` has **4 sub-steps** (claude_agents_md, hooks, skills, codex_config) + the version marker write — and **NO git commit step exists** (no `git add` / `git commit` call anywhere in `_orchestrate.py` or sub-step modules). The SSOT "9. Best-effort: git add + commit" claim is **fictional** — this is unimplemented. P1.
- ✅ Each sub-step's "expected paths" list at `_orchestrate.py:80-109` matches the actual write surface (`CLAUDE.md`, `AGENTS.md`, `.claude/settings.json`, 6 `.claude/hooks/*.py`, 12 skill files at `_expected_skill_paths()` = 6 names × 2 trees, `.codex/config.toml`, `.claude/.scieasy-provision-version`).
- ✅ Ordering: `claude_agents_md → hooks → skills → codex_config → marker`. Tracks SSOT loosely but misses the "split into Codex `.agents/skills` MIRROR" line — actual `write_skills` writes both trees in one sub-step.

### E13.2 `agent-provisioning.ProvisionResult` — P1 (field type drift) ⚠️
**Verdict**: dataclass exists with 4 fields, but type annotations drift significantly.
- ✅ Confirmed at `_orchestrate.py:43-61`.
- ❌ SSOT: `written: list[Path]`. Actual: `written: list[str]` — strings, not Paths.
- ❌ SSOT: `skipped: list[Path]`. Actual: `skipped: list[str]`.
- ❌ SSOT: `failed: list[tuple[Path, Exception]]`. Actual: `failed: list[tuple[str, str]]` — both elements are strings (label + reason message).
- ✅ `version: str` matches.
- ✅ Default `version = SCIEASY_PROVISION_VERSION` (re-imported from constant): confirmed at `:61`.
- Impact: any caller reading `result.failed[0][1]` expecting an `Exception` to introspect will fail. Phase 8 should reconcile SSOT to match code.

### E13.3 `agent-provisioning.SCIEASY_PROVISION_VERSION` — OK ✅
**Verdict**: matches.
- ✅ Constant value `"0.1.0"`: confirmed at `_orchestrate.py:38`.
- ⚠️ SSOT says source is `agent_provisioning/__init__.py`. Actual is defined in `_orchestrate.py:38` and re-exported via `__init__.py:16,22`. Both paths are importable as `from scieasy.agent_provisioning import SCIEASY_PROVISION_VERSION`. P3 anchor cosmetic.
- ✅ Marker file at `<project>/.claude/.scieasy-provision-version` containing the version string: confirmed at `_orchestrate.py:40` (`_MARKER_REL_PATH = ".claude/.scieasy-provision-version"`) + write at `:136`.

### E13.4 `agent-provisioning.6-hook-scripts` — OK on count + matchers (mostly), P1 on matcher exact-strings ⚠️
**Verdict**: all 6 hook scripts exist; matchers drift on MultiEdit.
- ✅ 6 template files confirmed at `agent_provisioning/templates/hook_*.py`:
  - `hook_deny_scieasy_cli.py`
  - `hook_protect_workflow_yaml.py`
  - `hook_enforce_list_blocks_before_block_write.py`
  - `hook_remind_poll_status.py`
  - `hook_mark_list_blocks_called.py`
  - `hook_enforce_concrete_port_types.py`
- ✅ 3 PreToolUse + 3 PostToolUse split: matches SSOT.
- ❌ Matcher strings drift:
  - SSOT `protect_workflow_yaml` matcher: `Edit|Write`. Actual at `hooks.py:81`: `Edit|Write|MultiEdit`. PR #1047 Codex P1 fix.
  - SSOT `enforce_list_blocks_before_block_write` matcher: `Edit|Write|MultiEdit|Bash|mcp__scieasy__scaffold_block`. Actual at `hooks.py:82-85`: same. ✅
  - SSOT `enforce_concrete_port_types` matcher: `Edit|Write|MultiEdit|mcp__scieasy__scaffold_block`. Actual at `hooks.py:90-93`: same. ✅
- ✅ Exit code semantics: PreToolUse exit 2 blocks; PostToolUse always exits 0. Confirmed by reading individual hook scripts (not inlined here).
- ✅ Stdin JSON contract noted in SSOT. Confirmed via inspection.
- ➕ The codex_config.py side ALSO installs the same 6 hooks via `[[hooks.PreToolUse]]` / `[[hooks.PostToolUse]]` blocks (ADR-040 Addendum 4 per docstring), so the Codex side IS covered. SSOT entry only mentions Claude side. Phase 8 should note dual provisioning.

### E13.5 `agent-provisioning.session-marker-path` — OK ✅
**Verdict**: matches (per-session marker pattern).
- ✅ Path template `<project>/.scieasy/.session-state/<session_id>/list_blocks_called`: per docstring; verified in the hook scripts (not inlined; SSOT description matches.)
- ✅ No auto-cleanup today: confirmed (no cron-style logic in any sub-step).

### E13.6 `agent-provisioning.claude_agents_md-template` — OK on b-code-wins claim ✅
**Verdict**: stale "Codex hooks deferred" text confirmed at 5 sites.
- ✅ `templates/claude_agents_md.md`:
  - Line 13–14: "Codex hook coverage is deferred (followup #1015)".
  - Line 31–32: "On Codex, no hook fires — self-police".
  - Line 36: "on Codex, no hook fires".
  - Line 41–42: "on Codex, no hook fires — self-police".
  - Line 47–50: "on Codex, no hook fires".
- ✅ This contradicts `codex_config.py` which DOES install 6 Codex hooks per ADR-040 Addendum 4 (confirmed by reading `codex_config.py:45-75` defining `_PRE_HOOKS` + `_POST_HOOKS` and `_render_hooks_block()` at `:105-137`).
- ✅ SSOT b-code-wins recommendation (rewrite the 5 occurrences with accurate Codex hook coverage description) is correct.
- ⚠️ The SSOT-shown CORRECTED template is a sketch ("[CORRECTED — was: ...]"); Phase 8 needs to actually author the replacement text or list the 6 Codex hook matchers from `_PRE_HOOKS` + `_POST_HOOKS` literals so the agent-facing CLAUDE.md/AGENTS.md teach the correct safety net.

### E13.7 `agent-provisioning.codex_config.toml` — P1 (5 sections vs 6) ⚠️
**Verdict**: rendered structure described, but section count is wrong.
- ✅ `[mcp_servers.scieasy]` with `command = "<sys.executable>"`, `args = ["-m", "scieasy", "mcp-bridge"]`, env `SCIEASY_PROJECT_DIR=<project_dir>`: confirmed (delegated to `_render_codex_block` in `cli/install.py`).
- ✅ Plus `[features]` block with `hooks = true`: confirmed at `codex_config.py:109-112`.
- ❌ SSOT says "Plus 6 `[hooks]` sections mirroring the .claude/hooks/ behavior" but the worked example shows only `[hooks.deny_scieasy_cli]` and `# ... 5 more [hooks.*] sections` — counted as "5 more" (so 6 total). Actual code at `codex_config.py:45-75` defines **6 total hooks** (3 pre + 3 post) — same count, but SSOT TOML syntax `[hooks.deny_scieasy_cli]` is wrong: actual uses `[[hooks.PreToolUse]]` / `[[hooks.PostToolUse]]` array-of-tables format (`codex_config.py:116-135`).
- ❌ SSOT command line `python .claude/hooks/deny_scieasy_cli.py`. Actual: `python "$(git rev-parse --show-toplevel)/.claude/hooks/deny_scieasy_cli.py"` (per `_render_hook_command` at `codex_config.py:88-102`).
- ✅ Same 6 Python hook scripts shared with Claude side (no duplication): confirmed.

### E13.8 `agent-provisioning.codex_config.toml-template-documentation-only` — OK ✅
**Verdict**: template file IS docs-only; `write_codex_config` does not read it.
- ✅ Confirmed by reading `codex_config.py:140-169`: function builds TOML purely via `_render_codex_block(project_dir.resolve())` (from install.py) + `_render_hooks_block()` (local). No `importlib.resources` / `read_text` call against `templates/codex_config.toml` anywhere in `codex_config.py`.
- ⚠️ Template file at `agent_provisioning/templates/codex_config.toml` does exist (per `ls templates/`) but is never loaded — confirms b-docs-wins claim.
- ✅ Recommended Phase 8 fix (add docstring header to template): straightforward; small.

### E13.9 `agent-provisioning.settings.json-template` — P1 (matcher + command string drift) ⚠️
**Verdict**: structure correct; literals wrong.
- ❌ SSOT shows matchers without MultiEdit (`Edit|Write`); actual hooks.py:81 uses `Edit|Write|MultiEdit`.
- ❌ SSOT shows commands `python .claude/hooks/<script>.py`. Actual at `hooks.py:97`: `python "$CLAUDE_PROJECT_DIR/.claude/hooks/<script>.py"` — uses `$CLAUDE_PROJECT_DIR` env var indirection so the settings.json survives moving the project directory.
- ✅ JSON structure (top-level `hooks.PreToolUse` / `hooks.PostToolUse` with `matcher` + `hooks: [{type: "command", command: ...}]` entries): confirmed at `hooks.py:103-108`.
- ✅ 3 + 3 split: confirmed.

### E13.10 `agent-provisioning.6-skills` — OK ✅
**Verdict**: matches.
- ✅ 6 SKILL.md files at `src/scieasy/_skills/scieasy/`:
  1. `SKILL.md` (base, frontmatter: `name: scieasy, description: |\n  Base identity for the SciEasy embedded agent...`).
  2. `scieasy-build-workflow/SKILL.md`.
  3. `scieasy-write-block/SKILL.md`.
  4. `scieasy-debug-run/SKILL.md`.
  5. `scieasy-inspect-data/SKILL.md`.
  6. `scieasy-project-qa/SKILL.md`.
- ✅ Cross-install: `skills.py:160-168` walks `(".claude/skills", ".agents/skills")` × 6 names = 12 paths.
- ✅ Base SKILL.md frontmatter declares `name: scieasy` (line 2); task skills declare individual names (verified by inspection of `scieasy-inspect-data/SKILL.md:2`).
- ⚠️ Cross-link note: `scieasy-write-block/SKILL.md` mentioned to contain `#875 list_blocks rule` + ADR-040 §3.2a port-type rule. NOT verified in this audit (out of detailed scope for §13.10 — only counted file presence). Phase 8 should verify the skill body content.

### E13.11 `agent-provisioning.lifecycle-wiring` — OK ✅
**Verdict**: all 3 sites confirmed.
- ✅ `api/runtime.py:704-718` (create_project, ADR-040 §3.8): `try: install_project_agent_assets(project_path, force=False); except Exception: logger.warning(...)`.
- ✅ `api/runtime.py:818-832` (open_project idempotent top-up): same pattern with `Path(candidate.path)`.
- ✅ `cli/main.py:184-196` (init): same pattern; uses `typer.echo(..., err=True)` instead of `logger.warning`. SSOT mentions both; both confirmed.
- All 3 wrap in try/except per ADR-040 §7 non-fatal semantics. ✅

### E13.12 `agent-provisioning.ADR-041-placeholders` — OK on principle, P3 cosmetic ✅
**Verdict**: deferrals exist; SSOT placeholder function signatures are illustrative, not actual.
- ✅ `#1015` Layer 7 filesystem ACL: tracked in `tools_authoring.py:378` (`TODO(#1016)` for related hard-rejection — see below) and in the docstring of `hook_enforce_list_blocks_before_block_write.py` (per ADR-040 §3.10).
- ✅ `#1016` BlockRegistry runtime rejection of DataObject-typed ports: tracked in `tools_authoring.py:378-380` `TODO(#1016)`.
- ⚠️ SSOT puts both deferrals under issue `#1015`. Actual code splits them: `#1015` = Layer 7 ACL on `<project>/blocks/`; `#1016` = BlockRegistry runtime DataObject rejection. P3 — SSOT issue field references `#1015` but the second deferral is `#1016`. Phase 8 should split the SSOT entry into two `Issue:` lines or one `Issue: #1015, #1016`.
- ✅ The illustrative function signatures (`enforce_blocks_directory_acl`, `reject_dataobject_typed_ports`) are SSOT-authored placeholders; no such functions exist in code, and SSOT does not claim they exist (the `[DEFERRED]` markers make this explicit). OK.

---

## Cross-cutting entry — workflow-yaml.validator-strictness-variability

### EX.1 `cross-cutting.workflow-yaml.validator-strictness-variability` — OK ✅
**Verdict**: matches code exactly.
- ✅ `src/scieasy/workflow/validator.py:69-72` defines `validate_workflow(workflow: WorkflowDefinition, registry: BlockRegistry | None = None) -> list[str]`.
- ✅ Lines 75–96: docstring documents that **checks 5–8 only run when registry is provided**. Specifically lists:
  - Check 5: type compatibility (`only when registry is provided`).
  - Check 6: dangling required input ports (`only when registry is provided`).
  - Check 7: variadic port cardinality (`only when registry is provided`).
  - Check 8: AppBlock duplicate output-port extensions (Issue #680).
- ✅ Line 164-165 implements the early-return: `if registry is None: return errors`.
- ✅ Same workflow produces different diagnostic depth depending on caller's choice to pass registry. The strictness variability IS real.
- ✅ MCP wrapper `validate_workflow` at `tools_workflow.py:482-520` always passes `registry=ctx.block_registry`, so MCP-side always runs strict mode.
- ⚠️ REST endpoint counterpart (`api/routes/workflows.py` or similar — NOT in my audit scope but mentioned by SSOT) not verified here.
- Recommended fix per SSOT (Option A: strict-only + reject when registry unavailable) is a reasonable c-impl decision. Option B (parameterize via `ValidateWorkflowResult.mode: Literal["strict", "degraded"]`) is also valid and less invasive — Phase 8 + ADR-NNN should pick one.

---

## Additional findings not enumerated in SSOT

### F-EXTRA-1 (P1) — SKILL.md tool catalog misses `get_block_config`
Same SKILL.md line 103 that lists the ghost `list_block_runs` ALSO drops the real `get_block_config` tool. Fix is BOTH-sided: remove ghost + add `get_block_config`.

### F-EXTRA-2 (P1) — `scieasy-inspect-data/SKILL.md` further drift
Beyond the `preview_data` args bug, the same skill file has:
- `get_block_output(run_id, node_id, port_name)` — actual is `(run_id, block_id, port)`.
- `inspect_data(ref="rf-099")` — actual requires `ref: dict[str, Any]` not a string ID.
- Worked example at lines 95–115 is **non-executable** as written; an agent following it gets multiple TypeErrors.

### F-EXTRA-3 (P2) — `cli/install.py:5` "25-tool MCP surface"
Second site for the 25→26 docstring drift; not flagged by SSOT entry E11.13.

### F-EXTRA-4 (P1) — `mcp = FastMCP(...)` name drift
`server.py:51`: `mcp: FastMCP = FastMCP(name="scieasy-mcp", version="0.1.0")`. SSOT shows `name="scieasy"` with `instructions=<see system_prompt.py>`. The actual name is `"scieasy-mcp"` and `instructions` is not passed. Likely intentional (the prompt is composed externally via `compose_system_prompt`), but SSOT should match code.

### F-EXTRA-5 (P1) — `compose_system_prompt` over-spec on overlays
SSOT docstring claims 4 splice ingredients (builtin base, project overlay, per-machine overlay, SKILL.md splice). Actual code only does (1) load SKILL.md, (2) splice tool_catalog, (3) splice project_context. **No project / per-machine overlay implemented**. Either spec these as deferred features or remove from SSOT.

### F-EXTRA-6 (P1) — `install_project_agent_assets` does NOT git-commit
SSOT step 9 ("Best-effort: git add + commit 'provision: scieasy agent assets v<version>'") is fictional. No `git add` or `git commit` invocation anywhere in `_orchestrate.py` or sub-step modules. Either implement the commit step or remove from SSOT.

### F-EXTRA-7 (P2) — `StandaloneMCPRuntime.start_workflow` missing from SSOT
Actual code has a `start_workflow(workflow_id)` method (`runtime.py:86-93`) that always raises RuntimeError (standalone bridges don't host scheduler). SSOT misses this method.

### F-EXTRA-8 (P2) — `default_socket_path(project_dir)` signature missed
SSOT shows `default_socket_path() -> Path` (no args). Actual `default_socket_path(project_dir: Path | None) -> Path`. Required argument missed.

### F-EXTRA-9 (P3) — `_ensure_mcp_config` idempotency overclaim
SSOT says "skips if file unchanged". Actual writes the file every call (idempotent w.r.t. content but not w.r.t. mtime). Cosmetic.

---

## Cross-cutting observations

1. **Pydantic return-model section (E11.2) is the largest SSOT drift**: every sample shape differs from code in field names + types. Phase 8 should auto-generate this section from `tools_*.py` Pydantic class definitions rather than maintaining sample code by hand.

2. **Parameter name normalisation needed**: code consistently uses `block_id` / `port` / `path` / `workflow_path`; SSOT often shows `node_id` / `port_name` / `workflow_id`. Pick one vocabulary and converge. Recommendation: code wins, SSOT updates. The skill files (E11.12 / F-EXTRA-2) reinforce this — they teach wrong names.

3. **`MultiEdit` matcher coverage**: SSOT shows pre-PR-#1047 matcher strings. Update SSOT to current `Edit|Write|MultiEdit`.

4. **Codex hook coverage REAL but not in SSOT**: `codex_config.py:45-75` defines 6 Codex hooks. SSOT entry E13.7 acknowledges this generally ("6 [hooks] sections") but the SSOT-shown TOML syntax (`[hooks.deny_scieasy_cli]`) is wrong shape; actual uses `[[hooks.PreToolUse]]` array-of-tables.

5. **`_orchestrate.py` step list reality**: 4 sub-steps + marker write (NO git commit). SSOT 9-step list over-specifies.

6. **`ProvisionResult` field types (E13.2)**: `list[Path]` vs `list[str]` matters for downstream consumers (`os.fspath` is implicit; but typing matters for IDE / mypy). Reconcile.

7. **Cross-track healthy signal**: of the 28 entries, the c-class (E11.11, E11.12, E11.13, E13.6, E13.8) are all 100% confirmed. SSOT is **strong** on identifying bugs (these are the high-value entries for Phase 9 issue rollout). SSOT is **weak** on capturing exact signatures (a-class entries with signature blocks).

---

## Phase 8 recommendations (priority order)

1. **P1 fixes (must land in Phase 8)**:
   - Rewrite E11.5 (MCPServer ctor: `socket_path, project_dir`, not `mcp, context`).
   - Rewrite E11.6 (StandaloneMCPRuntime 4 fields + Path | None for project_dir, default_socket_path, start_inprocess_server return type).
   - Rewrite E11.8 (spawn_claude/spawn_codex actual signatures with required `dangerous`, no `user_prompt`).
   - Rewrite E11.7 over-spec (drop overlay claims OR open issues for them).
   - Rewrite E13.1 (drop fictional git commit step 9, list actual 4 sub-steps + marker).
   - Rewrite E13.2 (field types: list[str] / tuple[str, str]).
   - Rewrite E13.4 + E13.9 (matcher strings include MultiEdit; command shape uses `$CLAUDE_PROJECT_DIR`).
   - Rewrite E13.7 (TOML uses `[[hooks.PreToolUse]]` array-of-tables; command uses `git rev-parse --show-toplevel`).
   - Extend E11.4 (the 4 missing Protocol attrs are `event_bus, workflow_runs, ai_block_run_dir, start_workflow` — NOT `scheduler`).
   - Land F-EXTRA-1 (SKILL.md catalog needs BOTH ghost removal AND `get_block_config` addition).
   - Land F-EXTRA-2 (rewrite scieasy-inspect-data/SKILL.md examples with correct param names + ref dict shape).
   - Land F-EXTRA-4 (MCP name + instructions claim).
   - Land F-EXTRA-6 (remove fictional git commit step).

2. **P2 fixes (should land in Phase 8)**:
   - E11.2 generate Pydantic return-model section from code.
   - E11.9 fix line range (L91-L106 → L94-L109).
   - E11.10 reflect actual `register(app)` + `_typer_command` registration pattern.
   - E11.13 + F-EXTRA-3 fix BOTH `_context.py:3` AND `cli/install.py:5` docstrings.
   - E11.15 re-tally §11 entry count (currently 13 entries; aggregate table says 15).
   - E13.3 anchor (`_orchestrate.py:38` definition; `__init__.py:16,22` re-export).
   - E13.10 verify skill body content matches "scaffold + #875 + §3.2a port rule" claims.
   - F-EXTRA-7 add `StandaloneMCPRuntime.start_workflow()`.
   - F-EXTRA-8 fix `default_socket_path(project_dir)` signature.

3. **P3 cosmetic**:
   - E13.12 issue field split (#1015 + #1016).
   - F-EXTRA-9 `_ensure_mcp_config` idempotency wording.

4. **Phase 9 issue rollout** — the following SSOT `#TBD-*` placeholders are READY to instantiate (each tracks a confirmed code-side bug):
   - `#TBD-mcpcontext-protocol-gap` (E11.4) — Protocol expansion to 6 attrs.
   - `#TBD-skill-md-ghost-tool-fix` (E11.11) — combined fix per F-EXTRA-1.
   - `#TBD-preview-data-skill-body-fix` (E11.12) — combined fix per F-EXTRA-2.
   - `#TBD-mcp-context-docstring-fix` (E11.13) — combined fix per F-EXTRA-3 (2 sites).
   - `#TBD-claude-agents-md-codex-hook-text-fix` (E13.6) — 5 occurrences in template.
   - `#TBD-codex-config-template-documentation-only-note` (E13.8) — add docstring header.
   - `#TBD-validator-strictness-context-dependent` (EX.1) — Option A vs B decision.

5. **Cross-cascade**: The cross-cutting validator-strictness entry is the most coherent in the set — Codex M06 R2 reasoning is fully reproducible from code (E14.1 above). Phase 8 should preserve the entry verbatim and let ADR-NNN pick the option.

---

## Methodology notes

- This audit performed full-file reads of: `_context.py`, `server.py`, `runtime.py` (mcp/), `tools_workflow.py`, `tools_authoring.py`, `tools_inspection.py`, `tools_qa.py`, `system_prompt.py`, `terminal.py`, `cli/install.py` (partial), `cli/mcp_bridge.py` (partial), `_orchestrate.py`, `hooks.py`, `codex_config.py`, `skills.py`, `_skills/scieasy/SKILL.md`, `_skills/scieasy/scieasy-inspect-data/SKILL.md`, `templates/claude_agents_md.md`, `workflow/validator.py` (lines 60–180).
- Grep used to locate `@mcp.tool` decorators (26 confirmed), `SCIEASY_PROVISION_VERSION` (3 sites), `install_project_agent_assets` (4 sites for lifecycle wiring), `getattr(ctx, ...)` (3 missing-attr usage sites).
- Filesystem `ls` used to verify `_registry.py` absence (✅ does not exist), 6 hook templates exist, 5 task skill dirs exist (each with SKILL.md).
- Line numbers in this report are from the actual code at HEAD of `track/adr-040` branch on `agent-a3880f0f45770a395` worktree.
- No code modifications, no commits, no PRs created during this audit.

---

## Exit confirmation

- ✅ ONE file written: `docs/audit/2026-05-17-spec-ssot-p7-Z7.md`.
- ✅ All 28 entries (15 §11 + 12 §13 + 1 cross-cutting) addressed.
- ✅ Filesystem verification of `_registry.py` absence.
- ✅ Line-number citations for every drift finding.
- ✅ `list_block_runs` claim verified DEFINITIVELY against SKILL.md:103.
- ✅ No fixes pushed.
- ✅ No peer reports read.
- ✅ Scope discipline maintained.

End of Z7 report.
