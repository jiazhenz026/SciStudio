# Previewers are yours to write

A previewer is a small piece of code that renders a type — you wrote one in
level 2 to show Image as an actual image.

That is the whole pattern: make a type, then decide how it should *look*.
A previewer in your project serves that project; promoted to My Library, it
follows the type everywhere you use it.
