# Previewer

The pane on the right shows the output of whatever block you select — you
have leaned on it in every level.

When the sidebar is too small for the data, the magnifier lifts the preview
into a **floating window over the canvas**. Esc closes it; selecting a
different block swaps it for that block's view.
