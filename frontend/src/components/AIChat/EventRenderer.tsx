/**
 * Stub dispatcher that renders a single canonical AgentEvent.
 * T-ECA-303 implements the per-kind UI per ADR-033 §3 D5.1.
 */
export function EventRenderer() {
  return <div>EventRenderer (T-ECA-303 will implement)</div>;
}
