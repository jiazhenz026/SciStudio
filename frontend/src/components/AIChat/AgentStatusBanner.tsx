/**
 * Stub status banner. Renders `not_installed` / `installed_not_logged_in` /
 * `bypass_mode_active` states once T-ECA-303 wires `GET /api/ai/status`.
 */
export function AgentStatusBanner() {
  return <div>AgentStatusBanner (T-ECA-303 will implement)</div>;
}
