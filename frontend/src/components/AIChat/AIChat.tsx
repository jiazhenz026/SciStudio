/**
 * Top-level AIChat container (stub).
 *
 * T-ECA-303 will compose the real component tree per ADR-033 §3 D5.1:
 *   SessionSidebar | (AgentStatusBanner + ChatMessageList + input bar)
 *   PermissionPrompt modal overlay
 *   SettingsPanel
 */
export function AIChat() {
  return <div>AIChat (T-ECA-303 will implement)</div>;
}
