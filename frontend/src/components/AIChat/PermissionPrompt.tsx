/**
 * Stub modal for tool permission prompts.
 * T-ECA-303 implements approve / deny / always-allow + Monaco diff preview.
 */
export function PermissionPrompt() {
  return <div>PermissionPrompt (T-ECA-303 will implement)</div>;
}
