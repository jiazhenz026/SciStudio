frontend/
app/
backend/

# #1855: alpha check-in webhook URL — never commit (it would let anyone spam the
# Slack channel). Stamped into the build locally; ALPHA-ONLY, removed in beta.
alpha-checkin.json

packages/*
!packages/.gitkeep
!packages/README.md

git/*
!git/.gitkeep

python/*
!python/.gitkeep
# #1775: written by the staging scripts at build time (channel + manifest URL).
# A committed copy carries whatever channel the last local build used.
ota-config.json
